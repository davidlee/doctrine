id = 161
slug = "kind-registry-dry"
title = "DRY kind-registry seam: record-membership predicate + numbered-kind identity table"
status = "done"
created = "2026-06-26"
updated = "2026-06-26"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/kinds.rs"
intent = "design-target"

[[selector]]
selector = "src/integrity.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/dep_seq.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/partition.rs"
intent = "design-target"

[[selector]]
selector = "src/search.rs"
intent = "design-target"

[[selector]]
selector = "src/tag.rs"
intent = "design-target"

[[selector]]
selector = "src/relation.rs"
intent = "design-target"

[[selector]]
selector = "src/relation_graph.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/catalog/scan.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/test_helpers.rs"
intent = "design-target"

[[selector]]
selector = "src/knowledge.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/supersede.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/supersede.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/superserde.rs"
intent = "scope-relevant"

[[relation]]
label = "related"
target = "IMP-184"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-008"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-019"

[[relation]]
label = "governed_by"
target = "ADR-001"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
