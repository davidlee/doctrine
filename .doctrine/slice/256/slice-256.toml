id = 256
slug = "recording-an-act-emits-a-change-row"
title = "Recording an act emits a change row"
status = "design"
created = "2026-08-16"
updated = "2026-08-16"
tags = ["area:correctness", "area:design-run", "cluster:design-run", "footgun"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/design_run/change_log.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/design_run/run.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/design_run/render/**"
intent = "scope-relevant"

[[selector]]
selector = "src/design_run/bounds.rs"
intent = "scope-relevant"

[[selector]]
selector = "tests/e2e_design_state.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/design_run/snapshot.rs"
intent = "scope-relevant"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-355"

[[relation]]
label = "references"
role = "concerns"
descriptor = "SL-251 design.md 422-428 argues from this defect being unrepaired; needs a one-clause touch at its reconcile"
target = "SL-251"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the absorption half of the same pair; not repaired here"
target = "ISS-346"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-029"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
