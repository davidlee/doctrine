id = 226
slug = "cli-graph-emitter-and-ego-view"
title = "CLI graph emitter and ego-view"
status = "ready"
created = "2026-07-24"
updated = "2026-07-24"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/commands/**"
intent = "scope-relevant"

[[selector]]
selector = "src/catalog/**"
intent = "scope-relevant"

[[selector]]
selector = "src/concept_map.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/graph.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/dot.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/graph.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"

[[selector]]
selector = ".doctrine/adr/001/layering.toml"
intent = "design-target"

[[relation]]
label = "fulfils"
target = "IDE-043"

[[relation]]
label = "references"
role = "implements"
target = "PRD-016"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-001"

[[relation]]
label = "related"
target = "DEC-007"

[[relation]]
label = "related"
target = "DEC-008"

[[relation]]
label = "related"
target = "DEC-009"

[[relation]]
label = "related"
target = "ISS-237"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
