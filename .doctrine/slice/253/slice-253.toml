id = 253
slug = "conformance-verdict-kernel"
title = "Conformance verdict kernel"
status = "design"
created = "2026-08-12"
updated = "2026-08-12"
tags = ["area:confinement", "capsule", "cluster:capsule", "conformance"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "crates/doctrine-control/src/**"
intent = "scope-relevant"

[[selector]]
selector = "justfile"
intent = "design-target"
note = "capsule-verify at :145 runs 'backend verify' and four comments name it — DEC-194's rename reaches the recipe, not just the binary."

[[selector]]
selector = "crates/doctrine-control/src/conformance.rs"
intent = "design-target"
note = "Redundant against the scope-relevant crates/doctrine-control/src/** glob by path, retained by intent: the glob says the slice touches the crate, this says the design commits to changing this file."

[[selector]]
selector = "crates/doctrine-control/src/main.rs"
intent = "design-target"
note = "Same intent-vs-path overlap as conformance.rs. Commitment is admit/render_outcome, the exit constants and the verb — DEC-191 and DEC-194."

[[selector]]
selector = ".doctrine/adr/001/layering.toml"
intent = "design-target"

[[selector]]
selector = "crates/doctrine-control/src/qualification.rs"
intent = "design-target"
note = "Does not exist yet — the kernel unit DEC-197 creates. 'unmatched' in selector doctor is the correct state until the split phase lands; name provisional per DEC-198."

[[relation]]
label = "governed_by"
target = "ADR-020"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "references"
role = "concerns"
descriptor = "action (1) — the epistemic kernel"
target = "RFC-025"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-030"

[[relation]]
label = "related"
target = "DEC-189"

[[relation]]
label = "related"
target = "DEC-190"

[[relation]]
label = "related"
target = "DEC-191"

[[relation]]
label = "related"
target = "DEC-194"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
