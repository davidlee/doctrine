id = 253
slug = "conformance-verdict-kernel"
title = "Conformance verdict kernel"
status = "proposed"
created = "2026-08-12"
updated = "2026-08-12"
tags = ["area:confinement", "capsule", "cluster:capsule", "conformance"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "crates/doctrine-control/src/**"
intent = "scope-relevant"

[[selector]]
selector = "justfile"
intent = "scope-relevant"

[[relation]]
label = "governed_by"
target = "ADR-020"

[[relation]]
label = "references"
role = "concerns"
descriptor = "action (1) — the epistemic kernel"
target = "RFC-025"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-030"

[[relation]]
label = "related"
target = "DEC-189"

[[relation]]
label = "related"
target = "DEC-190"

[[relation]]
label = "related"
target = "DEC-191"

[[relation]]
label = "related"
target = "DEC-194"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
