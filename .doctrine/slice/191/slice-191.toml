id = 191
slug = "worker-contract-hymns-check-cadence"
title = "Dispatch worker contract: cascade hymns + check-cadence + role/model/stage bake"
status = "design"
created = "2026-07-03"
updated = "2026-07-03"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = [{ to = "SL-192", rank = 0 }, { to = "SL-193", rank = 0 }]                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "install/hymns/**"
intent = "scope-relevant"

[[selector]]
selector = ".doctrine/hymns/**"
intent = "scope-relevant"

[[selector]]
selector = "src/install.rs"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/dispatch/**"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-agent/**"
intent = "scope-relevant"

[[selector]]
selector = "install/hymns/role/worker.md"
intent = "design-target"

[[selector]]
selector = "install/hymns/model/adherence/low.md"
intent = "design-target"

[[selector]]
selector = "src/hymns.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/prompt.rs"
intent = "design-target"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"

[[selector]]
selector = "install/agents/pi/dispatch-worker.md"
intent = "design-target"

[[relation]]
label = "governed_by"
target = "ADR-011"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-197"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-023"

[[relation]]
label = "related"
target = "RFC-005"

[[relation]]
label = "related"
target = "SL-186"

[[relation]]
label = "related"
target = "SL-187"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
