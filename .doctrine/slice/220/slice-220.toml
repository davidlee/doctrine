id = 220
slug = "value-claims"
title = "Ledgered value claims"
status = "ready"
created = "2026-07-16"
updated = "2026-07-16"
tags = []

[relationships]
needs = ["SL-219"]                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/comparison/**"
intent = "scope-relevant"

[[selector]]
selector = "src/priority/**"
intent = "scope-relevant"

[[selector]]
selector = "src/value.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/facet.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/facet_write.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/facet.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/compare.rs"
intent = "design-target"

[[selector]]
selector = "src/main.rs"
intent = "design-target"

[[selector]]
selector = "scripts/**"
intent = "scope-relevant"

[[selector]]
selector = "src/comparison/wire.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/resolve.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/claims.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/store.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/graph.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/surface.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/render.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/view.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/config.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/elicit.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/findings.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"

[[selector]]
selector = "scripts/migrate_value_facets.py"
intent = "design-target"

[[selector]]
selector = "scripts/value_baseline.py"
intent = "design-target"

[[selector]]
selector = "tests/e2e_value_claims.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/compile.rs"
intent = "design-target"
note = "PHASE-02 adjudicated mechanical fallout of §1 struct motion (worker STOP resolution)"

[[selector]]
selector = "src/comparison/project.rs"
intent = "design-target"
note = "PHASE-02 adjudicated mechanical fallout of §1 struct motion (worker STOP resolution)"

[[selector]]
selector = "src/comparison/query.rs"
intent = "design-target"
note = "PHASE-02 adjudicated mechanical fallout of §1 struct motion (worker STOP resolution)"

[[selector]]
selector = "tests/e2e_estimate_non_blocking.rs"
intent = "design-target"
note = "PHASE-02 adjudicated mechanical fallout of §1 struct motion (worker STOP resolution)"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"
note = "PHASE-04 adjudicated: ValueAction clap enum + Command::Value dispatch arm live only in cli.rs — no alternative seam for the Pin subcommand (worker deviation accepted)"

[[selector]]
selector = "tests/e2e_compare_elicit.rs"
intent = "design-target"
note = "PHASE-05 R2 class-b golden churn: the design enumerates goldens authoring [value] facets as designed flip churn (design §3 behaviour-change accounting, EX-2)"

[[selector]]
selector = "tests/e2e_compare_estimate.rs"
intent = "design-target"
note = "PHASE-05 R2 class-b golden churn: the design enumerates goldens authoring [value] facets as designed flip churn (design §3 behaviour-change accounting, EX-2)"

[[selector]]
selector = "tests/e2e_compare_inference.rs"
intent = "design-target"
note = "PHASE-05 R2 class-b golden churn: the design enumerates goldens authoring [value] facets as designed flip churn (design §3 behaviour-change accounting, EX-2)"

[[selector]]
selector = "tests/e2e_priority_golden.rs"
intent = "design-target"
note = "PHASE-05 R2 class-b golden churn: the design enumerates goldens authoring [value] facets as designed flip churn (design §3 behaviour-change accounting, EX-2)"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/backlog.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/governance.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/memory.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/rec.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/review.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/revision.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/spec.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/concept_map.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/adr.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/standard.rs"
intent = "design-target"
note = "PHASE-06 §6 show value-line re-source: every kind module renders the facet value row via format_value_normal (9-fold duplicated pattern); design mandates re-sourcing from the comparison pipeline"

[[selector]]
selector = "src/lazyspec.rs"
intent = "design-target"
note = "PHASE-06 EX-3 completion: spec::render / memory::render_show are shared seams — these two callers must move with the signature change (worker-surfaced scope gap)"

[[selector]]
selector = "src/retrieve.rs"
intent = "design-target"
note = "PHASE-06 EX-3 completion: spec::render / memory::render_show are shared seams — these two callers must move with the signature change (worker-surfaced scope gap)"

[[relation]]
label = "governed_by"
target = "ADR-015"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-290"

[[relation]]
label = "references"
role = "originates_from"
target = "IDE-039"

[[relation]]
label = "related"
target = "RFC-020"

[[relation]]
label = "related"
target = "REV-024"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
