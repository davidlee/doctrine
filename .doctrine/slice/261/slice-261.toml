id = 261
slug = "design-adopt-verb"
title = "Design adopt verb"
status = "ready"
created = "2026-09-23"
updated = "2026-09-24"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/commands/design.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/run.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/submission.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/refusal.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/payload_contract.rs"
intent = "design-target"

[[selector]]
selector = "install/design-payload-contract.md"
intent = "design-target"

[[selector]]
selector = "install/hymns/stage/design.md"
intent = "design-target"

[[selector]]
selector = "install/design-prompts/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/e2e_design_*.rs"
intent = "scope-relevant"

[[selector]]
selector = "Cargo.toml"
intent = "design-target"

[[selector]]
selector = "src/design_run/contract_check.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/tests.rs"
intent = "design-target"

[[selector]]
selector = "install/design-prompts/drafting.md"
intent = "design-target"

[[selector]]
selector = "tests/e2e_design_state.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_design_materialise.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_design_delegation.rs"
intent = "design-target"

[[relation]]
label = "fulfils"
target = "ISS-320"

[[relation]]
label = "fulfils"
target = "ISS-348"

[[relation]]
label = "references"
role = "concerns"
descriptor = "adoption friction reported under this item; its own faces stay open"
target = "IMP-390"

[[relation]]
label = "references"
role = "concerns"
descriptor = "rule 2 (adopt_authored protocol) is superseded by this slice's decision"
target = "DEC-100"

[[relation]]
label = "references"
role = "concerns"
descriptor = "adoption crossing specified here; revision needed"
target = "SPEC-029"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
