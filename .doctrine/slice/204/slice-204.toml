id = 204
slug = "extract-per-kind-validation-contract-to-break-integrity-coupling"
title = "Extract per-kind validation contract to break integrity coupling"
status = "reconcile"
created = "2026-07-04"
updated = "2026-07-24"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = [{ to = "SL-203", rank = 0 }]                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/integrity.rs"
intent = "design-target"

[[selector]]
selector = "src/adr.rs"
intent = "design-target"

[[selector]]
selector = "src/backlog.rs"
intent = "design-target"

[[selector]]
selector = "src/concept_map.rs"
intent = "design-target"

[[selector]]
selector = "src/knowledge.rs"
intent = "design-target"

[[selector]]
selector = "src/policy.rs"
intent = "design-target"

[[selector]]
selector = "src/rec.rs"
intent = "design-target"

[[selector]]
selector = "src/requirement.rs"
intent = "design-target"

[[selector]]
selector = "src/review.rs"
intent = "design-target"

[[selector]]
selector = "src/revision.rs"
intent = "design-target"

[[selector]]
selector = "src/rfc.rs"
intent = "design-target"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"

[[selector]]
selector = "src/spec.rs"
intent = "design-target"

[[selector]]
selector = "src/standard.rs"
intent = "design-target"

[[selector]]
selector = ".doctrine/adr/001/layering.toml"
intent = "design-target"

[[selector]]
selector = "tests/architecture_layering.rs"
intent = "design-target"

[[selector]]
selector = "src/kinds.rs"
intent = "design-target"

[[selector]]
selector = "src/kinds/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/kinds/resolve.rs"
intent = "design-target"

[[selector]]
selector = "src/entity.rs"
intent = "design-target"

[[selector]]
selector = "src/governance.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/hydrate.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/scan.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/dep_seq.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/doctor.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/facet.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/map.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/relation.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/supersede.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/tag.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/validate.rs"
intent = "design-target"

[[selector]]
selector = "src/doctor_checks.rs"
intent = "design-target"

[[selector]]
selector = "src/map_server/markdown.rs"
intent = "design-target"

[[selector]]
selector = "src/map_server/routes.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/graph.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/surface.rs"
intent = "design-target"

[[selector]]
selector = "src/reconcile.rs"
intent = "design-target"

[[selector]]
selector = "src/relation.rs"
intent = "design-target"

[[selector]]
selector = "src/relation_graph.rs"
intent = "design-target"

[[selector]]
selector = "src/relation_query.rs"
intent = "design-target"

[[selector]]
selector = "src/search.rs"
intent = "design-target"

[[selector]]
selector = "src/supersede.rs"
intent = "design-target"

[[selector]]
selector = "src/lazyspec.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/compare.rs"
intent = "design-target"

[[selector]]
selector = "src/listing.rs"
intent = "design-target"

[[selector]]
selector = "src/memory.rs"
intent = "design-target"
note = "Collateral gate-blocker fix, not SL-204 design surface: ISS-235 (vt9 surface test non-hermetic on ambient CLAUDE_PROJECT_DIR) false-reds worker_commit's gate; fixed hermetic in PHASE-04's worker delta so the canonical_id delta can land."

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-266"

[[relation]]
label = "related"
target = "SL-016"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
