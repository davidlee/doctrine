id = 180
slug = "selector-conformance-hardening"
title = "Selector conformance hardening: design-time dry-run + import-belt scope-creep refusal"
status = "ready"
created = "2026-06-30"
updated = "2026-07-02"
tags = ["area:conformance", "area:dispatch"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/conformance.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/import.rs"
intent = "design-target"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/mod.rs"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-agent/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-subprocess/SKILL.md"
intent = "design-target"

[[relation]]
label = "references"
role = "concerns"
target = "IMP-199"

[[relation]]
label = "references"
role = "concerns"
target = "IMP-204"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-004"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-005"

[[relation]]
label = "references"
role = "concerns"
target = "SL-168"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
