id = 232
slug = "corpus-aware-memory-verify-gate"
title = "Corpus-aware memory verify gate"
status = "proposed"
created = "2026-07-26"
updated = "2026-07-26"
tags = []

[relationships]
needs = ["REV-034"]                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-007"

[[relation]]
label = "references"
role = "concerns"
descriptor = "non-contribution is reported, not classified"
target = "DEC-020"

[[relation]]
label = "references"
role = "concerns"
descriptor = "body digest — same schema change as the declared boundary"
target = "QUE-173"

[[relation]]
label = "references"
role = "concerns"
descriptor = "gates objective 4"
target = "QUE-175"

[[relation]]
label = "references"
role = "originates_from"
target = "SL-230"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
