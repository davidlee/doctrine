id = 227
slug = "library-and-minimal-projection"
title = "Library read surface and minimal projection"
status = "done"
created = "2026-07-24"
updated = "2026-07-25"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = [{ to = "SL-223", rank = 0 }]                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/install.rs"
intent = "design-target"

[[selector]]
selector = "src/asset_source.rs"
intent = "design-target"

[[selector]]
selector = "src/search.rs"
intent = "scope-relevant"

[[selector]]
selector = "install/**"
intent = "scope-relevant"

[[selector]]
selector = "src/publication.rs"
intent = "design-target"

[[selector]]
selector = "install/manifest.toml"
intent = "design-target"

[[selector]]
selector = "install/doctrine.toml"
intent = "design-target"

[[selector]]
selector = "install/project-orientation.md"
intent = "design-target"

[[selector]]
selector = "publication/manifest.toml"
intent = "design-target"

[[selector]]
selector = "src/commands/library.rs"
intent = "design-target"
note = "PHASE-02: command tier lives in src/commands/ (not top-level/main.rs) — design §5.2 predicted the stale layout; ADR-001 layering forbids an unclassified top-level module"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"
note = "PHASE-02: command tier lives in src/commands/ (not top-level/main.rs) — design §5.2 predicted the stale layout; ADR-001 layering forbids an unclassified top-level module"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"
note = "PHASE-02: command tier lives in src/commands/ (not top-level/main.rs) — design §5.2 predicted the stale layout; ADR-001 layering forbids an unclassified top-level module"

[[selector]]
selector = "src/commands/mod.rs"
intent = "design-target"
note = "PHASE-02: command tier lives in src/commands/ (not top-level/main.rs) — design §5.2 predicted the stale layout; ADR-001 layering forbids an unclassified top-level module"

[[selector]]
selector = "tests/e2e_policy_install_commit.rs"
intent = "design-target"
note = "PHASE-03: FR-008 lazy roots — migrate fresh_install_scaffolds_the_<kind>_tree from eager-install to lazy first-scaffold (design §9 omitted these e2e tests)"

[[selector]]
selector = "tests/e2e_knowledge_install_commit.rs"
intent = "design-target"
note = "PHASE-03: FR-008 lazy roots — migrate fresh_install_scaffolds_the_<kind>_tree from eager-install to lazy first-scaffold (design §9 omitted these e2e tests)"

[[selector]]
selector = "tests/e2e_standard_install_commit.rs"
intent = "design-target"
note = "PHASE-03: FR-008 lazy roots — migrate fresh_install_scaffolds_the_<kind>_tree from eager-install to lazy first-scaffold (design §9 omitted these e2e tests)"

[[selector]]
selector = "tests/e2e_revision_install_commit.rs"
intent = "design-target"
note = "PHASE-03: FR-008 lazy roots — migrate fresh_install_scaffolds_the_<kind>_tree from eager-install to lazy first-scaffold (design §9 omitted these e2e tests)"

[[relation]]
label = "governed_by"
target = "ADR-019"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-026"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-009"

[[relation]]
label = "references"
role = "implements"
target = "PRD-017"

[[relation]]
label = "references"
role = "implements"
target = "PRD-006"

[[relation]]
label = "references"
role = "concerns"
target = "QUE-172"

[[relation]]
label = "references"
role = "concerns"
target = "ASM-003"

[[relation]]
label = "references"
role = "concerns"
target = "DEC-010"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
