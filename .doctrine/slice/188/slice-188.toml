id = 188
slug = "cli-id-form-normalisation"
title = "CLI id-form normalisation: accept both prefixed and bare canonical ids uniformly"
status = "abandoned"
created = "2026-07-02"
updated = "2026-07-02"
tags = []

[relationships]
needs = ["SL-184"]                      # slice→work hard prerequisite
after = [{ to = "SL-185", rank = 0 }]                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/governance.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/integrity.rs"
intent = "design-target"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"

[[selector]]
selector = "src/review.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/relation.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/facets.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/explore.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/relation.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/facet.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/tag.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/dep_seq.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/supersede.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/map.rs"
intent = "design-target"

[[relation]]
label = "related"
target = "IMP-227"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-011"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
