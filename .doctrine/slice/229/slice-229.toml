id = 229
slug = "pre-design-research-stage-v1"
title = "Pre-design research stage v1"
status = "design"
created = "2026-07-24"
updated = "2026-07-24"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "plugins/doctrine/skills/**"
intent = "scope-relevant"

[[selector]]
selector = ".agents/skills/**"
intent = "scope-relevant"

[[selector]]
selector = "src/**"
intent = "scope-relevant"

[[selector]]
selector = "install/**"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/skills/research/**"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/slice/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/design/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/plan/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/phase-plan/SKILL.md"
intent = "design-target"

[[selector]]
selector = "src/research.rs"
intent = "design-target"

[[selector]]
selector = "src/main.rs"
intent = "design-target"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"

[[selector]]
selector = "src/contentset.rs"
intent = "design-target"

[[selector]]
selector = "install/governance.md"
intent = "design-target"

[[selector]]
selector = "install/glossary.md"
intent = "design-target"

[[relation]]
label = "references"
role = "originates_from"
target = "IDE-044"

[[relation]]
label = "related"
target = "RFC-011"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
