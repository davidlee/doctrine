id = 154
slug = "conformance-registry-capture"
title = "Reliable conformance-registry capture"
status = "design"
created = "2026-06-25"
updated = "2026-06-25"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/state.rs"
intent = "design-target"
note = "conformance-registry capture seam (solo binding + funnel write)"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"
note = "conformance-registry capture seam (solo binding + funnel write)"

[[selector]]
selector = "src/ledger.rs"
intent = "design-target"
note = "read_boundaries_file working-ledger reader for the commit splice (OQ-4)"

[[selector]]
selector = "src/boundary.rs"
intent = "design-target"
note = "provenance field on BoundaryRow (D12, pass-5 reshape)"

[[selector]]
selector = "plugins/doctrine/skills/dispatch/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-agent/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-subprocess/SKILL.md"
intent = "design-target"

[[selector]]
selector = "src/git.rs"
intent = "design-target"
note = "live_worktree_for_ref liveness wrapper (P2-2/D9)"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"
note = "run_record_delta provenance-preserving (Rev 5, P6-1 BLOCKER fix)"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-004"

[[relation]]
label = "related"
target = "ISS-051"

[[relation]]
label = "related"
target = "ISS-052"

[[relation]]
label = "related"
target = "ISS-039"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
