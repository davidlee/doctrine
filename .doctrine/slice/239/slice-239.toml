id = 239
slug = "dispatch-trunk-promotion-and-close-time-edge-gate"
title = "Dispatch trunk promotion and close-time edge gate"
status = "design"
created = "2026-07-29"
updated = "2026-07-29"
tags = ["area:close", "area:dispatch", "area:lifecycle", "cluster:close-lineage"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/dispatch.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/slice.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/git.rs"
intent = "scope-relevant"

[[selector]]
selector = "AGENTS.md"
intent = "scope-relevant"

[[selector]]
selector = ".agents/skills/dispatch*/**"
intent = "scope-relevant"

[[relation]]
label = "governed_by"
target = "ADR-006"

[[relation]]
label = "governed_by"
target = "ADR-011"

[[relation]]
label = "governed_by"
target = "ADR-012"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-267"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-308"

[[relation]]
label = "references"
role = "concerns"
descriptor = "generalises SL-126's third close-gate over the journal row's target ref"
target = "SL-126"

[[relation]]
label = "references"
role = "concerns"
descriptor = "adjacent base-freshness seam: refresh-base advances the dispatch branch, promotion advances trunk"
target = "SL-127"

[[relation]]
label = "references"
role = "concerns"
descriptor = "declared Non-Goal: FF-gating the --edge leg itself"
target = "RSK-230"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
