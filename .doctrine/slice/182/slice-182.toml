id = 182
slug = "claude-confinement-hooks"
title = "Claude-arm subagent write-confinement hooks"
status = "started"
created = "2026-06-30"
updated = "2026-06-30"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = ".claude/skills/doctrine/hooks/**"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/**"
intent = "scope-relevant"

[[selector]]
selector = "src/worktree/**"
intent = "scope-relevant"

[[selector]]
selector = "scripts/pi-spawn-confined.sh"
intent = "scope-relevant"

[[selector]]
selector = "src/worktree/jail.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/pretooluse.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/shared.rs"
intent = "design-target"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"

[[selector]]
selector = ".claude/skills/dispatch-agent/SKILL.md"
intent = "design-target"

[[selector]]
selector = "src/worktree/create.rs"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/hooks/hooks.json"
intent = "design-target"

[[selector]]
selector = "src/skills.rs"
intent = "design-target"

[[relation]]
label = "governed_by"
target = "ADR-008"

[[relation]]
label = "governed_by"
target = "ADR-006"

[[relation]]
label = "references"
role = "originates_from"
target = "RSK-014"

[[relation]]
label = "references"
role = "concerns"
target = "IMP-045"

[[relation]]
label = "related"
target = "SL-181"

[[relation]]
label = "related"
target = "IDE-024"

[[relation]]
label = "related"
target = "IDE-025"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
