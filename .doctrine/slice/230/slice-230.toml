id = 230
slug = "memory-body-write-seam"
title = "Memory body-write seam"
status = "design"
created = "2026-07-25"
updated = "2026-07-25"
tags = ["area:cli", "area:mcp", "area:memory"]

[relationships]
needs = []                      # slice→work hard prerequisite; REV-034 moved to SL-232 (DEC-027) — hand-edited, no removal verb exists (ISS-247)
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/memory.rs"
intent = "design-target"

[[selector]]
selector = "src/mcp_server/tools.rs"
intent = "design-target"

[[selector]]
selector = ".doctrine/spec/tech/007/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/**"
intent = "scope-relevant"

[[selector]]
selector = "src/entity.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_mcp_server.rs"
intent = "design-target"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-221"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-007"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the attestation defect SL-230 closes"
target = "EVD-001"

[[relation]]
label = "references"
role = "concerns"
descriptor = "residual digest-based invalidation deferred from SL-230"
target = "QUE-173"

[[relation]]
label = "references"
role = "concerns"
descriptor = "retrieve-side adoption of the claim-surface constructor"
target = "QUE-175"

[[relation]]
label = "references"
role = "concerns"
descriptor = "verify gate split out by DEC-027"
target = "SL-232"

[[relation]]
label = "related"
target = "CHR-035"

[[relation]]
label = "related"
target = "SL-164"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
