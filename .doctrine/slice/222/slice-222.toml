id = 222
slug = "estimate-claims"
title = "Ledgered estimate claims"
status = "reconcile"
created = "2026-07-17"
updated = "2026-07-18"
tags = []

[relationships]
needs = ["SL-219", "SL-220"]                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/comparison/**"
intent = "scope-relevant"

[[selector]]
selector = "src/priority/**"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/facet.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/compare.rs"
intent = "design-target"

[[selector]]
selector = "src/estimate.rs"
intent = "design-target"

[[selector]]
selector = "src/facet.rs"
intent = "design-target"

[[selector]]
selector = "src/facet_write.rs"
intent = "design-target"

[[selector]]
selector = "src/main.rs"
intent = "design-target"

[[selector]]
selector = "scripts/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/**"
intent = "scope-relevant"

[[selector]]
selector = "src/comparison/wire.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/claims.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/store.rs"
intent = "design-target"

[[selector]]
selector = "src/value.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/graph.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/surface.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/elicit.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/view.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/render.rs"
intent = "design-target"

[[selector]]
selector = "src/priority/findings.rs"
intent = "design-target"

[[selector]]
selector = "scripts/migrate_estimate_facets.py"
intent = "design-target"

[[selector]]
selector = "tests/e2e_estimate_claims.rs"
intent = "design-target"

[[selector]]
selector = ".doctrine/adr/015/**"
intent = "design-target"

[[selector]]
selector = "src/catalog/scan.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/hydrate.rs"
intent = "design-target"

[[selector]]
selector = "src/catalog/graph.rs"
intent = "design-target"

[[selector]]
selector = "src/backlog.rs"
intent = "design-target"

[[selector]]
selector = "src/governance.rs"
intent = "design-target"

[[selector]]
selector = "src/concept_map.rs"
intent = "design-target"

[[selector]]
selector = "src/lazyspec.rs"
intent = "design-target"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison/compile.rs"
intent = "design-target"
note = "PHASE-02 initializer ripple of the est_lower/est_upper columns (design §1: resolve.rs non-semantic; struct-literal sites must name new fields)"

[[selector]]
selector = "src/comparison/project.rs"
intent = "design-target"
note = "PHASE-02 initializer ripple of the est_lower/est_upper columns (design §1: resolve.rs non-semantic; struct-literal sites must name new fields)"

[[selector]]
selector = "src/comparison/query.rs"
intent = "design-target"
note = "PHASE-02 initializer ripple of the est_lower/est_upper columns (design §1: resolve.rs non-semantic; struct-literal sites must name new fields)"

[[selector]]
selector = "src/comparison/resolve.rs"
intent = "design-target"
note = "PHASE-02 initializer ripple of the est_lower/est_upper columns (design §1: resolve.rs non-semantic; struct-literal sites must name new fields)"

[[selector]]
selector = "tests/e2e_estimate_non_blocking.rs"
intent = "design-target"
note = "NF-001 allowlist enrolls each designed estimate::/value:: exposure (E13 at PHASE-03; flip/deletion phases will extend)"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"
note = "PHASE-06 flip surfaces: estimate pin verb wiring (cli), worker-refused write class (guard), compare-estimate goldens (design §4/§6)"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"
note = "PHASE-06 flip surfaces: estimate pin verb wiring (cli), worker-refused write class (guard), compare-estimate goldens (design §4/§6)"

[[selector]]
selector = "tests/e2e_compare_estimate.rs"
intent = "design-target"
note = "PHASE-06 flip surfaces: estimate pin verb wiring (cli), worker-refused write class (guard), compare-estimate goldens (design §4/§6)"

[[selector]]
selector = "src/memory.rs"
intent = "design-target"
note = "PHASE-07 show-line ripple: estimate_line threads every entity render/format_show seam (design §6 — same call-site family as the SL-220 value line)"

[[selector]]
selector = "src/rec.rs"
intent = "design-target"
note = "PHASE-07 show-line ripple: estimate_line threads every entity render/format_show seam (design §6 — same call-site family as the SL-220 value line)"

[[selector]]
selector = "src/retrieve.rs"
intent = "design-target"
note = "PHASE-07 show-line ripple: estimate_line threads every entity render/format_show seam (design §6 — same call-site family as the SL-220 value line)"

[[selector]]
selector = "src/review.rs"
intent = "design-target"
note = "PHASE-07 show-line ripple: estimate_line threads every entity render/format_show seam (design §6 — same call-site family as the SL-220 value line)"

[[selector]]
selector = "src/revision.rs"
intent = "design-target"
note = "PHASE-07 show-line ripple: estimate_line threads every entity render/format_show seam (design §6 — same call-site family as the SL-220 value line)"

[[selector]]
selector = "src/spec.rs"
intent = "design-target"
note = "PHASE-07 show-line ripple: estimate_line threads every entity render/format_show seam (design §6 — same call-site family as the SL-220 value line)"

[[selector]]
selector = "src/doctor_checks.rs"
intent = "design-target"
note = "PHASE-09 deletion ripple: facet-read consumers outside the F-5 named list (doctor/knowledge/map/reconcile/relation surfaces) — the design's 'replicated well beyond EntityFacets' clause"

[[selector]]
selector = "src/estimate/display.rs"
intent = "design-target"
note = "PHASE-09 deletion ripple: facet-read consumers outside the F-5 named list (doctor/knowledge/map/reconcile/relation surfaces) — the design's 'replicated well beyond EntityFacets' clause"

[[selector]]
selector = "src/knowledge.rs"
intent = "design-target"
note = "PHASE-09 deletion ripple: facet-read consumers outside the F-5 named list (doctor/knowledge/map/reconcile/relation surfaces) — the design's 'replicated well beyond EntityFacets' clause"

[[selector]]
selector = "src/map_server/routes.rs"
intent = "design-target"
note = "PHASE-09 deletion ripple: facet-read consumers outside the F-5 named list (doctor/knowledge/map/reconcile/relation surfaces) — the design's 'replicated well beyond EntityFacets' clause"

[[selector]]
selector = "src/reconcile.rs"
intent = "design-target"
note = "PHASE-09 deletion ripple: facet-read consumers outside the F-5 named list (doctor/knowledge/map/reconcile/relation surfaces) — the design's 'replicated well beyond EntityFacets' clause"

[[selector]]
selector = "src/relation_query.rs"
intent = "design-target"
note = "PHASE-09 deletion ripple: facet-read consumers outside the F-5 named list (doctor/knowledge/map/reconcile/relation surfaces) — the design's 'replicated well beyond EntityFacets' clause"

[[selector]]
selector = "tests/e2e_compare_elicit.rs"
intent = "design-target"
note = "orchestrator fixture-conversion (a2775657a); note-matched to mid-flight declarations — RV-284 F-4"

[[selector]]
selector = "tests/e2e_priority_golden.rs"
intent = "design-target"
note = "orchestrator fixture-conversion (a2775657a); note-matched to mid-flight declarations — RV-284 F-4"

[[relation]]
label = "governed_by"
target = "ADR-015"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "references"
role = "originates_from"
target = "IDE-013"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-290"

[[relation]]
label = "related"
target = "RFC-020"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
