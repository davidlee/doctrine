id = 254
slug = "collapse-dispatch-onto-one-subprocess-arm"
title = "Collapse dispatch onto one subprocess arm"
status = "ready"
created = "2026-08-12"
updated = "2026-08-13"
tags = ["area:dispatch", "area:worktree", "cluster:capsule", "harness:claude"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/worktree/**"
intent = "scope-relevant"

[[selector]]
selector = "src/dispatch_config.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/dtoml.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "scripts/pi-spawn-confined.sh"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = ".claude/settings.json"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = "tests/e2e_worktree_*"
intent = "scope-relevant"

[[selector]]
selector = "install/dispatch-mechanics.md"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/pretooluse.rs"
intent = "design-target"
note = "delete outright with the in-session arm (design §5.6)"

[[selector]]
selector = "src/worktree/subagent.rs"
intent = "design-target"
note = "delete outright with the in-session arm (design §5.6)"

[[selector]]
selector = "src/mcp_server/worker_commit.rs"
intent = "design-target"
note = "delete outright with the in-session arm (design §5.6)"

[[selector]]
selector = "tests/e2e_worktree_stamp.rs"
intent = "design-target"
note = "delete outright with the in-session arm (design §5.6)"

[[selector]]
selector = "tests/e2e_worktree_verify_worker.rs"
intent = "design-target"
note = "delete outright with the in-session arm (design §5.6)"

[[selector]]
selector = "src/worktree/jail.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/jail_prefix.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/marker.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/mod.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/create.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/fork.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/land.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/inventory.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/shared.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/gc.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/worktree/import.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/boot.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/mcp_server/tools.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/mcp_server/mod.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/commands/observation.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/main.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/test_support.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "src/regression_run.rs"
intent = "design-target"
note = "modified by the collapse (design §5.6)"

[[selector]]
selector = "scripts/lib/pi-reap.sh"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = "install/doctrine.toml.example"
intent = "design-target"
note = "authored config + skill sources (the .agents/ tree is the gitignored install projection)"

[[selector]]
selector = "tests/e2e_claude_install.rs"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = "tests/e2e_worktree_create_fork.rs"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = "tests/e2e_worktree_status_marker.rs"
intent = "design-target"
note = "script/config/skill/test surface of the collapse (design §5.6)"

[[selector]]
selector = ".doctrine/doctrine.toml"
intent = "design-target"
note = "authored config + skill sources (the .agents/ tree is the gitignored install projection)"

[[selector]]
selector = "plugins/doctrine/skills/dispatch/SKILL.md"
intent = "design-target"
note = "authored config + skill sources (the .agents/ tree is the gitignored install projection)"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-agent/SKILL.md"
intent = "design-target"
note = "authored config + skill sources (the .agents/ tree is the gitignored install projection)"

[[selector]]
selector = "plugins/doctrine/skills/dispatch-subprocess/SKILL.md"
intent = "design-target"
note = "authored config + skill sources (the .agents/ tree is the gitignored install projection)"

[[selector]]
selector = "plugins/doctrine/skills/worktree/SKILL.md"
intent = "design-target"
note = "authored config + skill sources (the .agents/ tree is the gitignored install projection)"

[[selector]]
selector = "scripts/spawn-confined.sh"
intent = "design-target"
note = "the generalised spawn script the pi one is renamed to (design §5.6, DEC-209)"

[[relation]]
label = "governed_by"
target = "ADR-011"

[[relation]]
label = "governed_by"
target = "ADR-006"

[[relation]]
label = "governed_by"
target = "ADR-008"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "governed_by"
target = "ADR-012"

[[relation]]
label = "references"
role = "concerns"
descriptor = "Retires-REQ-288-and-rewrites-REQ-291"
target = "SPEC-021"

[[relation]]
label = "references"
role = "concerns"
descriptor = "Source-anchors-point-at-deleted-confinement-modules"
target = "SPEC-012"

[[relation]]
label = "references"
role = "originates_from"
target = "IDE-024"

[[relation]]
label = "references"
role = "concerns"
descriptor = "Falsified-premise-enabling-this-change"
target = "EVD-023"

[[relation]]
label = "references"
role = "concerns"
descriptor = "Brings-forward-roadmap-step-2-for-the-incumbent-arm"
target = "RFC-025"

[[relation]]
label = "references"
role = "concerns"
descriptor = "Acute-leg-inherited-from-abandoned-SL-247"
target = "IMP-401"

[[relation]]
label = "supersedes"
target = "SL-247"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
