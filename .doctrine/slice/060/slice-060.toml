id = 60
slug = "cross-kind-depseq-capture"
title = "Cross-kind dep/seq capture: extend needs/after authoring beyond backlog"
status = "audit"
created = "2026-06-14"
updated = "2026-06-14"
[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }


[[relation]]
label = "governed_by"
target = "ADR-010"

[[relation]]
label = "governed_by"
target = "ADR-004"

[[relation]]
label = "specs"
target = "PRD-009"

[[relation]]
label = "specs"
target = "SPEC-001"

[[relation]]
label = "specs"
target = "PRD-011"

# Structural relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` table (SL-048 "the cut"). Slices carry no typed relation axes.
# Author an edge with the `doctrine link` verb — it validates the (source, label)
# pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
