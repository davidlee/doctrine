id = 248
slug = "capsule-provisioning-and-linux-backend"
title = "Capsule provisioning and Linux backend"
status = "done"
created = "2026-08-05"
updated = "2026-08-10"
tags = ["area:config", "area:confinement", "area:dispatch", "area:worktree", "cluster:capsule"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/dispatch_config.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/dtoml.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/**"
intent = "scope-relevant"

[[selector]]
selector = ".doctrine/doctrine.toml"
intent = "design-target"

[[selector]]
selector = "src/worktree/jail.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/worktree/jail_prefix.rs"
intent = "scope-relevant"

[[selector]]
selector = "crates/doctrine-control/**"
intent = "design-target"

[[selector]]
selector = "src/lib.rs"
intent = "design-target"

[[selector]]
selector = "src/config_file.rs"
intent = "design-target"

[[selector]]
selector = "src/interpretation.rs"
intent = "design-target"

[[selector]]
selector = "src/git.rs"
intent = "design-target"

[[selector]]
selector = "src/main.rs"
intent = "design-target"

[[selector]]
selector = "Cargo.toml"
intent = "design-target"

[[selector]]
selector = "tests/architecture_layering.rs"
intent = "design-target"

[[selector]]
selector = ".doctrine/adr/001/layering.toml"
intent = "design-target"

[[selector]]
selector = "Cargo.lock"
intent = "design-target"

[[selector]]
selector = "justfile"
intent = "design-target"

[[selector]]
selector = "src/clock.rs"
intent = "design-target"
note = "PHASE-01 promotes today to pub (E0364); added at the /plan critical pass"

[[selector]]
selector = "flake.nix"
intent = "design-target"
note = "util-linux (setsid, row 7's session escape) and socat (row 5); RV-352 F-2 at reconcile"

[[selector]]
selector = "LOOP.md"
intent = "scope-relevant"
note = "dispatch-loop orchestrator infrastructure: touched, not delivered. RV-352 F-4 adjudication — scope-relevant is ignored by conformance (slice.rs:2899), so LOOP.md stays in the undeclared cell BY RULING, not by oversight."

[[relation]]
label = "governed_by"
target = "ADR-020"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-030"

[[relation]]
label = "references"
role = "concerns"
target = "REV-046"

[[relation]]
label = "references"
role = "originates_from"
target = "SL-241"

[[relation]]
label = "references"
role = "concerns"
descriptor = "transaction binds phase identity only; plan and phase-sheet schema face RFC-027 reshaping"
target = "RFC-027"

[[relation]]
label = "references"
role = "concerns"
descriptor = "provisioning and the export half rest on Git"
target = "ASM-009"

[[relation]]
label = "references"
role = "concerns"
descriptor = "slice 1 of the capsule decomposition — contract resolution, provisioning, Linux backend and its conformance suite"
target = "RFC-025"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
