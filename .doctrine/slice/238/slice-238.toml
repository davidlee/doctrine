id = 238
slug = "cross-kind-backlog-ordering-admission-and-override-footer"
title = "Cross-kind backlog ordering admission and override footer"
status = "design"
created = "2026-07-29"
updated = "2026-07-29"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/backlog.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/backlog_order.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/kinds.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/priority/partition.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/priority/order.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/priority/graph.rs"
intent = "scope-relevant"

[[relation]]
label = "fulfils"
target = "IMP-099"

[[relation]]
label = "fulfils"
target = "IDE-019"

[[relation]]
label = "governed_by"
target = "ADR-017"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-015"

[[relation]]
label = "references"
role = "implements"
target = "PRD-011"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
