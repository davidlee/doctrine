id = 210
slug = "comparison-ledger-capture"
title = "Comparison ledger capture"
status = "reconcile"
created = "2026-07-10"
updated = "2026-07-10"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/value/**"
intent = "scope-relevant"

[[selector]]
selector = "src/priority/**"
intent = "scope-relevant"

[[selector]]
selector = "src/kinds.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/main.rs"
intent = "design-target"

[[selector]]
selector = "src/comparison.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/compare.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/facet.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"
note = "SL-115 moved CLI enum to cli.rs; guard write_class match is exhaustive-by-design — both compelled by the compare verb"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"
note = "SL-115 moved CLI enum to cli.rs; guard write_class match is exhaustive-by-design — both compelled by the compare verb"

[[selector]]
selector = "src/commands/**"
intent = "scope-relevant"
note = "fixes command/ typo"

[[relation]]
label = "governed_by"
target = "ADR-015"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "related"
target = "RFC-019"

[[relation]]
label = "related"
target = "REV-022"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
