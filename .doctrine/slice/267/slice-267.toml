id = 267
slug = "shipped-corpus-conformance"
title = "Shipped-corpus conformance"
status = "done"
created = "2026-09-25"
updated = "2026-09-26"
tags = ["area:install", "area:memory", "area:skills", "asset-policy"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "install/**"
intent = "design-target"
note = "the three shipped sub-corpora; no src/** in scope"

[[selector]]
selector = "memory/**"
intent = "design-target"
note = "the three shipped sub-corpora; no src/** in scope"

[[selector]]
selector = "plugins/**"
intent = "design-target"
note = "the three shipped sub-corpora; no src/** in scope"

[[selector]]
selector = ".doctrine/adr/**"
intent = "design-target"

[[selector]]
selector = "publication/manifest.toml"
intent = "design-target"

[[relation]]
label = "governed_by"
target = "ADR-005"

[[relation]]
label = "governed_by"
target = "ADR-019"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-309"

[[relation]]
label = "references"
role = "originates_from"
target = "CHR-080"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-484"

[[relation]]
label = "references"
role = "concerns"
descriptor = "gates every per-site sweep decision"
target = "QUE-226"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the deferred drift-gate slice's design question"
target = "QUE-227"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
