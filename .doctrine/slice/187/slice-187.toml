id = 187
slug = "prompt-cascade-delivery"
title = "Prompt cascade: per-harness delivery & boot integration"
status = "done"
created = "2026-07-02"
updated = "2026-07-02"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = [{ to = "SL-186", rank = 0 }]                      # slice→work soft sequence: { to, rank }

[[relation]]
label = "related"
target = "SL-186"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-155"

[[selector]]
selector = "src/boot.rs"
intent = "design-target"

[[selector]]
selector = "src/mcp_server/tools.rs"
intent = "design-target"

[[selector]]
selector = "src/mcp_server/**"
intent = "scope-relevant"

[[selector]]
selector = "src/memory.rs"
intent = "design-target"

[[selector]]
selector = "src/install.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/prompt.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_mcp_server.rs"
intent = "design-target"

[[selector]]
selector = "memory/**"
intent = "design-target"

[[selector]]
selector = "tests/e2e_prompt_resolve_golden.rs"
intent = "design-target"
note = "PHASE-04 VT-2 e2e: resolve delivery axis-invariance + stdout variance"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"
note = "PHASE-04 wiring: thread render_boot_map into prompt dispatch (delivery path, EX-2)"

[[selector]]
selector = "install/model-band.md"
intent = "design-target"
note = "PHASE-01 model-band floor asset — Static embed wired in boot.rs (RV-235 F-1)"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
