id = 170
slug = "dispatch-trust-gate"
title = "Dispatch handover trust-gate"
status = "done"
created = "2026-06-28"
updated = "2026-06-28"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/plan.rs"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "src/coverage_verify.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"
note = "Undelivered by D2 (design §5.4): conclude = /dispatch skill cadence, no dispatch.rs verb; prepare_review fold rejected (git-tree reads, ADR-001). Decided, not dropped."

[[selector]]
selector = "src/commands/coverage.rs"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/skills/dispatch/**"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/skills/handover/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/e2e_*"
intent = "scope-relevant"

[[selector]]
selector = "src/regression.rs"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "src/vtgate.rs"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "src/commands/**"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "plugins/doctrine/skills/dispatch/SKILL.md"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "plugins/doctrine/skills/handover/SKILL.md"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "plugins/doctrine/skills/plan/SKILL.md"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "tests/e2e_check_regression.rs"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "tests/e2e_slice_verify_vt.rs"
intent = "design-target"
note = "SL-170 design §5.2/code-impact. coverage_verify.rs deliberately EXCLUDED (granularity mismatch, design §2/D-correction). test file names predicted; adjust at impl."

[[selector]]
selector = "tests/e2e_dispatch_verify_vt.rs"
intent = "design-target"
note = "SL-170 PHASE-04 S6 conclude/handover VT-summary e2e (design §5.4). predicted name; adjust at impl."

[[selector]]
selector = "src/state.rs"
intent = "scope-relevant"
note = "PHASE-01 incidental: test-helper plan() struct literal needed the 3 new PlanPhase fields (compile-fix, no behaviour change; Plan::parse suite untouched)"

[[selector]]
selector = "src/regression_run.rs"
intent = "design-target"
note = "S1 impure shell — module split decided at impl (PHASE-02 D1); design §5.2 named the shell, not the module name"

[[selector]]
selector = "src/verify.rs"
intent = "scope-relevant"
note = "PHASE-02 incidental: [verification].regression config key (verify.rs, D2); mod decl (main.rs); marker_present re-export (worktree/mod.rs, F-1). All additive/mechanical."

[[selector]]
selector = "src/main.rs"
intent = "scope-relevant"
note = "PHASE-02 incidental: [verification].regression config key (verify.rs, D2); mod decl (main.rs); marker_present re-export (worktree/mod.rs, F-1). All additive/mechanical."

[[selector]]
selector = "src/worktree/mod.rs"
intent = "scope-relevant"
note = "PHASE-02 incidental: [verification].regression config key (verify.rs, D2); mod decl (main.rs); marker_present re-export (worktree/mod.rs, F-1). All additive/mechanical."

[[selector]]
selector = "src/slice.rs"
intent = "scope-relevant"
note = "PHASE-03 incidental: slice verify-vt CLI wiring (SliceCommand::VerifyVt + run_verify_vt + guard Read-class); layering.toml registers vtgate=leaf. Additive/mechanical."

[[selector]]
selector = ".doctrine/adr/001/layering.toml"
intent = "scope-relevant"
note = "PHASE-03 incidental: slice verify-vt CLI wiring (SliceCommand::VerifyVt + run_verify_vt + guard Read-class); layering.toml registers vtgate=leaf. Additive/mechanical."

[[relation]]
label = "governed_by"
target = "ADR-012"

[[relation]]
label = "related"
target = "SL-169"

[[relation]]
label = "related"
target = "RFC-005"

[[relation]]
label = "related"
target = "IDE-008"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
