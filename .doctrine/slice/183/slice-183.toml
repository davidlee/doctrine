id = 183
slug = "seatbelt-jail-arm"
title = "macOS Seatbelt write-confinement arm"
status = "design"
created = "2026-06-30"
updated = "2026-06-30"
tags = []

[relationships]
needs = ["SL-182"]                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[relation]]
label = "governed_by"
target = "ADR-008"

[[relation]]
label = "governed_by"
target = "ADR-006"

[[relation]]
label = "related"
target = "RFC-012"

[[relation]]
label = "related"
target = "SL-182"

[[relation]]
label = "fulfils"
target = "IMP-045"

[[selector]]
selector = "src/**/jail.rs"
intent = "design-target"
note = "Jailer seam: seatbelt_profile + sandbox_exec_argv fork + select_jailer macOS branch (D-mac1/2/3/4)"

[[selector]]
selector = "src/**/worktree/**"
intent = "scope-relevant"

[[selector]]
selector = "src/**/worktree/pretooluse*"
intent = "design-target"
note = "os-branch selection in the pretooluse dispatch (D-mac2); precise path inherited from SL-182 once jail.rs lands"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
