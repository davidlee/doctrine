id = 43
slug = "cordage-scale-hardening"
title = "cordage scale & robustness hardening"
status = "done"
created = "2026-06-11"
updated = "2026-06-11"

[relationships]
# Parent tech spec — hardens the cordage core delivered by SL-036, still under
# SPEC-001 (Graph-Derived Priority Engine). Hand-authored per SL-036's shape; no
# `slice link` verb yet.
specs = ["SPEC-001"]
# REQ subset — OQ-1 resolved at /plan (design §6; see plan.toml [requirements]).
# The cordage generic-core perf/robustness obligations the four fixes harden to the
# ~tens-of-thousands target: REQ-078 (recompute-from-authored; H1 O(V+E)/non-recursive)
# + REQ-092 (per-overlay cycle policy) realised; REQ-076 (degrade, never a false order)
# + REQ-077 (determinism) the preserved contracts the behaviour-preservation gate proves.
requirements = ["REQ-076", "REQ-077", "REQ-078", "REQ-092"]
# Sibling SL-036 built the core and filed every risk this slice closes (RSK-001..004
# all carry `slices: SL-036`); the backlog risks themselves have no axis in this
# schema. Both linkages are tracked in prose and closed via `backlog edit` at /close:
#   sibling: SL-036    risks: RSK-001 RSK-002 RSK-003 RSK-004
