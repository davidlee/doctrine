id = 244
slug = "gate-conditions-carry-their-own-contract"
title = "Gate conditions carry their own contract"
status = "proposed"
created = "2026-08-02"
updated = "2026-08-02"
tags = ["area:correctness", "area:design", "cluster:design-run"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/design_run/**"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/design.rs"
intent = "scope-relevant"

[[selector]]
selector = ".doctrine/spec/tech/029/**"
intent = "scope-relevant"

[[selector]]
selector = "install/design-prompts/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/e2e_design_*.rs"
intent = "scope-relevant"

[[selector]]
selector = "install/hymns/**"
intent = "scope-relevant"

[[selector]]
selector = "publication/manifest.toml"
intent = "scope-relevant"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "governed_by"
target = "ADR-009"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-285"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-286"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-390"

[[relation]]
label = "references"
role = "concerns"
descriptor = "moves gate conditions off payload-claimed evidence"
target = "SPEC-029"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the open-to-closed type rule bounding where satisfaction may be sourced"
target = "DEC-101"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the seal-vs-craft line and its deferred override seam"
target = "DEC-102"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the prose/mechanism split this extends to condition contracts"
target = "DEC-077"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
