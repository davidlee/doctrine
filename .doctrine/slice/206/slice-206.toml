id = 206
slug = "workflow-slice-driver"
title = "Workflow-templated slice-driver"
status = "done"
created = "2026-07-05"
updated = "2026-07-06"
tags = ["rev-021-applied"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/**"
intent = "scope-relevant"

[[selector]]
selector = ".claude/workflows/**"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/**"
intent = "scope-relevant"

[[selector]]
selector = "src/mcp_server/dispatch.rs"
intent = "design-target"

[[selector]]
selector = "src/dispatch.rs"
intent = "design-target"

[[selector]]
selector = "src/mcp_server/tools.rs"
intent = "design-target"

[[selector]]
selector = "install/agents/claude/dispatch-orchestrator.md"
intent = "design-target"

[[selector]]
selector = "install/agents/claude/dispatch-worker.md"
intent = "scope-relevant"
note = "behaviour-preservation assertion only (doctor #9 stays green unchanged); not a modification target"

[[selector]]
selector = "src/doctor_checks.rs"
intent = "design-target"

[[selector]]
selector = "install/agents/claude/dispatch-probe.md"
intent = "design-target"

[[selector]]
selector = "tests/e2e_mcp_server.rs"
intent = "design-target"
note = "tool-count/name behaviour-lock for the MCP registry PHASE-03 grows"

[[selector]]
selector = "install/workflows/drive-slice.js"
intent = "design-target"
note = "PHASE-04 authored home for /drive-slice reference workflow (OQ-1 resolved); EX-6 selector gate"

[[selector]]
selector = "src/install.rs"
intent = "design-target"
note = "PHASE-04 modifies it (EX-7 install seeding legs); import belt requires design-target to land — upgraded from scope-relevant"

[[selector]]
selector = "src/worktree/**"
intent = "design-target"
note = "PHASE-11 modifies: nominate/denominate + gate legs (worktree module), guard/main exhaustive-match ripple, hooks.json matcher data"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"
note = "PHASE-11 modifies: nominate/denominate + gate legs (worktree module), guard/main exhaustive-match ripple, hooks.json matcher data"

[[selector]]
selector = "src/main.rs"
intent = "design-target"
note = "PHASE-11 modifies: nominate/denominate + gate legs (worktree module), guard/main exhaustive-match ripple, hooks.json matcher data"

[[selector]]
selector = "plugins/doctrine/hooks/hooks.json"
intent = "design-target"
note = "PHASE-11 modifies: nominate/denominate + gate legs (worktree module), guard/main exhaustive-match ripple, hooks.json matcher data"

[[relation]]
label = "fulfils"
target = "IDE-031"

[[relation]]
label = "governed_by"
target = "ADR-006"

[[relation]]
label = "governed_by"
target = "ADR-011"

[[relation]]
label = "governed_by"
target = "ADR-012"

[[relation]]
label = "governed_by"
target = "ADR-008"

[[relation]]
label = "references"
role = "originates_from"
target = "SL-199"

[[relation]]
label = "related"
target = "IMP-174"

[[relation]]
label = "related"
target = "ISS-216"

[[relation]]
label = "related"
target = "REV-021"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
