id = 163
slug = "check-command-proxy"
title = "check command proxy verb"
status = "started"
created = "2026-06-27"
updated = "2026-06-27"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/commands/check.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"

[[selector]]
selector = "src/verify.rs"
intent = "design-target"

[[selector]]
selector = "src/dtoml.rs"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/skills/**/SKILL.md"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/guard.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/mod.rs"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/notes/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/audit/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/close/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/execute/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/worktree/SKILL.md"
intent = "design-target"

[[selector]]
selector = "plugins/doctrine/skills/dispatch/SKILL.md"
intent = "design-target"

[[selector]]
selector = "tests/e2e_check_proxy.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_no_shipped_couplings.rs"
intent = "design-target"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-013"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-010"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
