id = 143
slug = "shipped-memory-corpus-overhaul"
title = "Shipped memory corpus overhaul: holistic onboarding & self-correction"
status = "done"
created = "2026-06-23"
updated = "2026-06-24"
tags = ["memory", "shipped-memory", "onboarding", "audit", "self-correction"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = [{ to = "SL-147", rank = 1 }]  # slice→work soft sequence: { to, rank } — self-correction gate (IMP-163) integrates with SL-147's domain-map mechanism

[[relation]]
label = "governed_by"
target = "ADR-002"

[[relation]]
label = "governed_by"
target = "ADR-005"

[[relation]]
label = "specs"
target = "SPEC-007"

[[relation]]
label = "related"
target = "SL-144"

[[relation]]
label = "related"
target = "SL-147"

[[relation]]
# label = "tracked_by"
# target = "IMP-163"

[[relation]]
label = "governed_by"
target = "POL-002"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
