id = 243
slug = "spec-anchor-map"
title = "Spec anchor map"
status = "design"
created = "2026-08-02"
updated = "2026-08-02"
tags = ["area:cli", "area:coverage", "area:governance", "area:spec", "cluster:spec-requirements"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/spec.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/**"
intent = "scope-relevant"

[[selector]]
selector = "crates/**"
intent = "scope-relevant"

[[selector]]
selector = "Cargo.toml"
intent = "scope-relevant"

[[selector]]
selector = ".doctrine/doctrine.toml"
intent = "scope-relevant"

[[selector]]
selector = "plugins/doctrine/skills/spec-coverage-assessment/**"
intent = "scope-relevant"

[[selector]]
selector = ".doctrine/spec/product/012/**"
intent = "scope-relevant"

[[selector]]
selector = ".doctrine/spec/tech/017/**"
intent = "scope-relevant"

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-295"

[[relation]]
label = "references"
role = "concerns"
descriptor = "composes with the doctor liveness leg; shares the path-existence probe and the identifier-form convention"
target = "IMP-316"

[[relation]]
label = "references"
role = "implements"
target = "PRD-012"

[[relation]]
label = "references"
role = "concerns"
descriptor = "consumes the [[source]] anchor field model; may host the identifier-form convention"
target = "SPEC-017"

[[relation]]
label = "references"
role = "concerns"
descriptor = "the manual census whose baseline figures this slice must reproduce mechanically"
target = "IMP-381"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
