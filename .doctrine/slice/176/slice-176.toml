id = 176
slug = "finish-axis-b"
title = "Finish Axis B — slices retirement + fulfils priority burndown"
status = "plan"
created = "2026-06-28"
updated = "2026-06-29"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/relation.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/relation_graph.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/commands/relation.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "install/templates/*.toml"
intent = "scope-relevant"

[[selector]]
selector = "src/relation_query.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/catalog/hydrate.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/catalog/graph.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/backlog.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/lazyspec.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "install/templates/slice.toml"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "install/templates/backlog.toml"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "tests/e2e_relation_*.rs"
intent = "design-target"
note = "SL-176 Finish Axis B: fulfils label + degree facet, originates_from role, slices/drift-entity migration (design §A/B/C)"

[[selector]]
selector = "src/priority/graph.rs"
intent = "design-target"
note = "SL-176 codex F4/F5: slices consumer (priority scoring) + scoped_from CLI surface"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"
note = "SL-176 codex F4/F5: slices consumer (priority scoring) + scoped_from CLI surface"

[[relation]]
label = "governed_by"
target = "ADR-004"

[[relation]]
label = "governed_by"
target = "ADR-010"

[[relation]]
label = "governed_by"
target = "ADR-016"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-003"

[[relation]]
label = "references"
role = "concerns"
target = "IMP-207"

[[relation]]
label = "references"
role = "concerns"
target = "IMP-149"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
