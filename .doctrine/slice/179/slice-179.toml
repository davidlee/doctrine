id = 179
slug = "close-failed-cell-gate"
title = "Closure gate hardens on live Failed coverage cell; close the forget evidence-erasure leak"
status = "reconcile"
created = "2026-06-30"
updated = "2026-06-30"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[relation]]
label = "references"
role = "originates_from"
target = "RSK-008"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-002"

[[relation]]
label = "governed_by"
target = "ADR-009"

[[selector]]
selector = "src/slice.rs"
intent = "design-target"

[[selector]]
selector = "src/coverage.rs"
intent = "design-target"

[[selector]]
selector = "src/coverage_store.rs"
intent = "design-target"

[[selector]]
selector = "src/coverage_verify.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/reconcile.rs"
intent = "design-target"

[[selector]]
selector = "src/coverage_view.rs"
intent = "design-target"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.

# Closure-gate declared set (PHASE-01, REV-017): the governed requirement this slice
# amends is added to its own gate set so SL-179's close is answerable for it — the
# PHASE-03 dogfood (VA-1) is non-vacuous (codex M8).
[gate]
extra_reqs = ["REQ-113"]
