id = 266
slug = "inquiry-map-tree-view"
title = "Inquiry map tree view"
status = "ready"
created = "2026-09-25"
updated = "2026-09-25"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/design_run/render/**"
intent = "design-target"

[[selector]]
selector = "src/commands/design.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/inquiry.rs"
intent = "design-target"

[[selector]]
selector = "install/design-prompts/inquiry.md"
intent = "design-target"

[[selector]]
selector = "install/design-prompts/conditions/initial-concerns-recorded.md"
intent = "design-target"

[[selector]]
selector = "src/design_run/config.rs"
intent = "design-target"

[[selector]]
selector = "src/dtoml.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/mod.rs"
intent = "design-target"

[[selector]]
selector = "src/design_run/run.rs"
intent = "design-target"

[[selector]]
selector = "src/state.rs"
intent = "design-target"

[[selector]]
selector = "src/commands/cli.rs"
intent = "design-target"

[[relation]]
label = "fulfils"
target = "ISS-299"

[[relation]]
label = "references"
role = "concerns"
descriptor = "inquiry map visibility commitment"
target = "PRD-019"

[[relation]]
label = "references"
role = "concerns"
descriptor = "render layer and design show read surface"
target = "SPEC-029"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
