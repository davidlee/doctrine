id = 259
slug = "truthful-apply"
title = "Truthful apply"
status = "done"
created = "2026-09-11"
updated = "2026-09-15"
tags = ["area:correctness", "area:design-run", "cluster:design-run", "footgun"]

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/design_run/**"
intent = "scope-relevant"

[[selector]]
selector = "src/commands/design.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "tests/design_act/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/design_fixture/**"
intent = "scope-relevant"

[[selector]]
selector = "tests/e2e_design_*.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/design_run/payload_contract.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "src/design_run/submission.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "src/design_run/change_log.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "src/design_run/snapshot.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "src/design_run/run.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "src/design_run/render/envelope.rs"
intent = "design-target"
note = "SL-259 sec-7 code impact: the seven design targets the four legs commit to"

[[selector]]
selector = "src/design_run/contract_check.rs"
intent = "design-target"
note = "SL-259 PHASE-03: leg 3's key axis landed as a leaf sibling (contract_check) rather than inside payload_contract as sec-7 assumed, with Refusal::UnknownPayloadKey and the module declaration alongside"

[[selector]]
selector = "src/design_run/refusal.rs"
intent = "design-target"
note = "SL-259 PHASE-03: leg 3's key axis landed as a leaf sibling (contract_check) rather than inside payload_contract as sec-7 assumed, with Refusal::UnknownPayloadKey and the module declaration alongside"

[[selector]]
selector = "src/design_run/mod.rs"
intent = "design-target"
note = "SL-259 PHASE-03: leg 3's key axis landed as a leaf sibling (contract_check) rather than inside payload_contract as sec-7 assumed, with Refusal::UnknownPayloadKey and the module declaration alongside"

[[selector]]
selector = "install/design-payload-contract.md"
intent = "design-target"
note = "SL-259 PHASE-03 EX-3: the published contract is a deliverable of the flip — it is what tells every installed client whether a misspelt key is refused"

[[selector]]
selector = "src/design_run/fixture.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[selector]]
selector = "src/design_run/gate.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[selector]]
selector = "src/design_run/ids.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[selector]]
selector = "src/design_run/render/change_row.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[selector]]
selector = "src/design_run/tests.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[selector]]
selector = "tests/e2e_design_review.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[selector]]
selector = "tests/e2e_design_state.rs"
intent = "design-target"
note = "SL-259 RV-366 F-4: deliberate change that sec-7 as locked did not name; declared at reconcile so conformance reads the work rather than drift"

[[relation]]
label = "governed_by"
target = "STD-003"

[[relation]]
label = "governed_by"
target = "STD-001"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-029"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-315"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-333"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-328"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-327"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-290"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-367"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-361"

[[relation]]
label = "references"
role = "originates_from"
target = "ISS-450"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
