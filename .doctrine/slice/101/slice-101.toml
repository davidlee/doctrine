id = 101
slug = "spec-020-estimate-core"
title = "Estimate & Value facets — model, parse, validate, unit"
status = "done"
created = "2026-06-18"
updated = "2026-06-18"

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[relation]]
label = "specs"
target = "SPEC-020"

[[relation]]
label = "requirements"
target = "REQ-269"

[[relation]]
label = "requirements"
target = "REQ-270"

[[relation]]
label = "requirements"
target = "REQ-271"

[[relation]]
label = "requirements"
target = "REQ-272"

[[relation]]
label = "requirements"
target = "REQ-275"

[[relation]]
label = "requirements"
target = "REQ-276"

[[relation]]
label = "requirements"
target = "REQ-277"

[[relation]]
label = "governed_by"
target = "ADR-001"

[[relation]]
label = "governed_by"
target = "ADR-004"

[[relation]]
label = "requirements"
target = "REQ-278"

[[relation]]
label = "requirements"
target = "REQ-279"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
