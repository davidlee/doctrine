id = 156
slug = "cargo-binary-isolation"
title = "Per-worktree CARGO_TARGET_DIR for dispatch workers"
status = "reconcile"
created = "2026-06-26"
updated = "2026-06-26"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/worktree/fork.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/create.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/worktree/shared.rs"
intent = "scope-relevant"

[[selector]]
selector = "src/worktree/marker.rs"
intent = "scope-relevant"

[[selector]]
selector = ".agents/skills/dispatch-agent/SKILL.md"
intent = "scope-relevant"

[[selector]]
selector = ".agents/skills/dispatch-subprocess/SKILL.md"
intent = "design-target"

[[selector]]
selector = "src/worktree/coordinate.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/gc.rs"
intent = "design-target"

[[selector]]
selector = "flake.nix"
intent = "design-target"

[[selector]]
selector = "justfile"
intent = "design-target"

[[selector]]
selector = "AGENTS.md"
intent = "design-target"

[[selector]]
selector = "tests/e2e_worktree_coordinate.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_worktree_fork.rs"
intent = "design-target"

[[selector]]
selector = "tests/e2e_worktree_gc.rs"
intent = "design-target"

[[selector]]
selector = "src/worktree/mod.rs"
intent = "design-target"
note = "EAP-4: stale stdout env-contract references (CLI help / stdout-discipline comment / generic worktree skill)"

[[selector]]
selector = "src/worktree/provision.rs"
intent = "design-target"
note = "EAP-4: stale stdout env-contract references (CLI help / stdout-discipline comment / generic worktree skill)"

[[selector]]
selector = ".agents/skills/worktree/SKILL.md"
intent = "design-target"
note = "EAP-4: stale stdout env-contract references (CLI help / stdout-discipline comment / generic worktree skill)"

[[relation]]
label = "governed_by"
target = "ADR-008"

[[relation]]
label = "related"
target = "IMP-004"

[[relation]]
label = "related"
target = "ISS-011"

[[relation]]
label = "related"
target = "REV-011"

[[relation]]
label = "references"
role = "concerns"
target = "SL-152"

[[relation]]
label = "references"
role = "concerns"
target = "POL-002"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
