id = 158
slug = "trinary-actionability"
title = "Trinary actionability"
status = "done"
created = "2026-06-26"
updated = "2026-06-26"
tags = []

[relationships]
needs = []                      # slice→work hard prerequisite
after = []                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "src/priority/**"
intent = "scope-relevant"

[[selector]]
selector = "src/relation.rs"
intent = "design-target"
note = "D6: records author references (concerns)"

[[selector]]
selector = "src/priority/partition.rs"
intent = "design-target"
note = "SL-158 ADR-017 impl: trinary partition + needs-target gate widening + canon"

[[selector]]
selector = "src/commands/dep_seq.rs"
intent = "design-target"
note = "SL-158 ADR-017 impl: trinary partition + needs-target gate widening + canon"

[[selector]]
selector = ".doctrine/spec/tech/001/**"
intent = "design-target"
note = "SL-158 ADR-017 impl: trinary partition + needs-target gate widening + canon"

[[selector]]
selector = ".doctrine/spec/tech/019/**"
intent = "design-target"
note = "SL-158 ADR-017 impl: trinary partition + needs-target gate widening + canon"

[[relation]]
label = "related"
target = "RFC-007"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-001"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-019"

[[relation]]
label = "references"
role = "concerns"
target = "ADR-017"

[[relation]]
label = "fulfils"
target = "IMP-047"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
