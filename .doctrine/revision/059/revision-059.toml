id      = 59
slug    = "extend-spec-011-to-the-codex-hook-registry-and-generated-pi-extensions"
title   = "Extend SPEC-011 to the codex hook registry and generated pi extensions"
status = "done"
approval = "approved"
updated = "2026-09-24"
tags    = []

# A Revision (REV, ADR-013) is a first-class change-axis entity: a pending
# revise-intent against authored governance/spec truth plus a staged delta payload.
# Lifecycle (proposed→started→done; abandoned from any non-terminal) is moved by
# `revision status REV-N <state>`. `approval` is orthogonal — lifecycle transitions
# are approval-blind; the apply checkpoint (PHASE-05) consults it. The prose
# rationale + before/after excerpts live in the sister `revision-NNN.md`.

# change rows — the typed `revises` payload (the touched-entity set; the rows ARE
# the edges, members.toml precedent). Authored by `revision change add` (PHASE-03),
# NOT by hand and NEVER by `doctrine link`. A fresh `revision new` writes none:
#   [[change]]
#   target  = "ADR-006"   # durable peer id (the FK / anchor)
#   action  = "modify"    # modify | retire | move | status | introduce | create
#   primary = true        # display/headline hint; at most one, optional

[relationships]
needs = []
after = []

[[change]]
target = "SPEC-011"
action = "modify"
primary = true

[[change]]
action = "introduce"
primary = false
new_label = "FR-010"
member_of = "SPEC-011"
new_statement = "`boot install` idempotently merges doctrine's owned codex hook registry — a `SessionStart` emit hook and a `PreToolUse` memory-surface hook across codex's `Bash` and `apply_patch` matchers — into `.codex/hooks.json`, preserving foreign entries and refreshing an owned entry when its matcher, command, or handler fields differ; only the memory-surface handlers carry `additionalContextLimit` and `timeout`, both beside `command` rather than on the matcher group."

[[change]]
action = "introduce"
primary = false
new_label = "FR-011"
member_of = "SPEC-011"
new_statement = "`boot install` plans and installs the generated pi extensions — `index.ts`, `mcp.ts`, and `surface.ts` — through one descriptor core (four-way action, foreign-skip, regenerate-on-change) and reports each as its own `RefreshReport` leg; `surface.ts` is the neutral-wire adapter that maps pi's `read`/`edit`/`write` and `bash` tool results onto doctrine's neutral envelope, writes it to `memory surface --input neutral` on an async, bounded, fail-open child, and appends the returned block."
