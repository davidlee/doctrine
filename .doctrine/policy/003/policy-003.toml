schema = "doctrine.policy"
version = 1

id = 3
slug = "harness-independence-from-host-harness-seams"
title = "Harness independence from host-harness seams"
status = "required"
created = "2026-09-24"
updated = "2026-09-24"
tags = ["governance", "harness", "platform-independence"]

[relationships]
# `supersedes` stays typed — author with `doctrine supersede POL-NNN <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link POL-NNN related <target>`.
supersedes    = []
superseded_by = []   # reverse link is derived
