schema = "doctrine.policy"
version = 1

id = 1
slug = "engineering-action-figure-cosplay-clankspeak-prohibited"
title = "Engineering action figure cosplay clankspeak prohibited"
status = "required"
created = "2026-06-10"
updated = "2026-06-10"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link POL-001 supersedes|related <target>`.
# `doctrine inspect POL-001` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # reverse link is derived
tags          = []   # free-form classification

