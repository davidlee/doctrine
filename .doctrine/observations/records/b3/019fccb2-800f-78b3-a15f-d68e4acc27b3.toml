schema = "doctrine.observation"
schema_version = 1
uid = "019fccb2-800f-78b3-a15f-d68e4acc27b3"
recorded_at = "2026-08-04T12:14:38.095942718Z"
kind = "friction"
summary = "phase-plan: verify-vt UNATTRIBUTABLE message asserts keyword presence it never checked"
detail = "SL-244 PHASE-04 VT-3 reports 'keyword present but src/review.rs not modified by this slice'. Both declared keywords (undisposed_blockers, answered_blocker_is_not_undisposed) are absent from src/review.rs — grep count 0 each, positive control doc_unresolved_blockers = 3. The attribution gate short-circuits before the keyword check but the rendered message claims the keyword IS present, which reads as 'only attribution is owed' when in fact the whole test is owed. Same shape on PHASE-05 VT-8/VT-10. Cost: a planner who trusts the message under-scopes the task."

[facets.execution]
schema_version = 1
interface = "cli"
interface_origin = "automatic"
product_surface = "doctrine"
product_surface_origin = "automatic"
command = "observation record"
command_origin = "automatic"
repository_context = "primary"
repository_context_origin = "automatic"
