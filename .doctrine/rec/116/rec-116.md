# REC REC-116 — accept

Reconciliation record (SPEC-002). The structured deltas + evidence live in the
sister `rec-NNN.toml`; this prose companion carries the rationale for the act.

## Rationale

<!-- Why this reconciliation: what coverage/drift was observed, why this `move`
     (accept | revise | redesign), and what the deltas record. For a `redesign`,
     the escalation and its grounds — that REC carries no status_deltas (F7).
     Seeded at `rec new`. -->

`SL-256` closes having moved `REQ-478` (SPEC-029 `FR-009`, *report every recorded
mutation on the change log*) from `pending` to `active` — the one governance item
in `RV-364`'s reconciliation brief, applied through `REV-055` and recorded as
`REC-115`. That write is what left the requirement's authored status ahead of its
last-scanned coverage composite, which is the residual drift the close gate
refuses on.

This act discharges it as an **accept**: the authored status is correct and the
evidence supports it, so nothing is being revised or escalated.

- **`REQ-478` `active → active`.** A same-value delta. The status is not moving
  here; `REV-055` already moved it. What this records is the affirmation, at the
  status the corpus actually holds.
- **Evidence: the single `VT` cell `(SL-256, REQ-478, SL-256, VT)`.** `SL-256`
  introduced the requirement and is its only contributing change, so the composite
  has one input — confirmed by dereferencing every
  `.doctrine/slice/*/coverage.toml` hit for `REQ-478` to its real path, which
  collapses to one file. The cell is `verified` against a runnable positive
  control: `cargo test --test e2e_design_state` matched on one new test's own pass
  line, so it fails if that test was never compiled rather than passing on a
  pass-count that a stripped build would also satisfy.

No other requirement was flagged.
