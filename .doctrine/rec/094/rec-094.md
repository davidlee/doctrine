# REC REC-094 — accept

Reconciliation record (SPEC-002). The structured deltas + evidence live in the
sister `rec-NNN.toml`; this prose companion carries the rationale for the act.

## Rationale

REQ-317's coverage composite was `Indeterminate` (VA only). SL-165 makes the
substrate satisfy REQ-317 (the repair-to-integrate path it mandates), so `accept`
affirms it at its current `active` status. Same-value `active→active` delta
(status never moved); evidence_refs cover REQ-317's full composite (SL-064 origin
+ SL-165 conformance). Drives the SL-165 close.
