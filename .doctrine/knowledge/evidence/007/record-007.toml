schema = "doctrine.knowledge"
version = 1

id = 7
slug = "every-scored-refusal-token-is-computed-trusted-side-downstream-of-the-ingestion-mechanism"
title = "Every scored refusal token is computed trusted-side, downstream of the ingestion mechanism"
record_kind = "evidence"
status = "captured"
created = "2026-08-03"
updated = "2026-08-03"
tags = []

[facet]                         # every field typed; seeded empty (captured later)
datum        = "Across 16 hazard rows x 2 mechanisms, every scored refusal token is produced by the mechanism-independent downstream pipeline and computed trusted-side from object identity. The only mechanism-specific token, M-B's harvest/fsck-failed, is emitted at two sites and cannot distinguish bad ingested objects from a stale derived cache."
provenance   = "experiment"     # inspection | experiment | reproduction | citation
confidence   = "high"       # low | medium | high

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)

[[relation]]
label = "supports"
target = "QUE-200"
