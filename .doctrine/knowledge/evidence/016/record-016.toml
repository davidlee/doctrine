schema = "doctrine.knowledge"
version = 1

id = 16
slug = "host-initiated-git-works-in-both-directions-over-the-capsule-ssh-channel"
title = "Host-initiated git works in both directions over the capsule ssh channel"
record_kind = "evidence"
status = "captured"
created = "2026-08-11"
updated = "2026-08-11"
tags = ["capsule", "cluster:capsule", "ingestion", "microvm"]

[facet]                         # every field typed; seeded empty (captured later)
datum = "Host-initiated fetch (66,436 objects / 32.03 MiB @ 96 MiB/s) and host-initiated push with receive.denyCurrentBranch=updateInstead (66,537 objects / 32.05 MiB, unborn HEAD accepted) both succeed over the existing capsule ssh channel"
provenance = "experiment"
confidence = "medium"

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)

[[relation]]
label = "references"
role = "concerns"
descriptor = "measured evidence for the transport inversion"
target = "DEC-192"

[[relation]]
label = "references"
role = "concerns"
descriptor = "capsule round P0 measurement"
target = "RFC-025"
