Verification runs per delivery channel (ADR-019): install/ is published, memory/ is embedded+retrieved, plugins/ is installed. For each edited asset the evidence is the replacement resolving, read as an agent with no doctrine corpus: a published reference/<name>.md address the reader can reach, or prose that stands without an id. A grep is used to locate candidate sites and to catch re-drift, never to prove the sweep done - the old id being absent proves the deletion ran and nothing more. No existing gate scans the three corpora, so the slice records its own per-axis evidence rather than leaning on doctor/check.