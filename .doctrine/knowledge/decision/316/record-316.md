The accuracy pass pairs every behavioural claim in the shipped corpus with the artifact that decides it - doctrine <verb> --help for command shapes, live list output plus install/templates/*.toml for vocabularies and defaults - and corrects the mismatches. It covers the whole corpus, not only CHR-080 confirmed instances. Two mechanical legs run first because they are cheap: dangling [[mem.*]] wikilinks, and any id whose prefix the CLI does not mint.