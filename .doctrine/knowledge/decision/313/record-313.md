Two sites carry reasoning with no shipped home. (1) install/design-prompts/inquiring.toml cites sketches/thin-adapter.md (only home .doctrine/slice/233/sketches/) for two load-bearing runbook steps; the design-run engine is itself shipped, so this rationale is published as a reference doc under reference/ and the runbook points at that published address. (2) install/doctrine.toml.example per-knob ids name why each knob exists; the why is inlined as a comment on the knob, since a config comment rationale belongs on the thing it explains.