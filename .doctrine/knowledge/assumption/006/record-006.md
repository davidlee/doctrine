# ASM-006: Existing knowledge kinds fit design checkpoints

<!-- Knowledge record body — context, detail, links. The structured, queried
     fields live in the sister `record-NNN.toml`; this prose is free-form and is
     never structurally parsed (the storage rule). -->

SL-233 proceeds as if existing DEC, QUE, and ASM records are the correct durable
sinks for accepted design choices, retained questions, and carried assumptions.
Design must surface any semantic mismatch before introducing a new record kind
or parallel truth store.
