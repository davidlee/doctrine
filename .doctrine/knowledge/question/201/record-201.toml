schema = "doctrine.knowledge"
version = 1

id = 201
slug = "interpretation-surface-declaration-home"
title = "Interpretation-surface declaration home"
record_kind = "question"
status = "open"
created = "2026-08-01"
updated = "2026-08-01"
tags = []

[facet]                         # every field typed; seeded empty (captured later)
question    = "In shipped doctrine, where does a client project's interpretation-surface declaration live — a doctrine.toml block, a dedicated default-deny manifest, or a field on the work contract?"
why_matters = "Ergonomics only, since DEC-099 Amendment 2 (RV-340 F-5) rules the declaration is read from the contracted base B. Before that ruling two of the three candidates lived inside the tree a capsule clones, so the choice of home would have silently decided a security property."
answer      = ""
answered_by = ""
answered_on = ""

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = [
  "EVD-011 — a capsule rewriting its own in-repo declaration leaves trusted-side behaviour byte-identical",
]
contradicts = []
notes       = [
  "EVD-011 is this question's only probe-evidence input (SL-241 PHASE-05 guard probe (e), three legs). Its third leg's refusal token is fixture-specific — F-P05-43 — and must not be generalised.",
]

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)

[[relation]]
label = "references"
role = "concerns"
descriptor = "RFC-025 capsule spike rig design"
target = "SL-241"
