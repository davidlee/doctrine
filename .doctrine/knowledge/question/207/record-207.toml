schema = "doctrine.knowledge"
version = 1

id = 207
slug = "binary-and-crate-topology-for-the-control-plane"
title = "Binary and crate topology for the control plane"
record_kind = "question"
status = "answered"
created = "2026-08-05"
updated = "2026-08-05"
tags = ["area:architecture", "cluster:capsule"]

[facet]                         # every field typed; seeded empty (captured later)
question    = "What is the binary and crate story for RFC-025's capsule programme, across the trusted control plane, the existing CLI, provisioning, and any long-running process — and where does the authority boundary get expressed: in the environment, the crate graph, binary absence, or a protocol?"
why_matters = "It is upstream of nearly every module decision in the capsule implementation, and one of the three framings (privilege from environment) reproduces the inferred-from-process-identity pattern the capsule programme exists to retire."
answer      = "B — two binaries over shared code, split at canonical mutation. See DEC-153."
answered_by = "DEC-153"
answered_on = "2026-08-06"

[evidence]                      # free-text citations (supports / contradicts / notes)
supports    = []
contradicts = []
notes       = []

[relationships]                 # verb-written edges (set by doctrine supersede)
supersedes    = []   # record ids this one replaces (set by doctrine supersede)
superseded_by = []   # set on superseded predecessor (verb-written, ADR-004 §5)

[[relation]]
label = "shapes"
target = "RFC-025"
