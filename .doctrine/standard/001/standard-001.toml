schema = "doctrine.standard"
version = 1

id = 1
slug = "no-magic-strings-single-source-named-constants"
title = "No magic strings — single-source named constants"
status = "required"
created = "2026-06-27"
updated = "2026-06-27"
tags = ["config", "magic-strings", "tech-debt"]

[relationships]
# `supersedes` stays typed — author with `doctrine supersede STD-001 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link STD-001 related <target>`.
supersedes    = []
superseded_by = []   # reverse link is derived

