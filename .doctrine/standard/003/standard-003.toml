schema = "doctrine.standard"
version = 1

id = 3
slug = "no-silent-skip-a-degraded-read-is-disclosed"
title = "No silent skip — a degraded read is disclosed"
status = "required"
created = "2026-08-15"
updated = "2026-08-15"
tags = ["corpus-integrity", "error-handling", "tech-debt"]

[relationships]
# `supersedes` stays typed — author with `doctrine supersede STD-NNN <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link STD-NNN related <target>`.
supersedes    = []
superseded_by = []   # reverse link is derived
