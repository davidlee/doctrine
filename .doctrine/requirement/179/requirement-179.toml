schema = "doctrine.requirement"
version = 1

id = 179
slug = "lay-down-the-derived-canonical-tree-and-links-atomically-via-staged-swaps-so-a-crash-leaves-only-a-healable-dangling-link"
title = "Lay down the derived canonical tree and links atomically via staged swaps so a crash leaves only a healable dangling link"
status = "active"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
