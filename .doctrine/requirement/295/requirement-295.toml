schema = "doctrine.requirement"
version = 1

id = 295
slug = "durable-memory-that-must-outlive-a-fork-is-recorded-on-the-coordination-branchtrunk-never-inside-a"
title = "Durable memory that must outlive a fork is recorded on the coordination branch/trunk, never inside a worker fork branch, because a squash/sever orphans the git anchor."
status = "active"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
