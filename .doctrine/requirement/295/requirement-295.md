# REQ-295: Durable memory that must outlive a fork is recorded on the coordination branch/trunk, never inside a worker fork branch, because a squash/sever orphans the git anchor.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
