schema = "doctrine.requirement"
version = 1

id = 167
slug = "build-an-inspectable-plan-of-create-dir-install-skip-and-gitignore-steps-before-executing"
title = "Build an inspectable plan of create-dir, install, skip, and gitignore steps before executing"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
