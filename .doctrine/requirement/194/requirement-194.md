# REQ-194: All git, disk, and process impurity is confined to the `git.rs`/provision shell; the provision and copy-selection core is pure (paths/strings in, no clock/rng/disk).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
