schema = "doctrine.requirement"
version = 1

id = 128
slug = "mutable-runtime-state-is-held-physically-apart-from-the-authored-entity-taxonomy"
title = "Mutable runtime state is held physically apart from the authored entity taxonomy"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
