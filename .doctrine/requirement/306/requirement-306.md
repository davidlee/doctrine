# REQ-306: Relations are partitioned into three storage tiers — tier-1 uniform `[[relation]]`, tier-2 constrained-or-payload kept typed, tier-3 free-text — preserving typed guarantees rather than flattening (ADR-010).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
