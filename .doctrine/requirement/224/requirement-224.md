# REQ-224: Hide deprecated and retired rows from each kind's default list, revealed by --all or an explicit --status, via the GovKind hidden predicate.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
