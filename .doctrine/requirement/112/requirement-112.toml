schema = "doctrine.requirement"
version = 1

id = 112
slug = "route-reconciliation-through-a-sole-writer-applying-acceptreviseredesign"
title = "Route reconciliation through a sole writer applying accept/revise/redesign"
status = "active"
kind = "functional"

description = "Reconciliation is routed through a single writer that applies accept/revise/redesign and emits exactly one REC per act."
tags = []
acceptance_criteria = [
  "The reconcile writer is the only code that writes reconciled authored requirement status and spec truth in the loop.",
  "Per divergence it applies one move: accept (requirement-status write), revise (spec-truth write), or redesign (reconcile->design escalation, no instance write).",
  "Each reconciliation act emits exactly one REC.",
]
