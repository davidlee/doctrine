# REQ-293: Funnel guards read git through failure-tolerant idioms under output-rewriting proxies (rtk): existence/disjointness/landed decisions read printed output (ls-tree, --name-only, git cherry) or rtk proxy raw plumbing, never a chained exit code, and the combined-tree project verify is the real gate, not import's own fidelity check.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
