# REQ-297: An isolated worker returns a single non-merge source delta and never writes doctrine-authored state, runtime state, or memory; a multi-commit, merge, authored-tree-touching, or misbased delta is refused at the funnel.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
