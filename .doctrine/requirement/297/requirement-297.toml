schema = "doctrine.requirement"
version = 1

id = 297
slug = "an-isolated-worker-returns-a-single-non-merge-source-delta-and-never-writes-doctrine-authored-state"
title = "An isolated worker returns a single non-merge source delta and never writes doctrine-authored state, runtime state, or memory; a multi-commit, merge, authored-tree-touching, or misbased delta is refused at the funnel."
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
