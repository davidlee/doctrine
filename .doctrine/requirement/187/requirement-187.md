# REQ-187: `boot --check` recomputes the snapshot, reports `stale` when on-disk bytes differ (unreadable counts as stale), and lists sections still rendering a marker.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
