# REQ-458: The durable admission journal records identities, policy, verdicts, transitions, and exhibit lifecycle; unresolved source capsules remain frozen live work while post-close forensic exhibits may follow separately-owned retention policy.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
