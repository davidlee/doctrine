# REQ-181: Assemble the snapshot as a pure projection (boot_sequence + render_boot) with no clock/rng/disk, yielding a deterministic byte-identical string for identical governance state.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
