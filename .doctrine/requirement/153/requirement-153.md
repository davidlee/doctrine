# REQ-153: Partition memory by workspace and repo; a memory whose repo is non-empty and differs from the querying repo is hard-filtered, the empty-repo global class admitted in every partition

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
