schema = "doctrine.requirement"
version = 1

id = 141
slug = "the-spine-and-edge-tables-are-authored-toml-the-engine-round-trips-but-never-generates-no-cli-flag-sets-a-spine-field"
title = "The spine and edge tables are authored TOML the engine round-trips but never generates; no CLI flag sets a spine field"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
