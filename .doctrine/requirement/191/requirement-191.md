# REQ-191: `worktree branch-point-check --base B` exits 0 when coordination HEAD equals the pre-spawn base and 1 otherwise (→ re-dispatch); it is ref-equality, not a merge-base compute.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
