# REQ-204: Per-verb black-box goldens plus a cross-verb column-model net pin the rendered table and JSON envelope byte-exact, turning red on any surface drift.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
