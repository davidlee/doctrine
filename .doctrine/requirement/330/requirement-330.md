# REQ-330: Front the engine with prompt resolve (stdout composition; repeatable --model; the on-disk universal boot artifact is axis-invariant), model-keys (corpus-reflected taxonomy keys only), explain (precedence trace), and check (corpus integrity, feeding doctrine check).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
