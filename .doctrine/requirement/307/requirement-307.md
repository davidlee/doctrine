# REQ-307: The legal relation vocabulary has one code-authoritative source of truth — the `RELATION_RULES` table (`src/relation.rs`) keyed by `(source ∈ sources, label)`, sole driver of writer, validator, the SL-046 reader, and cordage overlay allocation — pointed at, never transcribed.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
