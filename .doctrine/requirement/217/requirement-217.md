# REQ-217: Hold the outbound [relationships] seam (slices/specs/drift, plus needs/after/triggers priority edges), appending edit-preservingly and refusing when a seeded array is absent.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
