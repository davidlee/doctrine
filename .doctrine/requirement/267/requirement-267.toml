schema = "doctrine.requirement"
version = 1

id = 267
slug = "the-estimate-facet-is-reusable-and-kind-agnostic"
title = "The estimate facet is reusable and kind-agnostic"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
