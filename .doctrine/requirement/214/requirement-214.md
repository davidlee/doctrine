# REQ-214: Fix the closed status vocabulary open, triaged, started, resolved, closed as both the transition set and the `backlog list` --status known-set, with resolved/closed terminal.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
