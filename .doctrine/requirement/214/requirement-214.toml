schema = "doctrine.requirement"
version = 1

id = 214
slug = "fix-the-closed-status-vocabulary-open-triaged-started-resolved-closed-as-both-the-transition-set-and-the-backlog-list-status-known-set-with-resolvedclosed-terminal"
title = "Fix the closed status vocabulary open, triaged, started, resolved, closed as both the transition set and the `backlog list` --status known-set, with resolved/closed terminal."
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
