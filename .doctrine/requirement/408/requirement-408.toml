schema = "doctrine.requirement"
version = 1

id = 408
slug = "query-observations-by-identity-facets-time-and-text"
title = "Query observations by identity facets time and text"
status = "pending"
kind = "functional"
description = "The query engine supports exact UUID lookup and stable filtering, lexical search, ordering, and pagination over active or historical projections."

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = ["observation", "query", "search", "pagination"]
acceptance_criteria = [
  "Bare UUID is the canonical observation identity; exact lookup returns raw identity or follows effective supersession transitively to its terminus, rendering a retracted terminus as retracted with its chain.",
  "Filters cover kind, time, and registered facet fields; lexical search reuses the shared tokenizer and requires every query token across summary, detail, and string facet values.",
  "Results order by recorded time descending and UUID with opaque keyset pagination that does not duplicate traversed rows after head insertion, without aggregation, relevance ranking, or a frozen-snapshot claim.",
]
