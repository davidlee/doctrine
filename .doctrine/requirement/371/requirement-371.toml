schema = "doctrine.requirement"
version = 1

id = 371
slug = "licence-classification-is-predictable-and-auditable"
title = "Licence is visible and auditable before copying"
status = "pending"
kind = "quality"

description = "A user can determine which licence governs a published asset before copying it by reading the asset's declared licence — not by guessing from its appearance or category."
tags = []
acceptance_criteria = [
  "Each published asset's declared licence is visible before the asset is copied.",
  "The set of possible licences is fixed and documented, so any declared licence is interpretable.",
]
