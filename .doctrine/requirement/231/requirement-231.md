# REQ-231: Carry `descends_from` (tech→PRD) and `parent` (→SPEC) as single-valued outbound scalars; the reciprocal children/realised-by views are derived, never stored (ADR-004).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
