# REQ-195: The coordination/runtime tier (`.doctrine/state/`, `phases` symlink, `handover.md`, memory caches) is absent from a fork by construction — no shared-mutable authored file crosses the isolation unit.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
