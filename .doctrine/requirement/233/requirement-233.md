# REQ-233: Render the spine on `spec show` kind-gated and Some-gated, keeping product and unfilled-tech output byte-stable; never render derived children.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
