# REQ-287: The per-batch funnel cadence is a fixed ordered sequence (capture B → precond HEAD==B & clean → delta S^==B single-non-merge → R-5 .doctrine/.claude belt → import non-committing → verify → branch-point guard → one coordination commit → record), report-and-halt on any breach, with knowledge recorded only after the confirmed code commit.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
