schema = "doctrine.requirement"
version = 1

id = 63
slug = "survey-and-filter-records-by-kind-status-and-tag"
title = "Survey and filter records by kind, status, and tag"
status = "pending"
kind = "functional"

description = "An operator surveys the records as a set and narrows them by kind, status, and tag, seeing at a glance what is open or held versus settled."
tags = []
acceptance_criteria = [
  "A survey lists records and filters by kind, status, and tag.",
  "Terminal-state records (e.g. obsolete, superseded, retired, rejected) are hidden by default yet remain addressable under an explicit reveal.",
]
