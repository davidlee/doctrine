# REQ-331: Source trait classification from the def's declared trait-key set (spawner may override at spawn); no central model-to-traits registry exists, and adherence is assigned, never self-asserted.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
