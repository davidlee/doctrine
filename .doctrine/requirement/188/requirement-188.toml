schema = "doctrine.requirement"
version = 1

id = 188
slug = "the-snapshot-is-gitignored-runtime-state-derived-disposable-never-authoritative-check-reports-disk-freshness-only-never-claiming-the-live-in-session-prefix-is-fresh"
title = "The snapshot is gitignored runtime-state — derived, disposable, never authoritative; `--check` reports disk freshness only, never claiming the live in-session prefix is fresh."
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
