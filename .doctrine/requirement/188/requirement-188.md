# REQ-188: The snapshot is gitignored runtime-state — derived, disposable, never authoritative; `--check` reports disk freshness only, never claiming the live in-session prefix is fresh.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
