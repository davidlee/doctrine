schema = "doctrine.requirement"
version = 1

id = 67
slug = "store-every-facet-and-evidence-entry-as-typed-data-never-an-untyped-bag"
title = "Store every facet and evidence entry as typed data, never an untyped bag"
status = "active"
kind = "quality"

description = "Every common field, kind facet, and evidence entry is enumerated, typed storage; no untyped frontmatter catch-all ever holds record data."
tags = []
acceptance_criteria = [
  "Each kind's facet fields are enumerated and typed; there is no raw frontmatter bag.",
  "Evidence is a minimal typed support structure (supports / contradicts / notes of citations), not a free-form blob and not its own entity kind.",
]
