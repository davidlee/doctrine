# REQ-149: Address a memory as a named entity by a client-minted content-addressable UUID, with an edit-preserving memory.toml plus prose memory.md and a mem-key slug aliased by a non-authoritative symlink

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
