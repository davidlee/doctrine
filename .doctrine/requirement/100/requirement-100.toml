schema = "doctrine.requirement"
version = 1

id = 100
slug = "surface-drift-between-authored-truth-and-observed-coverage-evidence"
title = "Surface drift between authored truth and observed coverage evidence"
status = "active"
kind = "functional"

description = "Divergence between a requirement's authored status (and its owning spec truth) and its observed coverage evidence is surfaced as a prompt to reconcile."
tags = []
acceptance_criteria = [
  "Where authored status and observed coverage disagree, the divergence is reported as drift.",
  "Drift is surfaced as a prompt to reconcile, never auto-corrected.",
  "A requirement whose authored status already matches its evidence is reported as coherent, not drifted.",
]
