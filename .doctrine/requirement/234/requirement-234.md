# REQ-234: Carry `interactions.toml` `[[edge]]` rows (target/type/notes) under the `edge` key as the tech-only outbound peer axis; a missing file yields zero edges.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
