# REQ-147: Attest a memory with verify by stamping its verification axis against a clean working tree, refusing a dirty tree so no false attestation is recorded

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
