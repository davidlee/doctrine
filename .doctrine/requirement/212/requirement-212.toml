schema = "doctrine.requirement"
version = 1

id = 212
slug = "tolerate-an-out-of-vocabulary-stored-status-never-hide-it-surface-a-trailing-drift-marker-and-leave-it-unrejected-on-disk-rather-than-failing-the-read"
title = "Tolerate an out-of-vocabulary stored status: never hide it, surface a trailing ? drift marker, and leave it unrejected on disk rather than failing the read."
status = "active"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
