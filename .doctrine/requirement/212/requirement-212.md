# REQ-212: Tolerate an out-of-vocabulary stored status: never hide it, surface a trailing ? drift marker, and leave it unrejected on disk rather than failing the read.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
