schema = "doctrine.requirement"
version = 1

id = 151
slug = "render-every-retrieved-memory-as-a-quoted-attributed-delimited-data-block-never-instruction-and-suppress-quarantined-and-retracted-memories-from-agent-context"
title = "Render every retrieved memory as a quoted, attributed, delimited data block, never instruction, and suppress quarantined and retracted memories from agent context"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
