# REQ-151: Render every retrieved memory as a quoted, attributed, delimited data block, never instruction, and suppress quarantined and retracted memories from agent context

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
