schema = "doctrine.requirement"
version = 1

id = 242
slug = "resolve-record-kind-from-the-id-prefix-on-the-read-path-so-one-verb-set-dispatches-across-all-four"
title = "Resolve record_kind from the id prefix on the read path so one verb set dispatches across all four prefixes; identity is permanent and record_kind is fixed at capture, never silently changing"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
