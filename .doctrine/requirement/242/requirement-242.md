# REQ-242: Resolve record_kind from the id prefix on the read path so one verb set dispatches across all four prefixes; identity is permanent and record_kind is fixed at capture, never silently changing

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
