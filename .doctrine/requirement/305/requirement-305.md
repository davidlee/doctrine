# REQ-305: Relations are stored outbound-only — inbound is always derived from `in_edges` and never persisted, the sole typed reverse being the `superseded_by` carve-out (ADR-004).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
