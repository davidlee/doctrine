# REQ-160: Detect id-integrity violations with validate: dir basename equals toml id, no intra-kind duplicate id, and every alias targets the dir whose toml id equals its encoded id

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
