# REQ-219: Model every kind as one entity discriminated by a kind facet over the kind-blind engine — never parallel per-kind schemas; kind variation is facet fields, not a fork.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
