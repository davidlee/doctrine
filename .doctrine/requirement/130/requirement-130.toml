schema = "doctrine.requirement"
version = 1

id = 130
slug = "fix-the-adr-status-vocabulary-to-proposed-accepted-rejected-superseded-and-deprecated-used-as-both-transition-set-and-filter-known-set"
title = "Fix the ADR status vocabulary to proposed, accepted, rejected, superseded, and deprecated, used as both transition set and filter known-set"
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
