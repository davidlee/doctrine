schema = "doctrine.requirement"
version = 1

id = 315
slug = "cas-journal-recovery-contract-with-worktree-aware-advance"
title = "CAS journal recovery contract with worktree-aware advance"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
