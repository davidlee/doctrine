# REQ-456: A stale result enters the existing object-only candidate engine through explicit capsule provenance; clean and conflicted outcomes are durable, and every clean, hand-resolved, or fix-on-top candidate is freshly verified before immutable-commit admission.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
