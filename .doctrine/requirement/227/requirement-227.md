# REQ-227: Project Active Policies (required) and Active Standards (default + required) into the boot snapshot via governance::list_rows with an explicit in-force set.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
