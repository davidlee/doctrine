# REQ-209: Resolve an orthogonal advisory conduct posture (actor × autonomy) per lifecycle state from the project doctrine.toml [conduct] table, surfaced on slice status as the source state's exit posture.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
