schema = "doctrine.requirement"
version = 1

id = 256
slug = "provide-a-production-write-and-withdraw-path-for-observed-coverage-entries"
title = "Provide a production write and withdraw path for observed coverage entries"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
