schema = "doctrine.requirement"
version = 1

id = 136
slug = "produce-requirements-only-through-spec-req-add-reserve-the-requirement-set-its-kind-allocate-the-next-free-membership-label-and-append-the-membership-edge-edit-preservingly"
title = "Produce requirements only through spec req add — reserve the requirement, set its kind, allocate the next-free membership label, and append the membership edge edit-preservingly"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
