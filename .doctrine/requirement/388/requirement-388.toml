schema = "doctrine.requirement"
version = 1

id = 388
slug = "every-funnel-git-read-is-a-first-class-read-verb-over-the-object-db-and-ref-primitives-no-funnel"
title = "Every funnel git read is a first-class read verb over the object-db and ref primitives; no funnel read shells raw git. Existing seams are reused or relocated rather than reimplemented (e.g. is_linked_worktree for isolation detection); only genuinely absent reads are newly built."
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
