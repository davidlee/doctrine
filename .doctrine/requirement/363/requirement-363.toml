schema = "doctrine.requirement"
version = 1

id = 363
slug = "emit-a-published-asset-by-logical-address"
title = "Emit a published asset by logical address"
status = "pending"
kind = "functional"

description = "The library emits a published asset's exact bytes by its stable logical address, suitable for reading, piping, or redirect."
tags = []
acceptance_criteria = [
  "Showing a published address emits that asset's exact bytes to stdout.",
  "An unknown or unpublished address is refused; a published address whose backing source is unavailable is a distinct, clearly reported failure.",
]
