schema = "doctrine.requirement"
version = 1

id = 123
slug = "address-entities-by-canonical-string-id-externally-and-numeric-id-internally-cross-references-target-the-durable-peer-id-membership-labels-ride-on-edges"
title = "Address entities by canonical string id externally and numeric id internally; cross-references target the durable peer id, membership labels ride on edges"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
