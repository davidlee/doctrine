schema = "doctrine.requirement"
version = 1

id = 319
slug = "trunk-resolution-via-freshest-descendant-with-ancestry-close-backstop"
title = "Trunk resolution via freshest_descendant with ancestry close backstop"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
