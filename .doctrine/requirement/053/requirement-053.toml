schema = "doctrine.requirement"
version = 1

id = 53
slug = "carry-kind-specific-facets-including-the-risk-facet"
title = "Carry kind-specific facets including the risk facet"
status = "active"
kind = "functional"

description = "A backlog item carries the descriptive facet fields its kind defines, including the risk facet (likelihood, impact, origin, controls), within the single shared entity."
tags = []
acceptance_criteria = [
  "Each kind carries its defined descriptive facet set; a risk item carries likelihood, impact, origin, and controls.",
  "A kind facet holds only the item's shape; no close-reason (accepted/expired/mitigated/…) is stored in it — those are `resolution` values.",
  "All kinds, including risk, are the one `backlog_item` entity discriminated by `item_kind` — no parallel schema.",
]
