# REQ-308: Validation is write-strict and read-tolerant — `link` hard-refuses dangling or illegal-kind numbered edges at write, while the reader and corpus `validate` report danglers and illegal hand-edited rows but never rewrite.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
