# REQ-328: Compare model-axis specificity root-wise — pinned patterns keyed by top-level segment, ordered (root, depth) pairs, lexicographic — so same-root stays depth-ordered, intersections outrank their factors, and cross-tree ordering is stable under taxonomy deepening.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
