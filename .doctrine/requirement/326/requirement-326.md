# REQ-326: Allow a selector to pin a conjunctive set of model patterns — every pinned pattern must match some context key — so trait intersections (e.g. smart AND loose) are targetable by one snippet without grammar-OR.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
