# REQ-185: `boot install` idempotently prepends the `@`-import once per harness file (Claude CLAUDE.md, codex AGENTS.md), deduping same-inode symlink targets to one write.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
