schema = "doctrine.requirement"
version = 1

id = 324
slug = "match-purely-conjunctively-every-pinned-selector-axis-must-match-the-context-unpinned-axes-are-dont"
title = "Match purely conjunctively: every pinned selector axis must match the context; unpinned axes are don't-care; model patterns prefix-match left-to-right with _default as per-level wildcard (contiguous-subtree OR); non-match is absence, never suppression."
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
