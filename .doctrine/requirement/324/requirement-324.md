# REQ-324: Match purely conjunctively: every pinned selector axis must match the context; unpinned axes are don't-care; model patterns prefix-match left-to-right with _default as per-level wildcard (contiguous-subtree OR); non-match is absence, never suppression.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
