schema = "doctrine.requirement"
version = 1

id = 222
slug = "fix-the-policy-status-vocabulary-to-draft-required-deprecated-retired-required-is-the-in-force-mandated-state"
title = "Fix the policy status vocabulary to draft, required, deprecated, retired; required is the in-force mandated state."
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
