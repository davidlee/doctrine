schema = "doctrine.requirement"
version = 1

id = 413
slug = "confine-storage-paths-and-worker-capture-authority"
title = "Confine storage paths and worker capture authority"
status = "pending"
kind = "quality"
description = "The ledger confines every filesystem path and narrows worker capture to a named primary-root capability that cannot become a general write primitive."

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = ["observation", "security", "containment", "confinement"]
acceptance_criteria = [
  "Kind, date, and UUID path components are derived from validated typed values and cannot escape the observation root.",
  "The MCP server resolves the registered primary repository root server-side and accepts no caller path.",
  "Worker capability conformance admits only the named observation capture tool in addition to explicitly approved worker tools.",
]
