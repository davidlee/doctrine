# REQ-332: Keep the engine pure (no disk/clock/env; seal set passed in) and deterministic: the same corpus, context, and seal set yield byte-identical output — downstream session-start cache-hold depends on it.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
