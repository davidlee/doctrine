# REQ-301: A spent isolation unit is reaped only when its result provably landed in durable git state, never on a disposable receipt; concurrent dispatch of the same slice is refused at creation.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
