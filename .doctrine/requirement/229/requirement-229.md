# REQ-229: Define `descends_from`, `parent`, `c4_level`, and `[[source]]` as tech-only flat spine fields on the parsed Spec, each serde-default so absent on a product or unfilled tech spec.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
