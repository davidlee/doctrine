schema = "doctrine.requirement"
version = 1

id = 77
slug = "produce-deterministic-derived-ordering-and-explanations"
title = "Produce deterministic derived ordering and explanations"
status = "active"
kind = "quality"

description = "The same registry graph, policy version, and query options always produce the same derived ordering and explanations."
tags = []
acceptance_criteria = [
  "Re-running the same query over unchanged input yields an identical order and identical explanations.",
  "Tie-breaking is a stable deterministic contributor, not an incidental ordering.",
  "Ordering is reproducible without dependence on clock, randomness, or filesystem iteration order.",
]
