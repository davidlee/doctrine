# REQ-197: Present every entity kind through a uniform `<kind> <verb>` grammar: a top-level kind enum delegating to per-kind verb enums sharing new/list/show/(status).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
