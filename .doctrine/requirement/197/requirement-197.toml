schema = "doctrine.requirement"
version = 1

id = 197
slug = "present-every-entity-kind-through-a-uniform-kind-verb-grammar-a-top-level-kind-enum-delegating-to-per-kind-verb-enums-sharing-newlistshowstatus"
title = "Present every entity kind through a uniform `<kind> <verb>` grammar: a top-level kind enum delegating to per-kind verb enums sharing new/list/show/(status)."
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
