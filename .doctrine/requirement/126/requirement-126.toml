schema = "doctrine.requirement"
version = 1

id = 126
slug = "write-status-and-coupled-key-transitions-edit-preservingly-leaving-comments-inert-tables-and-unknown-keys-intact"
title = "Write status and coupled-key transitions edit-preservingly, leaving comments, inert tables, and unknown keys intact"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
