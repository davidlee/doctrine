# REQ-193: Capture the born frame in `git.rs`, reproducing `forget.remote.v1` remote normalization and `forget.checkout.v1` checkout-state hashing byte-for-byte against the event store.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
