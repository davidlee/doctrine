# REQ-170: Install is idempotent: files written only when absent and never overwritten, dirs created-if-missing, gitignore lines deduplicated

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
