schema = "doctrine.requirement"
version = 1

id = 292
slug = "integration-is-two-stage-and-audit-gated-stage-1-sync-prepare-review-materialises-reviewslice-and"
title = "Integration is two-stage and audit-gated: stage-1 sync --prepare-review materialises review/<slice> and phase/<slice>-NN plus a CAS journal committed before any ref mutation and writes no trunk; audit runs from the parent/root against the prepared refs; stage-2 sync --integrate is opt-in, fast-forward-only, expected-tip-CAS, and reports (never auto-resolves, never force-pushes) a moved/non-ff target."
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
