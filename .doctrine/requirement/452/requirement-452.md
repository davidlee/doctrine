# REQ-452: Trusted Git reads only the parent-owned bundle snapshot through a fresh quarantine repository and never uses a capsule-authored repository as repository or working context.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
