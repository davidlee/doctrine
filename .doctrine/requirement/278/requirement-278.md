# REQ-278: Define a reusable ValueFacet with a single finite f64 magnitude parsed from an optional [value] TOML table

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
