schema = "doctrine.requirement"
version = 1

id = 257
slug = "keep-the-verification-command-contract-project-agnostic"
title = "Keep the verification command contract project-agnostic"
status = "active"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
