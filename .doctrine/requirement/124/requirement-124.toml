schema = "doctrine.requirement"
version = 1

id = 124
slug = "allocate-ids-and-place-named-entities-through-one-atomic-claim-seam-interpreting-an-existing-claim-per-caller"
title = "Allocate ids and place named entities through one atomic claim seam, interpreting an existing claim per caller"
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
