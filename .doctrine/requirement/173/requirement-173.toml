schema = "doctrine.requirement"
version = 1

id = 173
slug = "embed-the-plugins-source-tree-at-compile-time-as-the-single-canonical-skill-source-feeding-both-the-marketplace-and-binary-channels"
title = "Embed the plugins source tree at compile time as the single canonical skill source feeding both the marketplace and binary channels"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
