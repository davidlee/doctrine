schema = "doctrine.requirement"
version = 1

id = 6
slug = "maintain-storage-tier-integrity-between-authored-runtime-and-derived-data"
title = "Maintain storage-tier integrity between authored, runtime, and derived data"
status = "active"
kind = "quality"

description = "Authored contract data, disposable runtime progress, and derived indexes stay in their correct storage tiers; queryable data never leaks into prose."
tags = []
acceptance_criteria = [
  "Authored structured data lives in TOML and prose in markdown, never queried/derived data in prose.",
  "Runtime progress is disposable and never recorded in the authored contract.",
  "Derived indexes are regenerable and never the source of truth.",
]
