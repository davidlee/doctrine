# REQ-200: Select and order visible table columns via --columns over pre-materialised Column descriptors; reject unknown names with the available set; no effect under --json; rejected on memory list.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
