# REQ-245: Model all four kinds as one knowledge_record entity discriminated by record_kind over the kind-blind engine — never parallel per-kind schemas — reusing the shared entity scaffold so the existing slice, ADR, spec, backlog, and memory suites stay green unchanged

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
