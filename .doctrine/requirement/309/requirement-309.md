# REQ-309: The cross-kind write seam is the uniform `link`/`unlink` verb over generic `append_edge`/`remove_edge`, gated by the table’s `LinkPolicy` (`Writable` / `LifecycleOnly` / `TypedVerbOnly`).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
