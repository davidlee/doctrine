# REQ-154: Verification state is a separate axis from lifecycle status; a memory can be active, stale, and unverified at once, and verification is never folded into status

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
