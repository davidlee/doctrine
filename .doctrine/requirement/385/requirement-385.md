# REQ-385: Every funnel verb is legality-gated on funnel position: it refuses out-of-order execution and names the expected next verb (report-and-halt with prescription); conclude refuses after skipped or failed verification.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
