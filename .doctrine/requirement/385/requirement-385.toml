schema = "doctrine.requirement"
version = 1

id = 385
slug = "every-funnel-verb-is-legality-gated-on-funnel-position-it-refuses-out-of-order-execution-and-names"
title = "Every funnel verb is legality-gated on funnel position: it refuses out-of-order execution and names the expected next verb (report-and-halt with prescription); conclude refuses after skipped or failed verification."
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
