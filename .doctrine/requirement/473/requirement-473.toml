schema = "doctrine.requirement"
version = 1

id = 473
slug = "reference-the-executable-seam-never-duplicate-it"
title = "Reference the executable seam, never duplicate it"
status = "pending"
kind = "quality"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
description = "The plan cites canonical requirement, specification, and verification-procedure owners and projects into the executable coverage seam rather than growing a second command or evidence schema of its own."
tags = []
acceptance_criteria = [
  "The plan cites canonical requirement and specification identities instead of restating their normative content.",
  "Executable procedure identity and observed results stay with their canonical owner and are referenced or projected, not re-declared in the plan.",
  "The plan-side gate asserts that mandated work is present rather than that it is semantically correct, so it does not become a parallel execution contract.",
]
