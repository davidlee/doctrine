schema = "doctrine.requirement"
version = 1

id = 169
slug = "make-a-new-authored-kind-distributable-by-adding-its-dir-to-the-manifest-and-negating-its-derived-subtrees-in-gitignore"
title = "Make a new authored kind distributable by adding its dir to the manifest and negating its derived subtrees in gitignore"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
