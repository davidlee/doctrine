# REQ-455: Admission journals intent before canonical mutation, prechecks the expected accepted tip before object transfer, and advances once by expected-old-object compare-and-swap with idempotent replay classification.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
