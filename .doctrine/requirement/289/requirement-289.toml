schema = "doctrine.requirement"
version = 1

id = 289
slug = "parallel-execution-of-file-disjoint-phases-is-first-class-while-landing-is-one-per-base-under-the"
title = "Parallel execution of file-disjoint phases is first-class while landing is one-per-base under the stationary-head precondition; serial-dependent phases self-base by advancing coordination HEAD to phase N's integrated tip before spawning N+1."
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
