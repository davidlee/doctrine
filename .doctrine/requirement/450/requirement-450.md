# REQ-450: Every phase worker and every verifier launches headlessly in fresh mutable capsule state from explicit immutable inputs; harvested capsules never resume formal execution.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
