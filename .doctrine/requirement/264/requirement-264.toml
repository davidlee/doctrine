schema = "doctrine.requirement"
version = 1

id = 264
slug = "surface-estimates-in-detailed-display-with-optional-derived-spread"
title = "Surface estimates in detailed display with optional derived spread"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
