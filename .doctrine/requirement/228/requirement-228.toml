schema = "doctrine.requirement"
version = 1

id = 228
slug = "each-status-known-set-mirrors-its-clap-enum-exactly-held-in-lockstep-by-a-per-kind-drift-canary-test-so-the-known-set-is-the-complete-vocabulary"
title = "Each status known-set mirrors its clap enum exactly, held in lockstep by a per-kind drift-canary test so the known-set is the complete vocabulary."
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
