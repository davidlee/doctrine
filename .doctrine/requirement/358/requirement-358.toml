schema = "doctrine.requirement"
version = 1

id = 358
slug = "governance-stays-distinct-from-the-orientation-surface"
title = "Governance stays distinct from the orientation surface"
status = "pending"
kind = "quality"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
description = "Standing governance never ships as base projection and is never folded into the project-orientation.md orientation surface."
tags = []
acceptance_criteria = []
