# REQ-198: Provide a kind-blind list spine carrying filter semantics, the status known-set check, the canonical id form, the table layout, and the JSON envelope, lifted out of per-kind list code.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
