schema = "doctrine.requirement"
version = 1

id = 300
slug = "integration-to-trunk-is-audit-gated-and-opt-in-no-slice-integrates-before-its-prepared-surfaces"
title = "Integration to trunk is audit-gated and opt-in: no slice integrates before its prepared surfaces pass audit, and integration is fast-forward-only, expected-tip-guarded, and reports (never force-pushes or auto-resolves) a moved or non-fast-forward target."
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
