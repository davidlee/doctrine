# REQ-329: Permit suppression only via replaces, legal only on the unique most-specific active snippet of its slot; duplicate targets, non-top replacers, and cycles are authoring errors surfaced by prompt check/validate, never silently alpha-ordered.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
