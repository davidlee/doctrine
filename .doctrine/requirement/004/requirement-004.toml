schema = "doctrine.requirement"
version = 1

id = 4
slug = "reserve-slice-identity-collision-free-under-concurrent-local-agents"
title = "Reserve slice identity collision-free under concurrent local agents"
status = "active"
kind = "functional"

description = "Concurrent agents sharing one working tree can each reserve a slice identity without colliding, without a lock, daemon, or central authority."
tags = []
acceptance_criteria = [
  "An empty project yields the first identity.",
  "When two agents contend for an identity, exactly one wins and the loser retakes the next free identity.",
  "No agent ever proceeds on a duplicated identity.",
]
