schema = "doctrine.requirement"
version = 1

id = 312
slug = "evidence-refs-are-immutable-zero-oid-cas-report-not-clobber"
title = "Evidence refs are immutable: zero-oid CAS, report-not-clobber"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
