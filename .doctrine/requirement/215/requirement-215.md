# REQ-215: Hide terminal (resolved, closed) items from the default list via the typed is_terminal predicate, revealed by --all or an explicit --status.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
