schema = "doctrine.requirement"
version = 1

id = 163
slug = "validate-is-a-pure-check-layer-facts-in-findings-out-no-disk-over-an-impure-scan-a-malformed-metadata-toml-is-a-hard-error-never-a-papered-over-finding"
title = "validate is a pure check layer (facts in, findings out, no disk) over an impure scan; a malformed metadata toml is a hard error, never a papered-over finding"
status = "active"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
