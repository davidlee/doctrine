# REQ-163: validate is a pure check layer (facts in, findings out, no disk) over an impure scan; a malformed metadata toml is a hard error, never a papered-over finding

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
