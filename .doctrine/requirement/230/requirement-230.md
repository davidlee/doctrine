# REQ-230: Fix `c4_level` to the closed kebab set context/container/component/code as an optional field, failing to parse on an out-of-vocab level.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
