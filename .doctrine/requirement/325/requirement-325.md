# REQ-325: Carry the context's model axis as a set of trait keys, matched by membership: a model pattern matches if it matches any member, so all of an agent's declared trait guidance composes in one resolve.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
