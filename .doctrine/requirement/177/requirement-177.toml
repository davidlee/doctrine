schema = "doctrine.requirement"
version = 1

id = 177
slug = "surface-claude-install-with-subset-agent-global-dry-run-and-yes-selection"
title = "Surface claude install with subset, agent, global, dry-run, and yes selection"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
