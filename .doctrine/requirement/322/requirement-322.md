# REQ-322: Load the snippet corpus as a union of compile-embedded install/hymns (framework) and on-disk .doctrine/hymns (user), deriving slot+selector from path with per-axis sidecar supersede, and deriving provenance from the source root, never a stored flag.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
