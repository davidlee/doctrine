schema = "doctrine.requirement"
version = 1

id = 176
slug = "install-claude-as-a-derived-canonical-skills-tree-with-relative-agent-symlinks-reconciled-by-proven-target-value-ownership"
title = "Install Claude as a derived canonical skills tree with relative agent symlinks reconciled by proven target-value ownership"
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
