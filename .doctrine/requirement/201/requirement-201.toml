schema = "doctrine.requirement"
version = 1

id = 201
slug = "fix-the-canonical-id-form-as-prefix-zero-padded-three-digit-number-the-single-id-form-authority-for-prefixed-kinds-with-memory-conformant-by-exception"
title = "Fix prefix plus zero-padded-three-digit number as the canonical identity for numbered prefixed kinds, with memory conformant-by-exception and SPEC-028 observations governed by bare UUID."
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
