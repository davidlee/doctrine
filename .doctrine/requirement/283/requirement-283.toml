schema = "doctrine.requirement"
version = 1

id = 283
slug = "configure-a-project-wide-value-unit-with-a-default"
title = "Configure a project-wide value unit with a default"
status = "active"
kind = "functional"

description = "The value unit is read from project config [value].unit and defaults to magic_beans when unconfigured; the unit is project-wide, never entity-local."
tags = []
acceptance_criteria = [
  "With [value].unit configured, values are interpreted and displayed in that unit.",
  "With [value].unit omitted, the unit defaults to magic_beans.",
  "No entity carries its own value unit in v1.",
]
