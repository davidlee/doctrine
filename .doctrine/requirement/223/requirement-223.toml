schema = "doctrine.requirement"
version = 1

id = 223
slug = "fix-the-standard-status-vocabulary-to-draft-default-required-deprecated-retired-default-is-recommended-unless-justified-required-is-mandated"
title = "Fix the standard status vocabulary to draft, default, required, deprecated, retired; default is recommended-unless-justified, required is mandated."
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
