schema = "doctrine.requirement"
version = 1

id = 389
slug = "the-funnel-is-working-tree-free-and-every-coord-tree-operation-is-bounded-by-a-no-pathless-commit"
title = "The funnel is working-tree-free and every coord-tree operation is bounded by a no-pathless-commit / safe-commit guard, so the ISS-234 reverse-diff cannot commit mass reversions; read verbs keep funnel reads from observing the reverse-diff, the guard closes the write side, and ISS-234 is absorbed only when both hold."
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
