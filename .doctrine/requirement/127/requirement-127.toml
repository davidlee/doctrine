schema = "doctrine.requirement"
version = 1

id = 127
slug = "lifecycle-vocabulary-is-family-specific-each-kind-owns-its-status-set-and-approval-is-never-folded-into-status"
title = "Lifecycle vocabulary is family-specific; each kind owns its status set and approval is never folded into status"
status = "active"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
