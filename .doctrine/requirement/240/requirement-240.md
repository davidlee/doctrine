# REQ-240: Carry a per-kind lifecycle vocabulary keyed by record_kind; the transition verb validates the target against the record's own kind vocabulary and refuses a foreign-kind state, with a per-kind is_terminal predicate driving the default-list hide-set

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
