schema = "doctrine.requirement"
version = 1

id = 155
slug = "resolve-every-undecidable-git-reachability-case-to-an-explicit-freshstaleunknownunanchoredreference-state-never-a-silent-hide-or-silent-over-trust"
title = "Resolve every undecidable git-reachability case to an explicit fresh/stale/unknown/unanchored/reference state, never a silent hide or silent over-trust"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
