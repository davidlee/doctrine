# REQ-448: The trusted control plane is the sole canonical mutation authority; a capsule worker may mutate local state and commits but cannot mutate canonical repositories, refs, credentials, shared object storage, or control-plane state.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
