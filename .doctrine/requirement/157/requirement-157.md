# REQ-157: Allocate a numbered id as max(local ∪ trunk) + 1 and claim it atomically in a bounded retry loop, recomputing on a lost race

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
