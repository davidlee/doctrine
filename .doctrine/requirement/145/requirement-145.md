# REQ-145: Retrieve and find memories scope-first: OR-match paths/globs/commands/tags by specificity weight and order results by the nine-key deterministic sort tuple so the same query yields the same order

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
