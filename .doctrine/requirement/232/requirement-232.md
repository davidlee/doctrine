# REQ-232: Model a `[[source]]` anchor as language + identifier + optional module, parsed via serde rename into a repeatable `sources` list.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
