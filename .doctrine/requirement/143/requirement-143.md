# REQ-143: Spec composition reads only the spec directory and the requirement directories reached by FK, and the integrity pass parses the typed edge tables, never the prose bodies

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
