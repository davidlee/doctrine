schema = "doctrine.requirement"
version = 1

id = 33
slug = "route-each-agent-to-direct-placement-or-delegated-install"
title = "Route each agent to direct placement or delegated install"
status = "pending"
kind = "functional"

description = "Each target agent is routed to one of two paths: the agent whose layout doctrine owns is installed by direct placement, while every other agent is delegated to the universal external installer."
tags = []
acceptance_criteria = [
  "The agent doctrine owns the layout for yields direct placement of the skill trees.",
  "Every other agent yields a single delegated step carrying the equivalent external command.",
  "The delegated command appearing in the plan is reproducible by hand.",
]
