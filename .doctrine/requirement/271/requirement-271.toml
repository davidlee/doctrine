schema = "doctrine.requirement"
version = 1

id = 271
slug = "resolve-a-project-wide-estimation-unit-with-a-default"
title = "Resolve a project-wide estimation unit with a default"
status = "active"
kind = "functional"

description = "The estimation unit is read from doctrine.toml [estimation].unit, defaulting to espresso_shots; no entity carries its own unit."
tags = []
acceptance_criteria = [
  "With [estimation].unit configured, the resolved unit is that value.",
  "With [estimation].unit omitted, the resolved unit defaults to espresso_shots.",
  "The facet schema exposes no entity-local unit field.",
]
