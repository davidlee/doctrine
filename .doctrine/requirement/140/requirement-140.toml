schema = "doctrine.requirement"
version = 1

id = 140
slug = "a-membership-label-is-state-carried-on-the-spec-to-requirement-edge-never-the-requirements-durable-identity-which-every-reference-targets-as-req-nnn"
title = "A membership label is state carried on the spec-to-requirement edge, never the requirement's durable identity, which every reference targets as REQ-NNN"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
