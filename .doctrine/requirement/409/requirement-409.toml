schema = "doctrine.requirement"
version = 1

id = 409
slug = "serve-cli-and-mcp-capture-through-one-engine-contract"
title = "Serve CLI and MCP capture through one engine contract"
status = "pending"
kind = "functional"
description = "Trusted CLI adapters and the bounded MCP capture adapter invoke one shared observation service and return the same creation receipt contract."

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = ["observation", "cli", "mcp", "service"]
acceptance_criteria = [
  "Equivalent CLI and MCP friction requests produce equivalent stored records and receipts.",
  "The trusted CLI exposes friction record, show, list, search, supersede, and retract semantics without duplicating store or resolver logic.",
  "The MCP adapter exposes structured friction capture only and refuses measurement, correction controls, and arbitrary path input.",
  "Measurement creation is unavailable to public CLI/MCP callers and succeeds only through a registered machine-source adapter supplying source, scope, units, completeness, and supported counters.",
]
