schema = "doctrine.requirement"
version = 1

id = 477
slug = "abandoned-scope-sweep-gated-on-target-write-landing"
title = "A hook set that lands in the selected scope evicts doctrine-owned entries from the abandoned sibling scope by the same ownership predicate, gated on the target write landing, and reports what it removed, could not read, or did not attempt."
status = "active"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
