schema = "doctrine.requirement"
version = 1

id = 355
slug = "hymns-materialize-on-first-customization"
title = "Hymns materialize on first customization"
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
description = "Materialize a hymn on disk only when it is first customized. Trigger: explicit hymn customization; actor: the customization verb."
tags = []
acceptance_criteria = []
