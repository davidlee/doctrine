schema = "doctrine.requirement"
version = 1

id = 407
slug = "resolve-append-only-corrections-into-active-and-historical-projections"
title = "Resolve append-only corrections into active and historical projections"
status = "pending"
kind = "functional"
description = "The resolver derives exact, active, and historical projections from append-only correction controls without mutating source observations."

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = ["observation", "resolve", "supersession", "retraction"]
acceptance_criteria = [
  "A valid supersession selects its existing compatible primary replacement and a valid retraction removes only its exact primary target from the active projection; controls cannot target controls.",
  "History retains every primary observation and correction control.",
  "Malformed, dangling, kind-incompatible, cycle-introducing, and losing conflicting controls are individually diagnostic and inert without cancelling an earlier valid correction.",
  "Repeated retractions and same-replacement supersessions are idempotent, retraction dominates supersession for one target, and distinct successors resolve by earliest valid control key.",
]
