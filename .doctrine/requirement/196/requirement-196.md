# REQ-196: Normalizer algorithms are version-tagged (`REMOTE_NORMALIZER`/`CHECKOUT_NORMALIZER`) so a deliberate change is visible and never silently re-anchors existing memory.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
