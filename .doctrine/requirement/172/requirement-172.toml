schema = "doctrine.requirement"
version = 1

id = 172
slug = "install-owns-neither-skills-distribution-nor-memory-sync-treating-both-as-a-sibling-verb-it-hints-at-or-defers-to"
title = "Install owns neither skills distribution nor memory sync, treating both as a sibling verb it hints at or defers to"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
