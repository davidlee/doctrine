schema = "doctrine.requirement"
version = 1

id = 205
slug = "define-the-slice-kind-whose-reserved-scaffold-materialises-slice-idtoml-slice-idmd-and-an-id-slug-symlink-plus-non-reserved-designplannotes-sibling-kinds-on-the-shared-engine"
title = "Define the slice Kind whose reserved scaffold materialises slice-<id>.toml, slice-<id>.md, and an <id>-<slug> symlink, plus non-reserved design/plan/notes sibling kinds on the shared engine."
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
