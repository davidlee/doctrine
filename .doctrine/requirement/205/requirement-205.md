# REQ-205: Define the slice Kind whose reserved scaffold materialises slice-<id>.toml, slice-<id>.md, and an <id>-<slug> symlink, plus non-reserved design/plan/notes sibling kinds on the shared engine.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
