# REQ-386: dispatch next emits the single prescribed next action for the per-phase import-verify-conclude-reap sub-funnel from the current position; candidate/close/audit sourcing (REQ-317, OQ-4) is outside the oracle scope until a sibling revision settles it.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
