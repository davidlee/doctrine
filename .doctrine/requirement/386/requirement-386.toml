schema = "doctrine.requirement"
version = 1

id = 386
slug = "dispatch-next-emits-the-single-prescribed-next-action-for-the-per-phase-import-verify-conclude-reap"
title = "dispatch next emits the single prescribed next action for the per-phase import-verify-conclude-reap sub-funnel from the current position; candidate/close/audit sourcing (REQ-317, OQ-4) is outside the oracle scope until a sibling revision settles it."
status = "pending"
kind = "functional"

# description — the requirement statement (structured, queryable; `show` renders
# it as the statement line — the storage rule forbids deriving it from prose).
# Rationale and extended discussion stay in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
