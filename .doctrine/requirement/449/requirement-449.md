# REQ-449: Each transaction binds an immutable accepted base and a required interpretation policy resolved from that base; a phase work contract may only restrict permissions and strengthen verification.

## Statement

<!-- The sister TOML's `description` field is the primary, normative statement.
     Prose here may elaborate, expand upon, or disambiguate it — never
     duplicate it. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
