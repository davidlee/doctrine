schema = "doctrine.requirement"
version = 1

id = 57
slug = "mutate-a-backlog-item-atomically-and-edit-preservingly"
title = "Mutate a backlog item atomically and edit-preservingly"
status = "active"
kind = "quality"

description = "Every mutation of a backlog item is atomic and edit-preserving, round-tripping the file without dropping comments or unknown keys."
tags = []
acceptance_criteria = [
  "A status or facet change preserves comments and unknown keys verbatim.",
  "A mutation rewrites the item as a whole — never a partial field-level merge of old and new values.",
  "The item round-trips through read-modify-write unchanged except for the intended field.",
]
