# REQ-183: Fall back to a fixed `<!-- heading: not yet populated -->` marker on any producer miss (error or empty listing), so a missing source never crashes assembly.

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
