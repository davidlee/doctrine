# REQ-239: Bind four record_kinds (assumption, decision, question, constraint) onto four engine Kinds, each its own tree and reservation namespace, diverging only by prefix and the per-kind facet and lifecycle seed

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
