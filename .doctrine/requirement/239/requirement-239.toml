schema = "doctrine.requirement"
version = 1

id = 239
slug = "bind-four-record-kinds-assumption-decision-question-constraint-onto-four-engine-kinds-each-its-own"
title = "Bind four record_kinds (assumption, decision, question, constraint) onto four engine Kinds, each its own tree and reservation namespace, diverging only by prefix and the per-kind facet and lifecycle seed"
status = "pending"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
