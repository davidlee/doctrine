schema = "doctrine.requirement"
version = 1

id = 311
slug = "ref-taxonomy-and-two-mutability-classes"
title = "Ref taxonomy and two mutability classes"
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
