schema = "doctrine.requirement"
version = 1

id = 255
slug = "derive-observed-vt-coverage-status-from-a-real-command-run"
title = "Derive observed VT coverage status from a real command run"
status = "active"
kind = "functional"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
