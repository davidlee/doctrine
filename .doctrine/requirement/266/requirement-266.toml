schema = "doctrine.requirement"
version = 1

id = 266
slug = "estimates-are-optional-everywhere-and-never-gate-work"
title = "Estimates are optional everywhere and never gate work"
status = "pending"
kind = "quality"

# description — optional one-line summary (queryable). Omitted by default (None);
# the full statement/rationale stays prose in the sister `.md`. Add as:
#   description = "…"
tags = []
acceptance_criteria = []
