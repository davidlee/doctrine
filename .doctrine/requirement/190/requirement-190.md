# REQ-190: Refuse provision when any `.worktreeinclude` pattern names a withheld tier, and back `check-allowlist` with `allowlist_violations` as a static smell test (not a completeness guarantee).

## Statement

<!-- The requirement in full: what must hold, stated testably. -->

## Rationale

<!-- Why it must hold — the force behind it, not the implementation. -->
