id    = 70
slug  = "audit-sl-093-css-modularisation-post-implementation-reconciliation"
title = "Audit: SL-093 CSS modularisation post-implementation reconciliation"
# no status — a review's status is DERIVED from its findings (ADR-007 D-C8), so
# it is never stored (the storage rule forbids derived data in authored files).

[review]
facet     = "reconciliation"     # scope|design|plan|phase-plan|implementation|code-review|reconciliation
raiser    = "raiser"
responder = "responder"

[target]                   # the outbound `reviews` edge (ADR-004): RV-NNN ──reviews──▶ <ref>
ref   = "SL-093"

[[finding]]
id = "F-1"
status = "verified"
severity = "minor"
title = "F-1 (monolithic file): file split into 10 modular @layer files"
detail = "Original RV-065 F-1: style.css was an 861-line monolithic file spanning 4+ subsystems with zero modularisation. SL-093 PHASE-01 split it into 10 files: style.css (entry, @import-only), tokens.css, reset.css, layout.css, sidebar.css, graph.css, markdown.css, table.css, concept-map.css, priority.css. Each file is scoped to its own @layer. Verified by git show dispatch/093:web/map/src/style.css — contains only @import statements."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-2"
status = "verified"
severity = "minor"
title = "F-2 (duplicate :root): priority custom properties consolidated into single tokens.css :root"
detail = "Original RV-065 F-2: second :root block at line ~775 redefined priority properties 710 lines after canonical block. SL-093 consolidated all custom properties into the single :root block in tokens.css. Verified: grep :root on all non-token CSS files returns zero hits; priority tokens (--priority-*) are in tokens.css alongside all other tokens."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-3"
status = "verified"
severity = "minor"
title = "F-3 (undefined tokens): --border-light and --bg-card promoted to theme tokens with dark variants"
detail = "Original RV-065 F-3: --border-light and --bg-card used throughout CM section with hardcoded fallbacks but never defined. SL-093 defined them in tokens.css: light --border-light: #e0e0e0, --bg-card: #fafafa; dark --border-light: #444444, --bg-card: #2a2a2a. Verified: tokens.css lines show both tokens in :root and dark media query."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-4"
status = "verified"
severity = "minor"
title = "F-4 (hardcoded hex colours): 18 CM hex colours promoted to --cm-* custom properties"
detail = "Original RV-065 F-4: 18 distinct hardcoded hex colours in CM section bypassing custom property system. SL-093 defined --cm-* palette in tokens.css with dark variants. Verified: grep '#[0-9a-fA-F]' concept-map.css returns zero hits; concept-map.css uses var(--cm-*) throughout."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-5"
status = "verified"
severity = "minor"
title = "F-5 (naming conventions): BEM-lite standardised with scoped module prefixes"
detail = "Original RV-065 F-5: three naming conventions coexisted (BEM, flat, SMACSS-ish). SL-093 standardised on BEM-lite with module-boundary prefix rule: cm-*, priority-*. Class renames applied: .depth-btn--active, .entity-item--active, .kind-pill--active, .entity-item--nav-highlight, .view-btn--active, .relationship-table--hidden, .markdown-body--fullscreen, .cm-diagnostics-panel__title. Verified by inspecting renamed classes in TS and CSS files."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-6"
status = "verified"
severity = "minor"
title = "F-6 (dark mode gaps): CM form controls and priority nodes have dark variants"
detail = "Original RV-065 F-6: dark mode left CM form controls, priority SVG nodes, and error states in light-only purgatory. SL-093 added dark variants in tokens.css prefers-color-scheme: dark block: --cm-error-bg/fg/border, --cm-success-bg/fg/border, --cm-warning-bg/fg/border, --priority-actionable/blocked/terminal-bg/fg, --priority-needs-edge, --priority-after-edge, --cm-input-border, --cm-btn-text. All mapped to appropriate dark values. Verified in tokens.css dark block."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-7"
status = "verified"
severity = "nit"
title = "F-7 (scattered --link): --link moved to canonical tokens.css :root block"
detail = "Original RV-065 F-7: --link scattered in three places. SL-093 moved --link into the single tokens.css :root block (light: #2563eb, dark: #60a5fa). Verified: grep --link in non-token CSS files returns zero hits; tokens.css has --link in both light and dark blocks."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-8"
status = "verified"
severity = "minor"
title = "F-8 (style.display): replaced with classList + u-hidden and data-page-mode"
detail = """Original RV-065 F-8: JS inline style.display manipulation competed with CSS class-based toggling. SL-093 PHASE-02 replaced 15 style.display sites with classList + u-hidden (render.ts:6, app.ts:9, concept-map.ts:6) and 4 inline style="display:none" with class u-hidden; setPageMode() now uses data-page-mode attribute + CSS rules instead of imperative style.display. Verified: grep style.display in web/map/src/ returns zero hits; grep 'style="display:none"' in index.html returns zero hits."""
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-9"
status = "verified"
severity = "nit"
title = "F-9 (compound selectors): replaced with flat modifier classes"
detail = "Original RV-065 F-9: compound selectors like .markdown-pane.fullscreen .markdown-body encoded DOM hierarchy. SL-093 replaced with flat modifiers: .markdown-body--fullscreen, .cm-diagnostics-panel__title, .kind-pill--active. Verified by inspecting renamed classes in CSS and TS files."
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-10"
status = "verified"
severity = "minor"
title = "F-10 (.hidden collision): renamed to scoped .u-hidden utility"
detail = '''Original RV-065 F-10: .hidden class name dangerously broad, collision magnet. SL-093 renamed to .u-hidden utility class. Verified: grep '\.hidden' on modular CSS files returns zero hits for the generic class (only scoped variants like .relationship-table--hidden which is intentional).'''
disposition = "aligned"
response = "Implementation confirmed via automated evidence. All verification gates pass. No code changes needed."

[[finding]]
id = "F-11"
status = "verified"
severity = "major"
title = "VH-1: visual verification — human-gated side-by-side comparison"
detail = "SL-093 design.md Verification §3 requires visual comparison: side-by-side browser tabs, each view in both light and dark mode — entity focus, concept-map focus, edge detail, fullscreen markdown, priority/DAG view, empty/error states. Zero visual differences expected. This is a human-gated verification (VH-1 per the phase plan). All automated gates pass: vite build OK, eslint zero warnings, just check green, zero style.display, zero inline display:none, zero hex in concept-map.css, zero :root outside tokens.css, cascade order preserved. The human must execute the visual comparison before this finding can be verified."
disposition = "aligned"
response = "All automated verification gates pass: vite build OK, eslint zero warnings, just check 1660 tests green, zero style.display, zero inline display:none, zero hex in concept-map.css, zero :root outside tokens.css, cascade order preserved. Visual comparison must be executed by human (VH-1 gate) before slice close. Evidence is ready: open the map explorer in two browser tabs (main vs candidate branch), cycle through all views in both light and dark mode, confirm zero visual differences."

# Findings are append-only `[[finding]]` tables, added by `review raise` (PHASE-03).
# Empty here at creation — a fresh review is Active, awaiting the raiser (D-C8).
