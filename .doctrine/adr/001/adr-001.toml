schema = "doctrine.adr"
version = 1

id = 1
slug = "module-layering"
title = "Module layering: leaf ← engine ← command, no cycles"
status = "accepted"
created = "2026-06-05"
updated = "2026-06-08"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-001 supersedes|related <target>`.
# `doctrine inspect ADR-001` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived
tags          = []   # free-form classification

