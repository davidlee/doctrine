schema = "doctrine.adr"
version = 1

id = 1
slug = "module-layering"
title = "Module layering: leaf ← engine ← command, no cycles"
status = "accepted"
created = "2026-06-05"
updated = "2026-06-27"
tags = []

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-001 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-001 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

