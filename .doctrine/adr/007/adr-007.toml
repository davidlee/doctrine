schema = "doctrine.adr"
version = 1

id = 7
slug = "adversarial-review-kind-turn-based-ledger"
title = "Adversarial review as a first-class kind with turn-based ledger coordination"
status = "accepted"
created = "2026-06-08"
updated = "2026-06-11"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-007 supersedes|related <target>`.
# `doctrine inspect ADR-007` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived
tags          = []   # free-form classification

