schema = "doctrine.adr"
version = 1

id = 24
slug = "shipped-corpus-grounding-any-address-a-client-can-resolve-never-a-repo-private-id-or-path"
title = "Shipped-corpus grounding: any address a client can resolve, never a repo-private id or path"
status = "accepted"
created = "2026-09-25"
updated = "2026-09-25"
tags = ["shipped-corpus", "publication", "asset-policy", "citation", "reachability", "pol-002"]

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-NNN <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-NNN related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

[[relation]]
label = "related"
target = "ADR-005"

[[relation]]
label = "related"
target = "ADR-019"
