schema = "doctrine.adr"
version = 1

id = 2
slug = "global-orientation-memory-class-repo-empty-unanchored-evergreen"
title = "Global orientation memory class: repo-empty, unanchored, evergreen"
status = "accepted"
created = "2026-06-05"
updated = "2026-06-05"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-002 supersedes|related <target>`.
# `doctrine inspect ADR-002` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived


[[relation]]
label = "related"
target = "ADR-001"
