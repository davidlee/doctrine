schema = "doctrine.adr"
version = 1

id = 2
slug = "global-orientation-memory-class-repo-empty-unanchored-evergreen"
title = "Global orientation memory class: repo-empty, unanchored, evergreen"
status = "accepted"
created = "2026-06-05"
updated = "2026-06-05"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-002 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-002 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived


[[relation]]
label = "related"
target = "ADR-001"
