schema = "doctrine.adr"
version = 1

id = 9
slug = "slice-lifecycle-fsm-conduct-axis"
title = "Slice lifecycle state machine and conduct axis"
status = "accepted"
created = "2026-06-08"
updated = "2026-06-08"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-009 supersedes|related <target>`.
# `doctrine inspect ADR-009` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived

