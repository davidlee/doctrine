schema = "doctrine.adr"
version = 1

id = 9
slug = "slice-lifecycle-fsm-conduct-axis"
title = "Slice lifecycle state machine and conduct axis"
status = "accepted"
created = "2026-06-08"
updated = "2026-06-08"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-009 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-009 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

