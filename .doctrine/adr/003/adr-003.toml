schema = "doctrine.adr"
version = 1

id = 3
slug = "canonical-change-loop-slice-first-observe-reconcile-close"
title = "Canonical change loop: slice-first, observe, reconcile, close"
status = "accepted"
created = "2026-06-07"
updated = "2026-06-08"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-003 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-003 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

