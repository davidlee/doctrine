schema = "doctrine.adr"
version = 1

id = 3
slug = "canonical-change-loop-slice-first-observe-reconcile-close"
title = "Canonical change loop: slice-first, observe, reconcile, close"
status = "accepted"
created = "2026-06-07"
updated = "2026-06-08"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-003 supersedes|related <target>`.
# `doctrine inspect ADR-003` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived

