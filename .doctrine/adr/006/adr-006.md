# ADR-006: Worktree posture: policy-agnostic framework, orchestrator-sole-writer dispatch

> **AMENDED — FALSIFIED IN PART (SL-254, 2026-08-14).** Both of this ADR's
> load-bearing themes survive: the policy-agnostic posture (D1) and the
> orchestrator-sole-writer funnel (D2/D7) hold, and are now enforced harder than
> when they were written. Almost every *mechanism* named below has been replaced.
> SL-254 collapsed dispatch from **two arms** — an in-session "claude arm" using
> Claude's `Agent` tool with `isolation: worktree`, and a subprocess arm for
> codex/pi — onto **one confined subprocess arm**: every harness, claude
> included, is spawned by `scripts/spawn-confined.sh <harness>` and wrapped in a
> kernel-level `bwrap` (Linux) / `sandbox-exec` (macOS) jail. The two backends
> fence writes by different means, and the writable set is **not** the fork
> worktree alone:
>
> - **Linux:** `--ro-bind / /` makes the whole filesystem read-only, then explicit
>   binds re-open the writable set — the fork worktree, **the harness config dir**,
>   a private `/tmp` tmpfs, and `/dev` (`scripts/spawn-confined.sh:176-183`).
> - **macOS:** there is no `--ro-bind`. The Seatbelt profile is `(allow default)`
>   plus a coarse `(deny file-write*)` floor (`jail.rs:208-209`), re-opened by
>   subpath allows for the worktree, its scratch dir, the device sinks, and one
>   `RW0…RWn` allow per `--extra-rw` — the harness config dir rides `RW0`
>   (`jail.rs:482-486`). Reads and exec are **unrestricted** on macOS; only writes
>   are fenced.
>
> **The harness config dir is a writable carve-out, and it is security-relevant.**
> `$HOME/.claude` (claude) / `$HOME/.pi` (pi) is bound read-write into the jail
> (`spawn-confined.sh:36-37`, `:159`, `:179`) — deliberately, since claude needs it
> for the subscription credential (`DEC-210`) and pi writes it at runtime. Because
> `~/.claude` holds `settings.json`, hooks and agent definitions, **a confined
> claude worker can write the orchestrator's own harness configuration.** Earlier
> text in this ADR called the fork worktree the *sole* write floor; that was false,
> and is corrected wherever it appeared. The shipped `dispatch-spawn` skill has
> always stated the carve-out correctly.
>
> Three consequences run through the whole
> document:
>
> 1. **Worker identity is the `DOCTRINE_WORKER` environment variable and nothing
>    else** (`DEC-207`). The disk marker at
>    `<root>/.doctrine/state/dispatch/worker` is **gone**, together with
>    `marker --stamp-subagent`, `marker --clear`, `status --assert`'s stale exit
>    and the `Cause` truth table. Identity is a property of the **process** — set
>    by the same confinement argv that establishes the write floor, and dying
>    with the process — not of a tree. Every "marker present / marker absent"
>    clause below is historical.
> 2. **There is no harness-native creation rung, and no degraded rung.**
>    `src/worktree/subagent.rs` (`SubagentStart`/`SubagentStop` stamping and
>    worker nomination) and `src/worktree/pretooluse.rs` (the in-session hook
>    wall) were deleted whole; the Claude Code `WorktreeCreate`/`WorktreeRemove`
>    hooks are not on the shipped path. **On Linux** a missing `bwrap` is a
>    **named refusal — spawn fails closed** (`jail.rs::REASON_NO_BWRAP`,
>    `DEC-208`). **On macOS there is no backend-presence probe at all**: `have_bwrap`
>    is `#[cfg(not(target_os = "macos"))]` with no Darwin sibling,
>    `spawn-confined.sh:61` skips the probe on Darwin, and `jail_prefix.rs`'s macOS
>    arm fails only on topology, policy, or profile-write errors — so a mac without
>    `sandbox-exec` gets a successful prefix write and then an **unnamed** exec
>    failure. Fail-closed either way; *named* only on Linux. There is no
>    unconfined fallback and no work-in-place degrade.
> 3. **Enforcement moved from cooperative to kernel-level, on every harness.**
>    The CLI worker-mode guard (`src/commands/guard.rs::worker_guard`) still
>    refuses Write- and Orchestrator-classed verbs when `DOCTRINE_WORKER == "1"`
>    (a value compare, not a presence test — `DOCTRINE_WORKER=0` is set but is
>    *not* worker mode),
>    but it is now a second belt *inside* a hard floor, not the floor itself. The
>    worker cannot commit at all — the `worker_commit` MCP tool is deleted
>    (`DEC-204`); a worker hands back an **uncommitted working tree** and the
>    orchestrator imports the diff.
>
> The title remains accurate and is **deliberately left unchanged**. This ADR is
> **amended in place, not superseded** — every historical body below is retained
> as the record of what was decided, and no longer describes the shipped posture.

## Context

Doctrine is a *framework* consumed by other projects, not only its own repo.
Parallel agent execution (`/dispatch`) needs git working-tree isolation so
concurrent workers do not stomp each other. Three forces constrain the design:

1. **Framework neutrality.** Branch/worktree policy is the consuming project's
   choice. Doctrine must work equally for a solo developer on trunk with trivial
   merges and a team on branches with PR review and real conflicts. Doctrine
   therefore owns the *merge/fork behaviour of its own artifacts*, not the
   project's git policy. When in doubt, framework utility outranks the build
   repo's local convenience.

2. **Doctrine's storage tiers already pre-adapt.** Authored (committed) /
   runtime (gitignored, disposable) / derived. Runtime phase state being
   gitignored makes it per-worktree by construction; no central registry or
   counter exists, so authored entities are disjoint across slices.

3. **The hazards are concurrency, not storage.** Sequential `SL-NNN` ids can
   collide across concurrent creation; memory git-anchors can point at doomed
   branches; `notes.md` is shared-mutable *within* a slice; gitignored runtime
   state is invisible across worktrees.

Prior art: the spec-driver `dispatch` skill (orchestrator-sole-writer remit) and
the superpowers `using-git-worktrees` skill (detect-don't-fight via
`GIT_DIR != GIT_COMMON`, plus the native→git-fallback→in-place creation ladder and
regenerate-then-baseline setup). The Claude Code `WorktreeCreate`/`WorktreeRemove`
hooks fed by a `.worktreeinclude` allowlist supply native fork provisioning. None
solve doctrine's tier-aware concerns; all inform the mechanics (D5/D9).

> **AMENDED — FALSIFIED (SL-254, 2026-08-14).** The Claude Code
> `WorktreeCreate`/`WorktreeRemove` hooks informed a creation ladder that no
> longer exists. Doctrine forks every worktree itself — `scripts/spawn-confined.sh
> <harness>` is the sole spawn path — and **no harness-native creation or
> provisioning mechanism is on the shipped path** (see the D5 and D9 amendments
> below). The `using-git-worktrees` influences that survive are
> detect-don't-fight (`GIT_DIR != GIT_COMMON`, D1) and regenerate-then-baseline
> setup; the native→git-fallback→in-place **ladder** does not survive, and its
> in-place rung is abolished outright (`DEC-208`). The paragraph is retained as
> the record of what informed the original design.

## Decision

- **D1 — Policy-agnostic posture.** We will detect isolation
  (`GIT_DIR != GIT_COMMON`, with the submodule guard) rather than prescribe it.
  Defaults work out of the box trunk-based; one or two alternates
  (worktree-per-slice, worktree-per-batch) are cheap; deeper change is by forking
  editable skills/templates. No configuration Rube Goldberg.

- **D2 — Worker-sole-writer invariant (load-bearing).** Under parallel dispatch,
  worktree workers mutate **source code only**. *All* doctrine-mediated writes —
  authored tiers, runtime phase state, memory, id minting, commits — funnel
  through the **orchestrator on the coordination branch** (see D8: usually trunk
  in solo mode, a delta branch in team mode). Workers may **read** doctrine state
  freely — its authored and provisioned tiers are in the fork (the
  coordination/runtime tier is withheld, D9) — but **write** none of it. A worker returns a
  **structured report plus a source delta** — a branch/worktree diff or patch —
  not merely a description of changes; it never commits doctrine state. This
  single contract collapses the notes-collision,
  cross-worktree-progress-invisibility, global-tier-coordination, and
  memory-anchoring hazards into "the orchestrator writes on the coordination
  branch."
  - **D2a — Enforcement, in scope.** A CLI worker-mode guard hard-refuses every
    doctrine-mediated authored write (`memory record`, `slice/adr/spec new`, status
    transitions, doctrine-driven commits) — plus the `Orchestrator` verb class
    (`fork`/`import`/`land`/`gc`, which mutate git refs/dirs). Doctrine owns its CLI,
    so this is enforceable. It covers exactly the writes that mint ids and anchor
    memory.

    > **AMENDED — FALSIFIED (SL-254, 2026-08-14).** The whole "Signal"
    > subsection below is falsified — formula, exemption, fence and revert
    > rationale alike. The worker signal is now **`env DOCTRINE_WORKER`, and
    > nothing else** (`DEC-207`): `src/worktree/marker.rs` is an env-only module
    > whose own header records that *"the disk marker this module was named for
    > is gone"*, and `marker::describe_mode(env_set: bool)` is a **two-row** truth
    > table where SL-056's was eight rows over three inputs. Point by point:
    >
    > **(a) The formula.** `(is_linked_worktree && marker_present) OR env
    > DOCTRINE_WORKER` is retired. There is one leg. Neither linked-worktree
    > detection nor any on-disk artefact participates in identity, and worker
    > identity is a property of the **process**, not of a tree — set by the same
    > confinement argv that establishes the write floor, and dying with it.
    >
    > **(b) The primary/optimisation split.** "The disk marker is the
    > harness-agnostic primary, the env leg a codex/pi optimisation (claude has no
    > env channel)" is inverted. Claude is now spawned as a confined subprocess by
    > `scripts/spawn-confined.sh`, whose Linux prefix sets
    > `--setenv DOCTRINE_WORKER 1` in the same `bwrap` argv that establishes the
    > write floor. The env channel is universal; the disk medium is gone.
    >
    > **(c) The exemption.** The `marker --stamp-subagent` verb-identity exemption
    > is void — the verb no longer exists, and nothing writes identity to disk for
    > it to be exempted from.
    >
    > **(d) The fence against an unstamped worker.** Moot, not merely weakened.
    > `src/worktree/subagent.rs` was deleted whole, so there is no
    > `SubagentStart` stamp hook to fail, and the `IMP-052` orchestrator
    > post-spawn marker check has nothing to check. A worker's identity cannot be
    > *absent*: it is set by the same argv as the jail, or the spawn does not
    > happen at all (`jail.rs::REASON_NO_BWRAP`, `DEC-208` — a missing backend
    > fails closed, named on Linux and unnamed on macOS, with no unconfined
    > fallback). The R-5 import
    > belt does survive, but note precisely what it enforces: the two hard-coded
    > prefix floors `.doctrine/**` and `.claude/**`, **plus** the `--slice`-scoped
    > `undeclared-scope` leg (`Refusal::UndeclaredScope`, `import.rs:136-152`) —
    > three rejection grounds, not two. It does **not** enforce the
    > configurable `DispatchConfig::worker_forbidden_writes` tail — the deleted
    > `worker_commit` was that key's only production reader, and the gap is
    > carried to `SL-255`.
    >
    > **(e) The G2 revert.** The fail-closed-vs-open re-weigh turned entirely on
    > what "marker absent" should be taken to mean. With identity carried in the
    > process environment there is no absence to interpret: a solo `/execute`
    > simply does not have `DOCTRINE_WORKER` set. The re-weigh's *conclusion* —
    > that **D6a's "mode, not location, decides"** must not be contradicted at the
    > write seam — is preserved intact and made exact; its *reasoning*, resting on
    > `P(SubagentStart hook failure)` and jail-bounded harm, is about machinery
    > that no longer exists.
    >
    > **(f) The SL-181 note.** "A worker — stamped or not — cannot mutate shared
    > refs" is right in conclusion and obsolete in framing. There is no stamp, and
    > no "where a backend runs" qualifier: a kernel write floor now covers **every**
    > harness — `--ro-bind / /` over the whole filesystem on Linux, an SBPL
    > `(deny file-write*)` floor on macOS — because a backend always runs or spawn
    > refuses.
    >
    > The historical analysis below is retained for the record; it no longer
    > describes the shipped posture.

    - **Signal (amended SL-056 G2; re-amended SL-056 PHASE-05): disk-marker-primary,
      positive worker signal.** The worker signal is **`worker_mode =
      (is_linked_worktree && marker_present) OR env DOCTRINE_WORKER`**; the disk marker
      is the harness-agnostic primary, the env leg a codex/pi optimisation (claude has
      no env channel). The marker is the **positive** worker identity: **marker present
      in a linked worktree ⇒ refuse** (a confirmed worker); **marker absent ⇒ NOT
      worker-mode** — a solo `/execute` or the orchestrator may write (D6a: *mode, not
      location, decides*). The legit orchestrator runs at the coordination root
      (`!is_linked_worktree`); a solo agent in a worktree writes its own doctrine state
      directly. The sole marker-minting verb (`marker --stamp-subagent`) is exempt **by
      verb identity** — it writes the marker into its own linked-worktree `cwd`.
      - **The fence against an unstamped claude worker is the funnel + the jail, not a
        fail-closed CLI floor.** A worker whose SubagentStart stamp hook failed runs
        `marker_present=false` and is therefore *not* refused at the write seam. Its
        blast radius is bounded elsewhere: the **R-5 import belt** rejects any
        `.doctrine/` path in the `B..S` delta (an unstamped worker writing into its own
        fork is caught at import, report+halt); the **env worker-on-main catch**; the
        **pre-distilled prompt**; and — decisively for the build repo — the
        **bubblewrap jail with no push access** (ADR-008), which caps the worst case at
        *lost unpushed progress*. Stamp-failure is kept **non-silent** by an
        **orchestrator post-spawn marker check that aborts an unstamped fork** (IMP-052;
        the orchestrator *can* abort where the read-only SubagentStart hook cannot —
        ADR-011 D6), not by refusing the write.
      - **Why the G2 fail-closed clause was reverted (SL-056 PHASE-05, owner-locked
        VH).** G2 made marker-absent fail-CLOSED. That (a) contradicted **D6a**
        verbatim — D6a lets a solo writer write in a worktree; fail-closed refused it —
        and (b) killed solo-`/execute`-in-a-worktree direct-writing. Re-weighed against
        the *actual* risk — `P(SubagentStart hook failure) ≈ 0` (the hook blocks; a
        miss needs a crash / broken binary / disk error mid-stamp) **×** *jail-bounded*
        harm (no push ⇒ worst case is lost local work) — the floor bought negligible
        security over the funnel while costing a real workflow and a live D2a/D6a
        contradiction. The marker is the positive signal; the funnel + jail + IMP-052
        are the fence. (Codex-reviewed adversarially; see `worker-mode-floor-decision.md`
        §6.)
      - **D2a note (SL-181, 2026-07-03) — the enforcement floor has landed.** The
        SL-056 re-weigh concluded "the marker is the positive signal; the funnel +
        jail + IMP-052 are the fence" *because no fail-closed CLI floor was
        justified*. Confinement now supplies the floor at the OS layer, not the CLI:
        SL-182 (Linux/bwrap, **ro shared `.git`**), SL-183 (macOS Seatbelt), SL-185
        (subprocess parity). Where a backend runs, a worker — stamped or not —
        cannot mutate shared refs. The cooperative marker was never the fence; the
        OS floor now is.

    > **AMENDED — RESTATED (SL-254, 2026-08-14).** The conclusion holds and is
    > now unconditional; its stated basis is falsified. The orchestrator may
    > indeed run in a *linked* coordination worktree, and location still does not
    > decide — but its write permission rests on **`DOCTRINE_WORKER` being unset
    > in its own process environment**, not on marker-absence (`DEC-207`): there
    > is no marker. Read every "marker-absence" below as "env-absence"; the sole
    > refuse condition is `env DOCTRINE_WORKER` set, with the
    > `is_linked_worktree && marker_present` conjunct gone entirely. The
    > consequence this amendment names is thereby **promoted, not retired**: env
    > leakage is no longer "a live hazard on codex/pi where the env leg is
    > active" but the *only* identity channel on every harness, so
    > `DOCTRINE_WORKER` leaking into the coordination tree's environment would
    > false-flag worker-mode and refuse the orchestrator's own writes everywhere.
    > The historical body is retained for the record.

    - **D2a amendment (SL-064) — the orchestrator may run in a *linked*
      coordination worktree; permission rests on marker-absence, not on being at
      the root.** SL-064 moves the orchestrator off the session root into a
      dedicated coordination worktree (D8 amendment; the topology is ADR-012's).
      That tree *is* `git worktree add` ⇒ `is_linked_worktree=true`, so the prior
      prose pinning the legit orchestrator at the coordination **root**
      (`!is_linked_worktree`) is **retired**: the orchestrator's write permission
      rests on **marker-absence** (the positive-worker signal — `is_linked_worktree
      && marker_present` is the only refuse condition), not on its location. The
      `worker_mode` formula is unchanged; only the "runs at the root" assumption is.
      Consequence: **`env DOCTRINE_WORKER` must NOT leak** into the coordination
      tree's environment — the env leg would false-flag worker-mode and refuse the
      orchestrator's own writes (a live hazard on codex/pi where the env leg is
      active). SL-064 design §2 carries the verify-item.

  > **AMENDED — FALSIFIED (SL-254, 2026-08-14).** The gap is closed, and the
  > observation it rested on no longer describes anything shipped. Raw-tree
  > confinement is not "deferred to sandbox/harness work": it is enforced at the
  > kernel by the `bwrap` (Linux) / `sandbox-exec` (macOS) jail that wraps every
  > worker exec on the sole spawn path — `--ro-bind / /` on Linux, an SBPL
  > `(deny file-write*)` floor on macOS — leaving a writable set of the fork's
  > worktree **plus the harness config dir** (plus scratch and device sinks), so a
  > hand-edited file or a bare `git commit` outside that set is
  > refused by the OS, not by cooperation. The parenthetical observation
  > ("Claude Code creates the worktree but lets the agent write to main") is
  > doubly stale: the harness no longer creates the worktree, and there is no
  > in-session claude arm for it to create one for. Where confinement is
  > unavailable the answer is not a weaker mode but a refusal at spawn — **named**
  > on Linux (`jail.rs::REASON_NO_BWRAP`), unnamed on macOS, which has no
  > backend-presence probe (`DEC-208`). The historical body is retained for
  > the record.

  - **D2b — Residual gap, deferred.** Raw-tree confinement (a worker
    hand-editing a file or running bare `git commit`) is not CLI-stoppable, and
    the harness does **not** confine workers to their worktree (observed: Claude
    Code creates the worktree but lets the agent write to main). This is a known
    live risk, deferred to sandbox/harness work (see ADR-008), not papered over.

    > **AMENDED — FALSIFIED (SL-254, 2026-08-14).** This note's hazard model is
    > dissolved rather than mitigated. It reasons about an **unstamped worker**
    > arising from "stamp-hook failure / matcher drift" being mistaken for the
    > tree that owns the funnel. With the disk marker gone (`DEC-207`) there is no
    > stamp, no `SubagentStart` hook (`src/worktree/subagent.rs` deleted whole),
    > no matcher to drift, and therefore no unstamped-worker case to broaden any
    > blast radius. Coordination-tree identity is no longer *inferred from
    > absence*: worker identity is positively asserted by `DOCTRINE_WORKER` in the
    > worker's own process environment, set by the same confinement argv that
    > establishes its write floor, and a process without it is not a worker.
    >
    > **The dangling `IMP-065` forward reference is resolved.** `IMP-065` — the
    > "real positive-marker close" this note defers to — was **closed obsolete on
    > 2026-07-02 via `REV-018`**. It was **superseded by shipped confinement**
    > (SL-182/183/185, made uniform across every harness by SL-254), *not*
    > discharged by the positive coordination marker it was waiting for: that
    > marker was never built, and under `DEC-207` never will be. The OQ-D
    > obligations are answered accordingly — **(i)** Orchestrator-verb invocation
    > is restricted by the kernel write floor plus the `DOCTRINE_WORKER` guard,
    > rather than held "until a positive marker lands"; **(ii)** the
    > impersonation-test obligation stands on its own merits, now against the env
    > signal rather than the marker. The historical body is retained for the
    > record.

    - **D2b note (SL-064) — marker-absence is a *transitional assumption*, not a
      positive coordination identity; the fence is defence-in-depth, not a proof.**
      SL-064 ships markerless coordination-tree creation (D9 amendment): the
      coordination tree is identified by marker *absence*. This **broadens the
      blast radius** of the confessed ADR-011 D6/M2 false-clear — an unstamped
      worker (stamp-hook failure / matcher drift) now looks not merely like a solo
      `/execute` but like the tree that owns the funnel and the full Orchestrator
      verb class (`fork`/`import`/`gc`/sync). SL-064 does **not** claim to close
      that gap by absence. The D2b fence (R-5 import belt, IMP-052 post-spawn
      check, env-worker-on-main catch, bwrap-no-push) is **defence-in-depth, NOT a
      proof** that gc/sync impersonation is covered (RV-025 B3). The OQ-D
      plan-gate therefore stands as binding implementation obligations on the new
      sync surface: **(i)** Orchestrator-verb invocation (`fork`/`import`/`gc`/sync)
      is restricted to the trusted orchestrator path until a positive marker lands;
      **(ii)** impersonation tests for the new sync verbs are **mandatory, not
      optional**. The real positive-marker close (coordination-tree identity by a
      *present* orchestrator signal, not by absence) is **IMP-065** — out of SL-064
      scope; it touches the owner-locked D2a positive-signal model.

    > **AMENDED — RESTATED (SL-254, 2026-08-14).** The conclusion stands; three
    > of its qualifiers are void. "A worker — stamped or not" no longer means
    > anything: there is no stamp (`DEC-207`). "On confined arms" is redundant —
    > there is **one** arm and it is always confined; every harness, claude
    > included, is spawned by `scripts/spawn-confined.sh <harness>` inside the
    > jail. The "no-backend" degenerate case is not a fail-closed hook after which
    > "the worker executes nothing": it is a refusal before spawn — **named** on
    > Linux (`jail.rs::REASON_NO_BWRAP`), unnamed on macOS for want of a
    > backend-presence probe (`DEC-208`) — the worker is never created. And
    > "the unconfined arm" whose anti-accident guard was retired (RV-199 F-1) no
    > longer exists at all. The historical body is retained for the record.

    - **D2b note (SL-181, 2026-07-03) — the residual gap is enforcement-closed on
      confined arms.** Raw-tree confinement, "deferred to sandbox/harness work"
      here, has **shipped** (SL-182/183/185). The marker-absence transitional
      assumption's blast radius is now bounded to the degenerate cases, each
      self-limiting: no-backend → SL-182 hook fail-closed (worker executes nothing);
      standalone clone → contained to a disposable object store. The SL-181
      anti-accident guard for the unconfined arm was retired (RV-199 F-1); it added
      nothing the floor did not already give (SL-181 design §1).

- **D3 — Minting is a trunk-side act.** Ids are allocated against the configured
  **trunk ref** (the id baseline; auto-detected `origin/HEAD` then `main`/
  `master`, overridable) *before* a worktree forks. Cross-branch offline
  collisions are caught by detect+reseat in `validate` plus a reseat verb.

- **D4 — Tier merge-safety invariant.** No shared-mutable authored file across
  the unit of isolation; runtime state stays gitignored/per-worktree; the
  `phases` symlink is relative. This invariant is *defended*, not assumed — any
  future central index reintroduces conflict.

> **AMENDED — FALSIFIED IN PART (SL-254, 2026-08-14).** The preference clause is
> dead: doctrine does **not** prefer the harness's native mechanism, and does not
> reinvent `git worktree` "only at the fallback rung". There is no native rung and
> no ladder — `scripts/spawn-confined.sh <harness>` forks the worktree explicitly
> on the sole spawn path, for every harness including claude, and wraps the
> worker's exec in a kernel-level jail. What survives unchanged is the rest of
> D5: worktree creation remains in scope for skills (mandatory for `/dispatch`,
> optional for `/execute`), and both guards — **commit-before-spawn** and the
> **branch-point check** — stand. See the D9 amendment for the ladder itself.

- **D5 — Worktree creation is in-scope for skills.** `/dispatch` (mandatory
  isolation) and `/execute` (optional). Doctrine *prefers* the harness's native
  mechanism and reinvents `git worktree` only at the fallback rung when no native
  tool exists (the creation ladder, D9 — battle-tested by superpowers
  `using-git-worktrees`). It adds two guards: **commit-before-spawn** (a fork sees
  only committed HEAD) and the **branch-point check** (HEAD pre/post-spawn mismatch
  → re-dispatch).

- **D6 — Orchestrator pre-distills worker context.** Workers receive a
  self-contained prompt (policy digest, design excerpts, pre-fetched memories,
  task spec, mandatory verification command). Workers do not read boot/governance
  or run `/boot`, so "governance frozen at fork" and "hooks-not-inherited" are
  moot for workers.

- **D6a — Worker vs solo `/execute`.** A *worker* is a thin code-mutator under an
  orchestrator: worker-mode is ON (D2a), it returns a source delta, and all its
  doctrine-mediated writes funnel. Solo `/execute` is a *full agent and its own
  orchestrator*: worker-mode is OFF, and it writes doctrine state directly (its
  own slice status, notes, phase, memory). The worktree is merely *where* each
  runs; the **mode, not the location, decides who may write.**

- **D7 — Funnel discipline.** The orchestrator persists incrementally (per
  batch), in strict order **import delta → verify → commit → record knowledge**:
  it imports each worker's source delta, verifies it, commits from the
  coordination branch, then records derived knowledge (memory, AC evidence,
  notes). The coordination branch is the durable store; orchestrator context is
  disposable; crash/overflow recovery is rebuild-from-coordination-branch plus
  `git worktree list`. Knowledge always trails confirmed code.

- **D8 — Coordination branch and placement.** The orchestrator runs on a
  **coordination branch** — usually trunk in solo mode, a delta branch in team
  mode. Ids are still minted against the configured **trunk ref** (the id
  baseline, D3) before a worktree forks; globals "land on main at merge"
  (convention, not mechanism). A memory anchored to a coordination-branch commit
  keeps its sha through a *non-squash* merge (see the squash caveat in
  Consequences).

  - **D8 amendment (SL-064) — the coordination branch runs in its own dedicated
    worktree.** The original D8 placement ("usually trunk in solo mode, a delta
    branch in team mode") let the coordination branch *be* the session working
    tree — exposing the funnel to foreign writers, concurrent-dispatch contention,
    and unreviewed code touching `main` (SL-060 retrospective). SL-064 retires that:
    the orchestrator always runs on a dedicated `dispatch/<slice>` coordination
    worktree, provisioned per run (D9 amendment). Id-minting is unchanged — D3
    still resolves the configured **trunk ref** (a ref in the shared common dir,
    resolvable from any worktree), not coordination HEAD. **The branch topology
    (the `dispatch/<slice>` / `phase/<slice>-NN` / `review/<slice>` / `edge` roles),
    the two-stage downstream projection, the routing policy, and the projection
    recovery contract are owned by ADR-012 — not duplicated here.** This amendment
    fixes only *placement*: where the orchestrator runs.

> **AMENDED — FALSIFIED IN PART (SL-254, 2026-08-14).** The **ladder is gone at
> both ends**; the provisioning axes and the baseline-verify gate survive and are
> strengthened.
>
> - **The native rung is gone.** There is no "prefer the harness's native
>   worktree tool (Claude Code: the `WorktreeCreate`/`WorktreeRemove` hooks)"
>   step: those hooks are not on the shipped path, `src/worktree/subagent.rs` and
>   `src/worktree/pretooluse.rs` were deleted whole, and
>   `scripts/spawn-confined.sh <harness>` forks explicitly for every harness.
>   Likewise the allowlist is no longer "fed through the `WorktreeCreate` hook" by
>   Claude Code — nothing harness-side feeds it.
> - **The degraded rung is abolished** (`DEC-208`). "Else, on sandbox denial,
>   degrade to work-in-place, solo mode, no funnel" no longer exists: a missing
>   `bwrap` on Linux is a **named refusal and spawn fails closed**
>   (`jail.rs::REASON_NO_BWRAP`); on macOS the failure is closed but **unnamed**,
>   since no `sandbox-exec` presence probe exists. There is no unconfined fallback. Note this does
>   not touch D1/D6a's blessed solo trunk path as a *chosen* mode of work — what
>   is abolished is silently *degrading into* it when confinement is unavailable.
> - **What survives.** Detect-existing-isolation (D1); the two provisioning axes
>   (regenerate derivable prerequisites, copy only the irreducible gitignored
>   ones via the project-owned allowlist); the **invariant** that the allowlist
>   excludes the coordination/runtime tier, which is what makes worker-sole-writer
>   free and is now doubled by the jail's write floor; D6's pre-distilled prompt
>   substituting for the withheld coordination state; and the orchestrator's
>   pre-dispatch **baseline-verify**, which under one subprocess arm now holds
>   uniformly for *every* harness (see the SL-056 G2 amendment below, whose
>   claude-specific concession is withdrawn).
>
> The historical body is retained for the record.

- **D9 — Fork provisioning and the creation ladder.** Worktree creation follows a
  **preference ladder**: detect existing isolation (D1); else prefer the harness's
  native worktree tool (Claude Code: the `WorktreeCreate`/`WorktreeRemove` hooks);
  else fall back to `git worktree add`; else, on sandbox denial, degrade to
  **work-in-place, solo mode, no funnel** (the already-blessed D1/D6a trunk path).
  A fresh fork carries only committed files, so before dispatch the orchestrator
  **provisions** it along two axes: **regenerate** derivable prerequisites from
  committed sources (`cargo build`, `npm install`) and **copy** only the
  *irreducible* gitignored prerequisites (secrets, local config) via a
  **project-owned allowlist** (`.worktreeinclude`, gitignore syntax — D1
  policy-agnostic; Claude Code feeds it through the `WorktreeCreate` hook).
  **Invariant (guards D2):** the allowlist **excludes the coordination/runtime
  tier** (`.doctrine/state/`, the `phases` symlink, `handover.md`, memory caches)
  by construction — their *absence* in the fork is what makes worker-sole-writer
  free; a copied phase sheet would be invisibly mutable. D6's pre-distilled prompt
  substitutes for the withheld coordination state; provisioning substitutes for the
  absent execution env. Finally the orchestrator **baseline-verifies** the fork
  builds and tests green *before* handing the worker its task — an unbuildable fork
  is fixed in provisioning, never dispatched. (Doctrine's jail instantiates the
  regenerate axis with a per-worktree `CARGO_TARGET_DIR` — ADR-008 D-B1.)

  > **AMENDED — FALSIFIED (SL-254, 2026-08-14).** This amendment is falsified
  > whole: there is no claude *rung*, because there is no ladder and no
  > claude-specific path. Claude no longer "performs its own default worktree
  > creation" at `Agent isolation:worktree` — the in-session claude arm is
  > retired, and claude is spawned as a confined subprocess by
  > `scripts/spawn-confined.sh <harness>` like every other harness. Consequently:
  > the `WorktreeCreate` payload's missing `agent_type` / `worktree_path` / base
  > fields no longer block anything and no longer defer anything; the
  > **matcher-scoped, sync-blocking `SubagentStart` hook** that provisioned and
  > stamped no longer exists (`src/worktree/subagent.rs` deleted whole), and with
  > it the marker it stamped (`DEC-207`); and **the accepted weaker
  > baseline-verify class is withdrawn**. The orchestrator now owns the fork for
  > claude too, so there *is* a pre-spawn orchestrator moment: the pre-dispatch
  > baseline-verify guarantee holds for every harness, no claude worker starts on
  > an unverified fork, and the deliberate altitude concession named here — with
  > its wasted-worker-run cost multiplied by batch width — is spent. The
  > historical body is retained for the record.

  - **D9 amendment (SL-056 G2) — the claude rung is SubagentStart-stamp, and its
    baseline-verify is a weaker class.** The O3 spike (SL-056 PHASE-02) found the
    deployed Claude Code `WorktreeCreate` payload carries no `agent_type`,
    `worktree_path`, or base — so the create-replacing `create-fork` hook (fail-closed:
    provision+stamp *inside* creation) is **not buildable**, and is deferred until the
    payload grows those fields. The claude rung is therefore: **Claude performs its own
    default worktree creation** at `Agent isolation:worktree`; a **matcher-scoped,
    sync-blocking `SubagentStart` hook** (`matcher: dispatch-worker`) then provisions
    (the `.worktreeinclude` axis above) and stamps the marker into the payload `cwd`.
    Because SubagentStart blocks the worker until it exits, provision+stamp complete
    before the worker's first command **on hook success**.
    - **The pre-dispatch baseline-verify guarantee does NOT hold for claude (accepted
      weaker class).** Claude creates the worktree *at spawn*; there is no pre-spawn
      orchestrator moment, and SubagentStart is a read-only event that cannot abort the
      worker on a failed baseline. So a claude worker **may start on an unverified
      fork**; an unbuildable fork is caught **late, at the funnel's `import → verify`
      step** (cost: a wasted worker run — multiplied by batch width under the
      one-landing-per-base regime, SL-056 §7c). This is a deliberate altitude
      concession, named here, not papered over; codex/pi keep the pre-dispatch gate
      (the orchestrator owns `fork`, so it verifies before handoff).

  > **AMENDED — RESTATED (SL-254, 2026-08-14).** The substance survives; its
  > defining difference does not. "The worker disk-marker is **NOT** stamped" no
  > longer distinguishes the coordination tree from anything, because no marker is
  > stamped anywhere (`DEC-207`) — there is no marker. The coordination tree is
  > the orchestrator by virtue of running **without `DOCTRINE_WORKER` in its
  > environment**, which is the whole of the identity model now; nothing needs to
  > be withheld from it at creation for the identity guard to permit its writes.
  > "Markerless" is therefore no longer a *variant* of creation — it is simply
  > what creation is. What still stands, and is load-bearing: the coordination
  > tree is created by the same fork + provision path; both provisioning axes run
  > **minus the coordination/runtime tier**, which the orchestrator **regenerates**
  > (runtime phase sheets from the committed `plan.toml`) rather than copies,
  > because a copied phase sheet would be invisibly mutable (the D9 invariant);
  > and concurrent same-slice creation is refused at the native `git worktree add`
  > collision. The historical body is retained for the record.

  - **D9 amendment (SL-064) — markerless coordination-tree creation.** The
    coordination worktree (D8 amendment) is created by the **same fork +
    provision ladder**, with one difference: the worker disk-marker is **NOT
    stamped**. The coordination tree *is* the orchestrator (worker-mode OFF, must
    write); a stamped marker would make the identity guard refuse its writes
    (D2a). Marker absence ⇒ D6a "mode, not location, decides" permits the write
    (D2a amendment). Provisioning runs **both** D9 axes (regenerate the verify
    build; copy the irreducible gitignored allowlist) **minus the
    coordination/runtime tier**, which the orchestrator is the sole writer of and
    therefore **regenerates** (runtime phase sheets from the committed `plan.toml`)
    rather than copies — a copied phase sheet would be invisibly mutable (the D9
    invariant). Concurrent **same-slice** creation is refused at the native
    `git worktree add` collision; concurrent *different-slice* dispatch never
    shares the branch. This markerless variant is the one load-bearing new
    creation surface SL-064 adds.

- **D10 — Candidate interaction worktrees (SL-068 amendment).** Dispatch may
  materialise doctrine-owned candidate branches/worktrees for post-execution
  audit review, repair, and experiments. A candidate worktree is **not** a worker
  worktree: it is unstamped — *amended SL-254: there is no stamp to lack
  (`DEC-207`); it is simply a tree whose processes run without `DOCTRINE_WORKER`
  set, which is the whole of the identity model now* — ordinary Git state for a
  human/operator to inspect
  and commit against. Candidate commands remain doctrine-mediated writes, so they
  are refused under worker-mode and belong to the trusted orchestrator/operator
  path, not to worker forks. Raw evidence refs (`dispatch/*`, `review/*`, and
  `phase/*`) stay non-interaction surfaces: write-ish candidate/admit/close
  commands must refuse when the current worktree is checked out on one of those
  refs unless the command is explicitly operating by ref from a safe context.
  Read-only commands may warn, but no command should write into a worktree
  currently checked out on a raw evidence ref. This amendment legitimises safe
  candidate interaction without weakening D2/D2a: workers still return deltas
  through the funnel, and exact evidence refs remain protected.

The single-tree, turn-based coordination regime (design↔review, audit, consult)
is the orthogonal primitive; it is specified in ADR-007. The shared boundary rule
both ADRs enforce: **the unit of isolation determines the primitive — never a
live shared ledger across worktrees.**

## Consequences

### Positive

- Collapses four concurrency hazards at once via the funnel (D2).
- The solo trunk-based path is untouched (no worktree required); teams get
  isolation without doctrine dictating branch flow (D1).
- The funnel enables memory dedup — the orchestrator sees all worker reports
  before recording.
- Because all durable state lives on the coordination branch, the orchestrator's
  context is disposable: crash ≡ handover ≡ resume-from-coordination-branch (D7).
- Candidate worktrees give audit reviewers and operators an ordinary branch
  affordance without turning exact dispatch evidence refs into mutable repair
  branches (D10).

### Negative

- The worker-sole-writer invariant has **no harness enforcement** (D2b); until
  raw-tree confinement is solved it rests on the CLI guard (D2a) plus the prompt
  contract.

  > **AMENDED — INVERTED (SL-254, 2026-08-14).** This is the sharpest inversion
  > in the document. The invariant no longer rests on a cooperative CLI guard
  > plus a prompt contract: **enforcement is kernel-level on every harness.**
  > Every worker runs inside a `bwrap` (Linux) / `sandbox-exec` (macOS) jail
  > whose write floor (`--ro-bind / /` on Linux; an SBPL `(deny file-write*)` on
  > macOS) leaves a writable set of the fork's worktree **plus the harness config
  > dir** — so a worker cannot write doctrine
  > state, cannot mutate shared refs, and cannot commit at all (the
  > `worker_commit` MCP tool is deleted, `DEC-204`; it hands back an uncommitted
  > working tree the orchestrator imports). Raw-tree confinement, named here as
  > unsolved, **is solved**. Two riders keep this honest. First, it is not
  > *harness* enforcement even now — it is OS enforcement applied uniformly by
  > doctrine's own spawn path (`scripts/spawn-confined.sh <harness>`),
  > independent of what any harness offers; where the backend is unavailable
  > spawn **fails closed** — a *named* refusal on Linux
  > (`jail.rs::REASON_NO_BWRAP`) and an unnamed exec failure on macOS, which has
  > no `sandbox-exec` presence probe (`DEC-208`) — so there is no unenforced mode
  > to fall into. Second, the CLI guard (`src/commands/guard.rs::worker_guard`,
  > keyed on `DOCTRINE_WORKER == "1"`) and the pre-distilled prompt both remain —
  > but as belts *inside* a hard floor, not as the floor. **Third rider, added on
  > correction:** the floor is not absolute even inside the jail — the harness
  > config dir (`~/.claude` / `~/.pi`) is bound read-write, so a confined claude
  > worker can write `settings.json`, hooks and agent definitions belonging to the
  > orchestrator's own harness. The residual is narrower and
  > named elsewhere: the configurable
  > `DispatchConfig::worker_forbidden_writes` tail is currently enforced by
  > nothing, since the import belt only ever enforced its two hard-coded prefix
  > floors (`.doctrine/**`, `.claude/**`) plus the `--slice`-scoped
  > `undeclared-scope` leg, and the deleted `worker_commit` was the config
  > key's only production reader — carried to `SL-255`.

- Genuine code coupling across parallel batches can still merge-conflict; policy
  is report, never auto-resolve.
- **Solo `/execute` inside a worktree** writes memory directly (no funnel to
  reroute it, D6a); a memory anchored to that branch's sha is **orphaned by a
  squash-merge** — the content survives, but SL-008 staleness fires. Plain
  (non-squash) merges are unaffected.
- A new CLI worker-mode and trunk-ref/reseat machinery are required.

### Neutral

- Project-local concerns (bubblewrap jail, `CARGO_TARGET_DIR`,
  install-vs-installed templates, cargo bin races) are out of scope and belong to
  ADR-008.
- Two distinct worktree-creation paths exist (dispatch via harness isolation;
  solo `/execute` via native tools) and are named, not unified.

  > **AMENDED — FALSIFIED (SL-254, 2026-08-14).** Neither path is described
  > correctly any more. Dispatch creation is **not** "via harness isolation":
  > doctrine forks the worktree itself on the sole spawn path
  > (`scripts/spawn-confined.sh <harness>`), for every harness, and no
  > harness-native creation mechanism is used. The two paths are still not
  > unified, but the dispatch half is now doctrine-owned and confinement-wrapped
  > end to end.

## Verification

- `validate` flags duplicate ids (detect half of D3); a reseat verb resolves them.
- The CLI worker-mode guard (D2a) is covered by tests asserting that
  doctrine-mediated writes refuse under `DOCTRINE_WORKER=1`.
- The branch-point check (D5) is an orchestrator-side assertion: HEAD captured
  pre-spawn must equal the worktree HEAD post-spawn.
- The tier merge-safety invariant (D4) is checked by the absence of any
  shared-mutable authored file and by the `phases` symlink being relative.
- `doctrine memory record` detects worktree context (`GIT_DIR != GIT_COMMON`) and
  warns when recording on a branch (squash-orphan risk), nudging record-on-trunk.
- Fork provisioning (D9): the project allowlist (`.worktreeinclude`) is asserted to
  **exclude** the coordination/runtime globs (`.doctrine/state/`, `phases`,
  `handover.md`, memory caches); and a baseline build+test must pass in the fork
  before dispatch (an unbuildable fork blocks dispatch, not the worker).
- Candidate/write guard (D10): candidate/admit/close writes refuse under
  worker-mode and from worktrees checked out on `dispatch/*`, `review/*`, or
  `phase/*`; status/read-only surfaces identify raw refs as evidence and point to
  the candidate workflow.

## Open

- **Anchor stability (deferred seam).** If squash-orphaning of solo-in-worktree
  memory (Negative) proves common, move the memory anchor from the volatile
  branch sha to a stable ref (merge-base or content-hash). Touches SL-007/008;
  deferred until evidence warrants.
- **Convention (unenforced for solo):** record durable memory on the coordination
  branch / trunk, not in a transient worktree branch — the same rule the dispatch
  funnel enforces for workers.

## References

- ADR-003 — canonical change loop (slice-first, observe, reconcile, close).
- ADR-004 — relations stored outbound-only; reciprocity derived.
- ADR-007 — adversarial review kind / turn-based ledger (the orthogonal
  coordination primitive; shared isolation-boundary rule).
- ADR-008 — project-local jail / cargo / install dogfooding concerns.
- ADR-012 — dispatch integration topology (the `dispatch/<slice>` /
  `phase/<slice>-NN` / `review/<slice>` / `candidate/<slice>/<label>` / `edge`
  roles, two-stage projection, candidate admission, routing policy, projection
  recovery contract, D1 tightening). The owner of the topology the SL-064 and
  SL-068 amendments above place the orchestrator and interaction worktrees onto.
- SL-064 — the implementing slice for the D2a/D2b/D8/D9 amendments (coordination
  worktree placement, markerless creation, the OQ-D fence). IMP-065 — positive
  coordination marker, **retired** (SL-181); OQ-D real close = **shipped
  confinement** (SL-182/183/185). *(Amended SL-254: IMP-065's disposition is
  precisely — **closed obsolete on 2026-07-02 via `REV-018`**, superseded by
  shipped confinement, **not** discharged by the positive marker it was waiting
  for. Under `DEC-207` no coordination marker will ever be built: identity is
  `DOCTRINE_WORKER` and nothing else, and confinement is uniform across every
  harness. This resolves the forward reference in the D2b SL-064 note above.)*
- SL-068 — the implementing slice for D10 and ADR-012's candidate/admission
  topology amendment.
- SL-009 — slice status rollup divergence (the hand-edit-status gap this funnel
  avoids for runtime progress).
- Prior art: spec-driver `dispatch` skill; superpowers `using-git-worktrees` (the
  creation ladder + regenerate/baseline setup, D5/D9); Claude Code
  `WorktreeCreate`/`WorktreeRemove` hooks + `.worktreeinclude` allowlist for native
  fork provisioning (mattbrailsford.dev GitHub discussion #54). *(Amended
  SL-254: these remain the historical influences, but the Claude Code hooks are
  **not on the shipped path** and the creation ladder they informed no longer
  exists — doctrine forks every worktree itself via
  `scripts/spawn-confined.sh <harness>`. See the D5 and D9 amendments.)*
