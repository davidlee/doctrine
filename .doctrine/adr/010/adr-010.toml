schema = "doctrine.adr"
version = 1

id = 10
slug = "relation-modelling-unify-contract-and-write-seam-keep-storage-bespoke"
title = "Relation modelling: unify the contract and write seam, keep storage bespoke"
status = "proposed"
created = "2026-06-12"
updated = "2026-06-12"

[relationships]
# Reserved. Empty in v1 — supersession (F1) and tagging are not wired yet.
# Authored-but-inert: present so the shape is stable when those land.
supersedes    = []   # ADR ids this one replaces (F1 `adr supersede`)
superseded_by = []   # set on the old ADR by F1; reverse link is derived
related       = ["ADR-004"]   # non-superseding cross-references
tags          = []   # free-form classification
