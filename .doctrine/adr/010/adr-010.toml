schema = "doctrine.adr"
version = 1

id = 10
slug = "relation-modelling-unify-contract-and-write-seam-keep-storage-bespoke"
title = "Relation modelling: unify the contract and write seam, keep storage bespoke"
status = "accepted"
created = "2026-06-12"
updated = "2026-06-13"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-010 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-010 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived


[[relation]]
label = "related"
target = "ADR-004"
