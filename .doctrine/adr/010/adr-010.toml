schema = "doctrine.adr"
version = 1

id = 10
slug = "relation-modelling-unify-contract-and-write-seam-keep-storage-bespoke"
title = "Relation modelling: unify the contract and write seam, keep storage bespoke"
status = "accepted"
created = "2026-06-12"
updated = "2026-06-13"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-010 supersedes|related <target>`.
# `doctrine inspect ADR-010` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived
tags          = []   # free-form classification


[[relation]]
label = "related"
target = "ADR-004"
