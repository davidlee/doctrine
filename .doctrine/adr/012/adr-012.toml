schema = "doctrine.adr"
version = 1

id = 12
slug = "dispatch-integration-topology"
title = "Dispatch integration topology: isolated coordination worktree, class-routed projection, preserved code branches"
status = "accepted"
created = "2026-06-14"
updated = "2026-06-15"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048) — author `supersedes` via the transactional `doctrine supersede` verb.
# `doctrine inspect ADR-012` renders them. `superseded_by` and `tags` stay typed.
supersedes = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

[[relation]]
label = "supersedes"
target = "ADR-004"
