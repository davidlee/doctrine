schema = "doctrine.adr"
version = 1

id = 12
slug = "dispatch-integration-topology"
title = "Dispatch integration topology: isolated coordination worktree, class-routed projection, preserved code branches"
status = "accepted"
created = "2026-06-14"
updated = "2026-07-03"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-012 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-012 related <target>`.
supersedes    = []
superseded_by = []
