schema = "doctrine.adr"
version = 1

id = 13
slug = "revision-as-a-first-class-change-axis-kind-governance-dependency-routes-through-a-revision"
title = "Revision as a first-class change-axis kind; governance dependency routes through a Revision"
status = "accepted"
created = "2026-06-14"
updated = "2026-06-14"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-013 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-013 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived


# `related` cross-references are uniform `[[relation]]` rows now (SL-048 "the cut"),
# never a typed key here. Author one with `doctrine link ADR-NNN related <target>`;
# `doctrine inspect ADR-NNN` renders it. The supersession pair + tags stay typed.
