schema = "doctrine.adr"
version = 1

id = 8
slug = "jail-build-isolation-worker-confinement"
title = "Project-local jail build isolation and worker confinement for parallel dispatch"
status = "accepted"
created = "2026-06-08"
updated = "2026-06-13"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-008 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-008 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

