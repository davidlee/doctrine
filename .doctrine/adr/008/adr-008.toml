schema = "doctrine.adr"
version = 1

id = 8
slug = "jail-build-isolation-worker-confinement"
title = "Project-local jail build isolation and worker confinement for parallel dispatch"
status = "accepted"
created = "2026-06-08"
updated = "2026-06-13"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-008 supersedes|related <target>`.
# `doctrine inspect ADR-008` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived

