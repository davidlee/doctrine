schema = "doctrine.adr"
version = 1

id = 19
slug = "embedding-publication-and-projection-are-independent-asset-policies"
title = "Embedding, publication, and projection are independent asset policies"
status = "accepted"
created = "2026-07-18"
updated = "2026-07-18"
tags = ["install", "projection", "publication", "embedding", "asset-policy", "minimal-projection", "semantic-ownership"]

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-NNN <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-NNN related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

[[relation]]
label = "related"
target = "ADR-005"
