schema = "doctrine.adr"
version = 1

id = 16
slug = "relation-intent-as-a-closed-role-dimension"
title = "Relation intent as a closed role dimension"
status = "accepted"
created = "2026-06-24"
updated = "2026-06-24"
tags = []

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-016 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-016 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

[[relation]]
label = "related"
target = "ADR-010"

[[relation]]
label = "related"
target = "ADR-004"

