schema = "doctrine.adr"
version = 1

id = 15
slug = "multi-dimensional-priority-scoring"
title = "Multi-dimensional priority scoring"
status = "accepted"
created = "2026-06-21"
updated = "2026-06-23"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-015 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-015 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived
tags          = []   # free-form classification

