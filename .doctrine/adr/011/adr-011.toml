schema = "doctrine.adr"
version = 1

id = 11
slug = "spawn-interface-agnostic-contract"
title = "Harness-agnostic orchestrator spawn interface and per-harness capability altitude"
status = "accepted"
created = "2026-06-13"
updated = "2026-06-13"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-011 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-011 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

