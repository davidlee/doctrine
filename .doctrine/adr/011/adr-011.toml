schema = "doctrine.adr"
version = 1

id = 11
slug = "spawn-interface-agnostic-contract"
title = "Harness-agnostic orchestrator spawn interface and per-harness capability altitude"
status = "accepted"
created = "2026-06-13"
updated = "2026-06-13"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-011 supersedes|related <target>`.
# `doctrine inspect ADR-011` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived

