schema = "doctrine.adr"
version = 1

id = 14
slug = "rfc-governance-neutral-first-class-kind-precursor-to-revision"
title = "RFC: governance-neutral first-class kind, precursor to Revision"
status = "accepted"
created = "2026-06-20"
updated = "2026-06-20"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-NNN supersedes|related <target>`.
# `doctrine inspect ADR-NNN` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = []   # set on the old ADR by F1; reverse link is derived
tags          = []   # free-form classification

[[relation]]
label = "related"
target = "ADR-013"

