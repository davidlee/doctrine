schema = "doctrine.adr"
version = 1

id = 14
slug = "rfc-governance-neutral-first-class-kind-precursor-to-revision"
title = "RFC: governance-neutral first-class kind, precursor to Revision"
status = "accepted"
created = "2026-06-20"
updated = "2026-06-20"

[relationships]
# `supersedes` stays typed — author with `doctrine supersede ADR-014 <previous>`.
# `related` edges are uniform `[[relation]]` rows — author with `doctrine link ADR-014 related <target>`.
supersedes    = []
superseded_by = []   # set on the old ADR by F1; reverse link is derived

[[relation]]
label = "related"
target = "ADR-013"

