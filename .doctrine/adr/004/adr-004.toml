schema = "doctrine.adr"
version = 1

id = 4
slug = "relations-stored-outbound-only-reciprocity-is-derived"
title = "Relations stored outbound-only; reciprocity is derived"
status = "superseded"
created = "2026-06-07"
updated = "2026-06-27"

[relationships]
# The `supersedes` and `related` axes are uniform `[[relation]]` rows now (SL-095,
# SL-048). Author them with `doctrine link ADR-004 supersedes|related <target>`.
# `doctrine inspect ADR-004` renders them. `superseded_by` (ADR-004) and `tags` stay typed.
superseded_by = ["ADR-012"]   # set on the old ADR by F1; reverse link is derived


[[relation]]
label = "related"
target = "ADR-002"
