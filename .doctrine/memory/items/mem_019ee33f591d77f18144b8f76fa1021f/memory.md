# Dispatched phase can land green-but-incomplete

Audit a dispatched slice against every VT/exit criterion, not just a green suite — a worker can land a phase green while skipping a promised integration test.
