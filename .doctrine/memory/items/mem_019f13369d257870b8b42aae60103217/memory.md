# Reconcile: new ratifying ADR is authored directly, not via REV

Reconcile authors new ratifying ADRs DIRECTLY (adr new); the REV change grammar has no create-ADR action — REV carries only amendments (modify) + the narrative link
