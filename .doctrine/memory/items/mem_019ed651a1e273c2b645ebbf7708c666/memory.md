# Web frontend TypeScript dev workflow

Web frontend dev: bun run dev (Vite HMR), typecheck (tsc --noEmit), lint (eslint), test (vitest), build (vite build). Debug embed raw web/map/, release web/map/dist/. Vendor from npm.
