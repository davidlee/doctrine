When a change only creates or edits authored Doctrine entities and does not alter Rust code, build inputs, or executable machinery, run doctrine validate. Do not run the full just gate / cargo test suite for entity-only edits. Escalate to doctrine check commit or doctrine check gate only when the changed surface or governing workflow requires it. This is a user-confirmed local workflow rule.