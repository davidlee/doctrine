# record CLI lacks --trust/--severity flags (audit A-3)

The doctrine memory record CLI has no --trust or --severity flags. Risk axes are TOML-only — you must edit the TOML by hand after recording. Producer follow-up from SL-008 remains open.
