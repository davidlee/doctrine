# MCP handler tests with seeded memories: seeds need empty repo scope since temp dirs produce None repo from freeze(); find_for_mcp returns ALL ranked candidates, test for containment not count; memory_show does two freezes (run_show + handler enrichment) needing matching partition; clippy flags indexing_slicing on json value indexing - use get() chains; collapsible if blocks - use if-let chains (stable since 1.65).


