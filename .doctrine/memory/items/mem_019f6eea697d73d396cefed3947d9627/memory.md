# Boot regen rolls back governance when run from a stale-embed binary

boot regen uses the generating binary's embedded install/ assets; a stale binary silently rolls back governance rows and --check self-reports clean — in the doctrine repo regenerate with ./target/debug/doctrine after asset edits
