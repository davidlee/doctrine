# CLI normalization designs must distinguish flag-parity from schema-parity

When a design claims to normalize output across kinds, distinguish CLI-grammar parity (flags exist) from output-schema parity (JSON shapes match). Conflating the two leads to under-specified scope.
