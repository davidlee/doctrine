# CatalogKey is the catalog-layer identity boundary between numbered and named entities

CatalogKey enum (Numbered(EntityKey) | Memory(String)) is the catalog/graph identity boundary — EntityKey stays KINDS-only Copy; CatalogKey is Clone with manual flat Serialize
