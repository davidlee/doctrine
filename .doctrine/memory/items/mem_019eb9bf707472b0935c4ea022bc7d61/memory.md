# jpi double op-run strips pi's UI; pi 0.79 trust gate then exits silently on a repo with .pi/.agents

jpi nested op run strips pi UI -> pi 0.79 trust gate exits silently; fix is single op + project trust
