# Anchor attachment is row-gated per compiled constraint system

Anchors attach only to entities present in >=1 row of THAT compile; a subset system (e.g. human-only, SL-218 D1) silently drops anchors of entities with no subset rows -> indeterminate
