`tests/e2e_claude_install.rs::design_prompts_have_no_consumer_outside_the_design_run` scans the tree for files mentioning the string `design-prompts` and asserts set-equality with a repo-wide allowlist. Adding a `tests/e2e_design_*.rs` crate that reads an embedded runbook via `include_str!("../install/design-prompts/exploring.toml")` (the right way to pin a step's text without re-typing it, SL-262 PHASE-03) makes that crate a consumer — the gate fails until a row is added, with a comment naming the slice and phase.

This only surfaces in `doctrine check gate`/`test-all`: the new crate's own test target compiles and passes, so a focused `cargo test --test e2e_design_forward` will not catch it. Same shape as the existing `src/design_run/render/envelope.rs` row added by SL-262 PHASE-02 `VT-6`.