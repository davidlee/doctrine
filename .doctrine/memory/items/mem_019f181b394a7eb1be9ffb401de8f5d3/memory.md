# REV `modify --target SPEC-NNN` amends spec prose directly (surfaced-for-manual at apply)

A SPEC is a valid `revision change add --action modify --target`; spec-prose (decision) amendments route through the REV, applied as a surfaced-for-manual hand-edit.
