[[edge]]
target = "SPEC-018"
type = "uses"
notes = "Consumes the relation contract: CatalogEdge labels, roled references, and the tier partition are SPEC-018's vocabulary, projected verbatim and never re-validated here."
# Interactions — outbound spec -> spec edges (tech specs only).
# Seeded empty; authored via `doctrine spec interactions add`:
#   [[edge]]
#   target = "SPEC-002"       # canonical FK into .doctrine/spec/tech/NNN
#   type = "uses"             # free-text edge kind (calls / extends / uses / …)
#   notes = "…"               # optional
