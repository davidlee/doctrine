[[edge]]
target = "SPEC-007"
type = "uses"
notes = "Id lifecycle references memory's UUID-identity model as the reference case for a non-reservation, named-UUID entity; validate excludes memory kinds from its intra-kind scan"
# Interactions — outbound spec -> spec edges (tech specs only).
# Seeded empty; hand-authored in v1 (no verb — design §7 D-Q4):
#   [[edge]]
#   target = "SPEC-002"       # canonical FK into .doctrine/spec/tech/NNN
#   type = "uses"             # free-text edge kind (calls / extends / uses / …)
#   notes = "…"               # optional
