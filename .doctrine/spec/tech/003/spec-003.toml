schema = "doctrine.spec.tech"
version = 1

id = 3
slug = "doctrine"
title = "Doctrine"
status = "draft"
kind = "tech"
category = "platform"
c4_level = "context"
tags = ["doctrine", "context", "synthesis", "storage-rule", "outbound-relations", "pure-imperative-split", "architecture"]
responsibilities = [
  "Name the containers Doctrine is composed of — the entity engine, spec composition, memory, id lifecycle, install, skills distribution, boot, dispatch/worktree, the priority engine, reconciliation, and the CLI surface — and the C4 decomposition that arranges them under one whole-system context.",
  "Hold the cross-cutting principles that govern every container system-wide: the storage rule's three-tier truth model, outbound-only relations with derived reciprocity, and the pure/imperative split.",
  "State the system-wide quality invariants the corpus is checked against — never restating any single container's mechanism, only the composition and the shared discipline.",
]

# category — OPEN domain vocabulary (a taxonomy that drifts by design). Add as:
#   category = "cli"
# c4_level — closed C4 set: context | container | component | code. Add as:
#   c4_level = "container"
# descends_from — the product capability (PRD-NNN) this spec realises; tech-only,
#   single-valued, validated against the product specs. Add as:
#   descends_from = "PRD-001"
# parent — single decomposition parent (SPEC-NNN); tech-only, single-valued, the
#   tree must stay acyclic. Add as:
#   parent = "SPEC-002"
# [[source]] — code anchors this spec governs (tech-only, repeatable). Add as:
#   [[source]]
#   language = "rust"
#   identifier = "doctrine/cli"
#   module = "doctrine::cli"   # optional
