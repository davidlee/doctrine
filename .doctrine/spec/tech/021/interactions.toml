# Interactions — outbound spec -> spec edges (tech specs only).
# Seeded empty; hand-authored in v1 (no verb — design §7 D-Q4):
#   [[edge]]
#   target = "SPEC-002"       # canonical FK into .doctrine/spec/tech/NNN
#   type = "uses"             # free-text edge kind (calls / extends / uses / …)
#   notes = "…"               # optional

[[edge]]
target = "SPEC-022"
type = "uses"
notes = "The orchestrator process (funnel cadence, two-stage integration) wields the git interaction model: the pinned fork-point, two-stage projection, and worktree-aware CAS advance are defined there."
