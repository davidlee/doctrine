schema = "doctrine.backlog"
version = 1

id = 11
slug = "flake-edits-unvalidatable-inside-bubblewrap-jail-nix-absent-broken-flake-only-caught-at-relaunch"
title = "Flake edits unvalidatable inside bubblewrap jail — nix absent, broken flake only caught at relaunch"
kind = "risk"
status = "closed"
resolution = "wont-do"
created = "2026-06-26"
updated = "2026-06-27"
tags = ["area:dispatch", "area:worktree"]

[facet]                         # risk only — every field typed; seeded empty
likelihood = ""                 # low | medium | high | critical
impact     = ""                 # low | medium | high | critical
origin     = ""
controls   = []

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link RSK-NNN <label> <target>`; `doctrine inspect RSK-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "RFC-005"
