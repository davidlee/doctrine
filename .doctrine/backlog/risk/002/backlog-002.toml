schema = "doctrine.backlog"
version = 1

id = 2
slug = "cordage-explain-path-enumeration-is-exponential-on-diamond-lattices-sl-036-r-bn-2"
title = "cordage explain() path enumeration is exponential on diamond lattices (SL-036 R-B/N-2)"
kind = "risk"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-10"
updated = "2026-06-11"
tags = ["cordage", "scale"]

[facet]                         # risk only — every field typed; seeded empty
likelihood = "medium"           # low | medium | high | critical
impact     = "medium"           # low | medium | high | critical
origin     = "SL-036 design Lock known-open; audit N-2/R-B; notes.md design-stage. Re-rated low->medium 2026-06-11: real scale target raised from H1 hundreds to tens of thousands, where deep diamond lattices are routine rather than fixture-only"
controls   = [
  "F47 bounds termination (chains end at roots or degraded-SCC entry) — combinatorics, not finiteness, is the residual",
  "exposure was nil at H1/H2 (all VT fixtures <=3 nodes); at tens-of-thousands scale deep lattices are reachable in real input — likelihood re-rated medium",
  "fix direction: return the predecessor sub-DAG (or direct + one canonical chain); policy enumerates on demand",
  "quantify in the perf spike (handover Task 2) with a diamond-lattice generator before fixing; sibling of [[RSK-003]] (recursion overflow)",
  "EMPIRICALLY CONFIRMED in SL-036 post-close review 2026-06-11 (Opus probe, deleted): 16.7M chains at 24 diamond layers in 1.1s, 2^layers growth; OOM/hang beyond. Both reviewers (codex + Opus) agree. extend_chains also clones the suffix per emitted branch (query.rs:150/158) -> O(path_len) copy per path on top of the exponential count",
]

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = ["SL-036"]
specs  = ["SPEC-001"]
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
