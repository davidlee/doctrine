schema = "doctrine.backlog"
version = 1

id = 7
slug = "inspect-inbound-sort-is-lexical-on-canonical-ref-strings-not-numeric-sl-1000-sorts-before-sl-999-at-id1000-relation-graphrs-permutation-invariance-test-only-covers-single-digits"
title = "Inbound relation sort is lexical not numeric — SL-1000 sorts before SL-999 at id≥1000"
kind = "risk"
status = "resolved"
resolution = "fixed"
created = "2026-06-12"
updated = "2026-06-17"
tags = ["area:cli", "area:relations"]

[facet]                         # risk only — every field typed; seeded empty
likelihood = "low"              # low | medium | high | critical
impact     = "medium"           # low | medium | high | critical
origin     = "SL-046 /code-review — inspect inbound render sort"
controls   = ["zero-pad min 3 holds determinism below id 1000", "permutation-invariance test (single-digit) is green but does not exercise the cliff"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-005", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-046"

[[relation]]
label = "slices"
target = "SL-092"
