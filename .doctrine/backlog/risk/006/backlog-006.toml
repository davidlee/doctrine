schema = "doctrine.backlog"
version = 1

id = 6
slug = "sl-042-coverage-scanstaleness-perf-conditioned-reverse-index-staleness-batching-triggers-revisit-at-the-slice-b-reader"
title = "SL-042 coverage scan/staleness perf: reverse-index + staleness-batching — revisited at the SL-044 reader, deferred (no cliff); build on real-scale cliff"
kind = "risk"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-11"
updated = "2026-06-12"
tags = []

[facet]                         # risk only — every field typed; seeded empty
likelihood = ""                 # low | medium | high | critical
impact     = ""                 # low | medium | high | critical
origin     = ""
controls   = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = []
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
