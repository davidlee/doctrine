schema = "doctrine.backlog"
version = 1

id = 5
slug = "harden-grid-min-width-floor-test-to-assert-sliver-freedom-against-real-comfy-table"
title = "Harden grid_min_width floor test to assert sliver-freedom against real comfy-table"
kind = "chore"
status = "closed"
resolution = "wont-do"
created = "2026-06-13"
updated = "2026-06-16"
tags = ["area:cli", "testing"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-038", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
