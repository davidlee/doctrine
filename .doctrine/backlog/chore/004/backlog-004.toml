schema = "doctrine.backlog"
version = 1

id = 4
slug = "close-skill-text-is-stale-pre-adr-009-lifecycle-needs-reconcile-against-slice-status-state-machine"
title = "close skill text is stale (pre-ADR-009 lifecycle), needs reconcile against slice status state machine"
kind = "chore"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-12"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = []
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
