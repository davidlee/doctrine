schema = "doctrine.backlog"
version = 1

id = 54
slug = "scope-a-rev-for-the-mechanism-census-delete-rows"
title = "Scope a REV for the mechanism-census DELETE rows"
kind = "chore"
status = "resolved"
resolution = "done"
created = "2026-08-03"
updated = "2026-08-03"
tags = ["area:dispatch", "area:governance", "cluster:capsule-spike"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "CHR-053", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "originates_from"
target = "SL-241"

[[relation]]
label = "references"
role = "concerns"
descriptor = "scopes this target-state governance revision"
target = "REV-046"
