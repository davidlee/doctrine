# CHR-035: Empty body: mem.pattern.doctrine.plan-va-scoping needs content

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->

Found during `/dreaming` corpus maintenance (2026-07-03). Memory
`mem.pattern.doctrine.plan-va-scoping` (`mem_019f0617181d7522ba51613084359890`,
title "Plan VA criteria scoping: verify what the phase touches, not the
final end-state") has an empty body — only the title heading, no content.
Scope: `src/commands/dep_seq.rs`, `src/catalog/scan.rs`.

Not fixed inline here because reconstructing the missing content without
the original context risks fabricating a false attestation. Whoever
authored it (or has the SL/RV context behind the title) should either
fill in the body via `doctrine memory edit` or retract it if it no longer
carries a distinct lesson from its sibling
`mem.pattern.doctrine.en-criteria-honest-dependency`
(`mem_019ed26a45527092acfb586ebf9dd2ce`, now linked `related`).
