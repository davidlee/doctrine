schema = "doctrine.backlog"
version = 1

id = 6
slug = "constraint-facet-owner-immutabilityenforceability-axis-spec-019"
title = "Constraint facet: owner + immutability/enforceability axis (SPEC-019)"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-15"
tags = ["area:spec"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-074", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
