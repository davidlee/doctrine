schema = "doctrine.backlog"
version = 1

id = 7
slug = "guidance-when-a-dec-record-vs-an-adr-vs-a-governance-surface-architectural-significance-governance"
title = "Guidance: when a DEC record vs an ADR vs a governance surface (architectural significance / governance reach)"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-15"
tags = ["area:governance", "area:memory", "docs"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-050", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
