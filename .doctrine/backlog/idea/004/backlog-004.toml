schema = "doctrine.backlog"
version = 1

id = 4
slug = "channels-spawn-backend-for-claude-workers-env-seamed-subscription-tier-no-p"
title = "Channels spawn backend for claude workers (env-seamed, subscription-tier, no -p)"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-15"
tags = ["area:dispatch", "area:skills"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-056"
