schema = "doctrine.backlog"
version = 1

id = 11
slug = "structured-vhva-test-plan-findings-record-investigate-overlap-with-rv-structured-findings-turn"
title = "Structured VH/VA test plan + findings record: investigate overlap with RV (structured findings, turn-based/adversarial), REC (evidence_refs, status_deltas), and phase sheets (disposable Findings section) before committing to a new kind — VH/VA checks currently have no durable structured home for plan or results"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-14"
updated = "2026-06-14"
tags = []

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
