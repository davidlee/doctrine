# IDE-040: Tier-attributed hoisted-anchor render

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->

SL-220 PHASE-06 deferral (RV-277 F-6). A class-mate's hoisted claim anchor
(`ValueProvenance::Authored`) renders as `projected` — correct (rung 2 is what
fed scoring) but the provenance chain stops there. A tier-attributed hoist
shape would disclose *whose* anchor the projection hoisted (e.g. "projected ·
hoisted from SL-204 pin"). Out of SL-220 design scope by adjudication.
