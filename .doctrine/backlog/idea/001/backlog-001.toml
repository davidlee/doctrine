schema = "doctrine.backlog"
version = 1

id = 1
slug = "ship-a-scaffold-skill-skill-user-owned-skills-install-repo-config"
title = "Ship a scaffold-skill skill + user-owned skills install repo config"
kind = "idea"
status = "closed"
resolution = "wont-do"
created = "2026-06-10"
updated = "2026-06-16"
tags = ["area:skills"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-057", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
