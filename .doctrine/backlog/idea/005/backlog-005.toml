schema = "doctrine.backlog"
version = 1

id = 5
slug = "detect-harness-from-env-inside-doctrine-commands-to-shrink-the-skill-decision-surface"
title = "Detect harness from ENV inside doctrine commands to shrink the skill decision surface"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-15"
tags = ["area:cli", "area:skills"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-057", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
