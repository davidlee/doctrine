# IDE-002: Durable intra-file region reference primitive — anchor a code region without rot-prone line numbers (content-hash-of-span / symbol path); serves RV warm-cache domain_map (SL-040 T-c) and review citations generally

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->
