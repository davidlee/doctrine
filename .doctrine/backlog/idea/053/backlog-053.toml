schema = "doctrine.backlog"
version = 1

id = 53
slug = "new-stub-agent-role-rewrite-orchestratorworker-role-prompts-for-the-clone-workflow"
title = "New stub agent role + rewrite orchestrator/worker role prompts for the clone workflow"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-08-13"
updated = "2026-08-13"
tags = []

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "originates_from"
target = "SL-254"

[[relation]]
label = "references"
role = "concerns"
descriptor = "clone-workflow role prompts presuppose SL-255's clone provisioning shape"
target = "SL-255"

[[relation]]
label = "references"
role = "concerns"
descriptor = "a revived drive-slice driver pattern may be exactly what a clone-workflow orchestrator role prompt should describe"
target = "IDE-052"
