# IDE-041: Sequence library vs federated search delivery under PRD-017

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->

Relocated from PRD-017 §8 OQ-3 (codex review, M13): whether federated `search`
lands together with the library, or the library lands first and search federation
follows once the library is one of several providers, is **slice sequencing**, not
product intent — PRD-017 is valid either way. Captured here so it is settled at
plan time, not carried as a false product open question.

The coverage census notes IMP-154 (`search --all` with a path column) already
seeds the federation work. Governed by PRD-017 / SPEC-026.
