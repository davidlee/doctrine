# IDE-016: Agent UX research: which MCP read surfaces yield more efficient interactions than CLI? Review-adjacent reads proven valuable; memory retrieval next; inspect/next/survey/backlog unproven.

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->
