schema = "doctrine.backlog"
version = 1

id = 58
slug = "semi-global-g-flag-pipe-markdown-output-through-glow-pager"
title = "Semi-global -G flag: pipe markdown output through glow --pager"
kind = "idea"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-09-26"
updated = "2026-09-26"
tags = ["area:cli", "area:render", "area:ux", "cluster:cli-ux"]

# outbound-only. Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-452", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
descriptor = "CLI UX sweep this convenience flag serves"
target = "IMP-133"
