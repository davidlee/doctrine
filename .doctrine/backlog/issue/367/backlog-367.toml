schema = "doctrine.backlog"
version = 1

id = 367
slug = "live-acts-is-blind-to-same-kind-replacement-so-actinvalidated-under-reports"
title = "live_acts is blind to same-kind replacement, so ActInvalidated under-reports"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-08-16"
updated = "2026-08-16"
tags = ["area:correctness", "area:design-run", "cluster:design-run", "footgun"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "SL-256", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
descriptor = "states the general rule this defect is an instance of"
target = "DEC-238"

[[relation]]
label = "references"
role = "originates_from"
target = "SL-256"
