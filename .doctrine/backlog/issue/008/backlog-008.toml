schema = "doctrine.backlog"
version = 1

id = 8
slug = "prioritygraphtestsnon-backlog-nodes-carry-no-dep-seq-edges-reported-flaky-under-parallel-just-check"
title = "priority::graph::tests::non_backlog_nodes_carry_no_dep_seq_edges reported flaky under parallel just check"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-16"
tags = ["area:backlog", "testing"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
