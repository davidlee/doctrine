schema = "doctrine.backlog"
version = 1

id = 56
slug = "stale-base-dispatch-integrate-silently-deletes-authored-corpus"
title = "Stale-base dispatch integrate silently deletes authored corpus"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-27"
updated = "2026-06-27"
tags = ["area:dispatch", "area:worktree", "correctness", "data-loss"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "ISS-038"

[[relation]]
label = "related"
target = "RSK-010"

[[relation]]
label = "related"
target = "SL-127"

[[relation]]
label = "related"
target = "ISS-036"

[[relation]]
label = "related"
target = "RFC-005"

[[relation]]
label = "related"
target = "IMP-174"
