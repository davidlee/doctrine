
Observed driving SL-220 PHASE-03: the orchestrator passed a `code_start` whose
short prefix was real but whose suffix was fabricated (hand-expanded from a
short SHA); `dispatch_conclude_phase` landed it verbatim. A second row stored
a bare 8-char short form. Neither was rejected.

Expected: the tool resolves each oid against the repo (`git rev-parse
--verify <oid>^{commit}`), rejects unknown/ambiguous ids, and normalises to
the full 40-char form before landing the boundary row. Conformance derivation
(prepare-review) and F-5/F-6 guards consume these rows — a corrupt oid
surfaces at the worst time, audit.

Both SL-220 rows were corrected via the UPSERT path immediately.
