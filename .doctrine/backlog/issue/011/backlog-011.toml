schema = "doctrine.backlog"
version = 1

id = 11
slug = "sl-056-subagentstart-hook-merge-keys-identity-on-command-only-stale-matcher-never-healed-on"
title = "SL-056 SubagentStart hook merge keys identity on command only — stale matcher never healed on reinstall (fail-open unstamped worker)"
kind = "issue"
status = "closed"
resolution = "done"
created = "2026-06-14"
updated = "2026-06-24"
tags = ["area:dispatch", "area:harness", "area:worktree"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-034", rank = 1 }] # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-124"

[[relation]]
label = "slices"
target = "SL-125"


