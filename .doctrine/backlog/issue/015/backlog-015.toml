schema = "doctrine.backlog"
version = 1

id = 15
slug = "dispatch-claude-arm-funnel-import-verb-corrupt-patch-on-valid-diff-and-doctrinedispatch-lacks"
title = "dispatch claude-arm funnel: import verb corrupt-patch on valid diff, and .doctrine/dispatch lacks gitignore negation for boundaries.toml"
kind = "issue"
status = "closed"
resolution = "duplicate"
created = "2026-06-14"
updated = "2026-06-18"
tags = ["area:dispatch"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
