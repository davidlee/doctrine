schema = "doctrine.backlog"
version = 1

id = 10
slug = "read-side-cli-does-not-surface-slice-outbound-tier-1-relations-inspect-and-slice-show-return-empty"
title = "Read-side CLI does not surface slice outbound tier-1 relations: inspect and slice show return empty despite authored [[relation]] rows"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-13"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = []
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
