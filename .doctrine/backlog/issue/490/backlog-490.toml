schema = "doctrine.backlog"
version = 1

id = 490
slug = "search-default-kind-set-omits-pol-std-and-req-revisit-the-exclusions"
title = "Search default kind set omits POL, STD and REQ — revisit the exclusions"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-09-26"
updated = "2026-09-26"
tags = ["area:cli", "area:entity", "area:governance", "area:spec"]

# outbound-only. Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "PRD-017"

[[relation]]
label = "related"
target = "SPEC-026"

[[relation]]
label = "related"
target = "SL-141"

[[relation]]
label = "related"
target = "IMP-154"

[[relation]]
label = "related"
target = "IDE-041"

[[relation]]
label = "related"
target = "IMP-488"
