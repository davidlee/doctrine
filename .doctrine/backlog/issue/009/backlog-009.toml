schema = "doctrine.backlog"
version = 1

id = 9
slug = "slice-new-scaffolder-emits-the-stale-relationships-reserved-comment-not-the-post-sl-048-relation"
title = "slice new scaffolder emits the stale [relationships] reserved comment, not the post-SL-048 [[relation]] row format"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-13"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
