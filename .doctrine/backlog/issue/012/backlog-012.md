# ISS-012: doctrine claude install self-enforces a too-broad .doctrine/agents/* gitignore that swallows authored AGENTS.md and is unclassified in the worktree DERIVED_RUNTIME tier (SL-056 gap)

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->
