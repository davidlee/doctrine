schema = "doctrine.backlog"
version = 1

id = 6
slug = "scan-coverage-double-walks-slice-dir-slug-symlink-over-counting-coverage-keys"
title = "scan_coverage double-walks slice dir + slug symlink, over-counting coverage keys"
kind = "issue"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-12"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = []
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
