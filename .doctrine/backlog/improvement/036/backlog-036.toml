schema = "doctrine.backlog"
version = 1

id = 36
slug = "inspect-full-corpus-scan-should-tolerate-a-malformed-sibling-entity-not-abort-corpus-wide"
title = "inspect full-corpus scan should tolerate a malformed sibling entity, not abort corpus-wide"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-12"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = ["SL-046"]
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
