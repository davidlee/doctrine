schema = "doctrine.backlog"
version = 1

id = 467
slug = "design-run-gate-prose-actor-batching-present-then-ask"
title = "Design-run gate prose: actor, batching, present-then-ask"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-09-23"
updated = "2026-09-23"
tags = ["area:design", "area:design-run", "area:skills", "cluster:design-run"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-466", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
descriptor = "prose carries DEC-088's on-behalf acceptance"
target = "DEC-088"

[[relation]]
label = "related"
target = "IMP-464"

[[relation]]
label = "related"
target = "ISS-287"

[[relation]]
label = "related"
target = "ISS-299"
