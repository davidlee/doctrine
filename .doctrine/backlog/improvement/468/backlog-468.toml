schema = "doctrine.backlog"
version = 1

id = 468
slug = "plural-agent-def-install-project-all-of-a-harnesss-agent-defs-capsule-defs-are-published-only"
title = "Plural agent-def install: project all of a harness's agent defs (capsule defs are published-only)"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-09-23"
updated = "2026-09-23"
tags = ["area:agents", "area:install", "cluster:capsule", "harness:claude"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-434"

[[relation]]
label = "references"
role = "concerns"
descriptor = "co-delivery: reseat semantics gate a plural install leg (same function + materialize-then-link layout)"
target = "ISS-216"

[[relation]]
label = "references"
role = "concerns"
descriptor = "pi arm of the same harness-to-asset match; symlink points at the historical codex/ dir"
target = "CHR-040"

[[relation]]
label = "references"
role = "concerns"
descriptor = "wildcard-fallback hardening subsumed by the explicit arms a plural leg needs"
target = "IMP-181"
