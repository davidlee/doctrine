schema = "doctrine.backlog"
version = 1

id = 457
slug = "design-run-verbs-occupy-the-document-render-verb-slots"
title = "Design-run verbs occupy the document-render verb slots"
kind = "improvement"
status = "closed"
resolution = "duplicate"
created = "2026-09-18"
updated = "2026-09-18"
tags = ["area:cli", "area:design-run"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
descriptor = "the uniform <kind> <verb> grammar this debt violates"
target = "SPEC-013"

[[relation]]
label = "references"
role = "originates_from"
target = "SL-246"

[[relation]]
label = "references"
role = "concerns"
descriptor = "naming conventions the rehome must satisfy"
target = "STD-002"

[[relation]]
label = "related"
target = "IMP-393"
