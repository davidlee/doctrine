schema = "doctrine.backlog"
version = 1

id = 21
slug = "filter-and-sort-backlog-by-risk-facet-axes-likelihoodimpactexposure"
title = "Filter and sort backlog by risk facet axes (likelihood/impact/exposure)"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-11"
updated = "2026-06-15"
tags = ["area:backlog"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-028", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
