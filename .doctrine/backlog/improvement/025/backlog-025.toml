schema = "doctrine.backlog"
version = 1

id = 25
slug = "promote-content-hashed-path-set-to-shared-primitive"
title = "Promote content-hashed-path-set to shared primitive"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-11"
updated = "2026-07-03"
tags = ["area:memory", "area:quality", "area:review"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-068", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "originates_from"
target = "SL-040"

[estimate]
lower = 2.0
upper = 4.0
