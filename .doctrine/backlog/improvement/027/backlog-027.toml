schema = "doctrine.backlog"
version = 1

id = 27
slug = "deferred-doc-canon-has-no-tracked-home-or-enforcement-after-tech-spec-backfill-bifurcates-the-doc"
title = "Deferred doc/* canon has no tracked home or enforcement after tech-spec backfill bifurcates the doc"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-11"
updated = "2026-06-16"
tags = ["area:governance", "area:spec", "docs"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
