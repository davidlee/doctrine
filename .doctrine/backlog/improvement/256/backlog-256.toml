schema = "doctrine.backlog"
version = 1

id = 256
slug = "plan-time-selector-completeness-check-flag-vt-test-file-paths-not-in-design-target-selectors"
title = "Plan-time selector completeness check: flag VT test_file paths not in design-target selectors"
kind = "improvement"
status = "resolved"
resolution = "fixed"
created = "2026-07-04"
updated = "2026-07-24"
tags = ["area:dispatch", "area:plan", "area:slice", "cluster:selector-scope"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-162", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
target = "RFC-011"
