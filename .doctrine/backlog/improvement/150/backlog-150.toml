schema = "doctrine.backlog"
version = 1

id = 150
slug = "mcp-review-tool-inline-help-document-response-fields-add-workflow-guidance-examples"
title = "MCP review tool inline help: document response fields, add workflow guidance + examples"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-22"
updated = "2026-06-22"
tags = ["help-text", "mcp", "review"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "drift"
target = "install/review-ledger.md: the shipped ledger protocol whose invariants should be inlined or linked from MCP tool descriptions"

[value]
value = 1.5

[estimate]
lower = 0.5
upper = 2.0
