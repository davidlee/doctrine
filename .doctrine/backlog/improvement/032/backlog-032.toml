schema = "doctrine.backlog"
version = 1

id = 32
slug = "governance-superseded-by-is-a-stored-reciprocal-derive-it-from-supersedes-dont-store-it-adr-004"
title = "Governance superseded_by is a stored reciprocal — derive it from supersedes, don't store it (ADR-004)"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-12"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = []
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
