schema = "doctrine.backlog"
version = 1

id = 41
slug = "worktree-skill-fork-cleanup-after-merge-ownership-is-unstated-decide-who-removes-the-worktree-and"
title = "worktree skill: fork cleanup-after-merge ownership is unstated — decide who removes the worktree and deletes the merged branch"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-13"
updated = "2026-06-14"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
