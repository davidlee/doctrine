schema = "doctrine.backlog"
version = 1

id = 464
slug = "locking-a-design-should-be-one-command-not-eleven"
title = "Locking a design should be one command, not eleven"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-09-19"
updated = "2026-09-19"
tags = ["area:cli", "area:design-run", "area:docs", "area:ergonomics", "cluster:design-run"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
descriptor = "the design-run lock ceremony this item wants collapsed"
target = "PRD-019"

[[relation]]
label = "references"
role = "concerns"
descriptor = "eleven hand-built payloads as a measurable token cost"
target = "RFC-011"

[[relation]]
label = "related"
target = "IMP-430"

[[relation]]
label = "related"
target = "ISS-310"
