schema = "doctrine.backlog"
version = 1

id = 26
slug = "backlog-triggers-actionability-mask-spec-001-d6"
title = "Backlog triggers actionability mask (SPEC-001 D6)"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-11"
updated = "2026-06-15"
tags = ["area:backlog"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-012", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
