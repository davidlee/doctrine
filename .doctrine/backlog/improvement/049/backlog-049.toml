schema = "doctrine.backlog"
version = 1

id = 49
slug = "agent-facing-support-skill-memory-docs-for-howwhen-to-relate-entities-structurally"
title = "Agent-facing support (skill + memory + docs) for how/when to relate entities structurally"
kind = "improvement"
status = "resolved"
resolution = "fixed"
created = "2026-06-13"
updated = "2026-06-15"
tags = ["area:relations", "area:skills"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
