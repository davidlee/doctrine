schema = "doctrine.backlog"
version = 1

id = 15
slug = "boot-check-flags-empty-governance-sections-active-policiesstandards-as-unpopulated"
title = "boot --check flags empty governance sections (Active Policies/Standards) as unpopulated"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-30"
tags = ["area:governance"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
