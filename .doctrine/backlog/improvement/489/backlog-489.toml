schema = "doctrine.backlog"
version = 1

id = 489
slug = "prompt-cascade-review-model-coverage-currency-and-role-fitness"
title = "Prompt cascade review: model coverage, currency, and role fitness"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-09-26"
updated = "2026-09-26"
tags = ["area:prompt-cascade", "hymns", "model-tier"]

# outbound-only. Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "RFC-013"

[[relation]]
label = "related"
target = "SPEC-023"

[[relation]]
label = "related"
target = "ISS-308"

[[relation]]
label = "related"
target = "ISS-447"

[[relation]]
label = "related"
target = "ISS-491"

[[relation]]
label = "related"
target = "IMP-242"

[[relation]]
label = "related"
target = "IDE-042"

[[relation]]
label = "related"
target = "IDE-053"
