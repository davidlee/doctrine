schema = "doctrine.backlog"
version = 1

id = 148
slug = "mcp-memory-tool-inline-help-document-response-fields-expose-validate-add-examples"
title = "MCP memory tool inline help: document response fields, expose validate, add examples"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-22"
updated = "2026-06-22"
tags = ["mcp", "memory", "help-text"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "drift"
target = "mem.concept.doctrine.memory-model: the memory model concept that should be linked from the tool descriptions"

[value]
value = 1.8

[estimate]
lower = 1.0
upper = 2.0
