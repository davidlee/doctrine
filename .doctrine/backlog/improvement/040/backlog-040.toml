schema = "doctrine.backlog"
version = 1

id = 40
slug = "add-colorautoalwaysnever-flag-for-listing-colour-control"
title = "Add --color=auto|always|never flag for listing colour control"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-12"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-053"
