# IMP-285: RFC-019 Phase D: tension narrative and session surface

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->

## Intent

RFC-019 Phase D: tension narrative + session surface.

- Extend `next`/`explain` with the value-rank vs delivery-rank disagreement
  callout ("SL-x outranks SL-y on value; SL-y is cheaper and unblocking —
  surfaces first"); render where surviving seq structure overrides the value
  order, never resolve silently.
- **Pre-D obligation (SL-217 Deferred):** D7 rank-aware quarantine /
  agent-testimony demotion knob before any stakeholder-facing surface —
  `confirm_boost` (D13) is selection-order bias only, no determinacy
  semantics. Product-critique tensions #3 (agent evidence fully
  authoritative) and #6 (cardinal from ordinal) land here.
- Later, per RFC OQ-1: session mode + read-write web elicitation surface for
  distinct stakeholder audiences — only after C's interaction is proven
  (RFC-002 demotion discipline).

Unblocked: piggybacks any time after Phase B (shipped, SL-213).
