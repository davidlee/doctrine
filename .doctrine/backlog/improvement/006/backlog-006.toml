schema = "doctrine.backlog"
version = 1

id = 6
slug = "uniform-destructive-lifecycle-verbs"
title = "Uniform destructive + lifecycle-transition verbs across kinds (delete/archive, status-transition)"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-08"
updated = "2026-06-24"
tags = ["area:backlog", "area:memory", "area:slice", "area:spec"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-062"
