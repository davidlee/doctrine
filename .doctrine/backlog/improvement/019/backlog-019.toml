schema = "doctrine.backlog"
version = 1

id = 19
slug = "cordage-golden-net-proves-determinism-but-not-value-correctness-no-independent-value-oracle"
title = "cordage golden_net proves determinism but not value-correctness: no independent value oracle"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-10"
updated = "2026-06-16"
tags = ["area:relations", "cordage", "correctness", "testing"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-020", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-036"

[[relation]]
label = "specs"
target = "SPEC-001"
