schema = "doctrine.backlog"
version = 1

id = 43
slug = "import-verb-moved-head-re-anchor-allow-reanchor-3way-onto-moved-coordination-head-computable-path"
title = "import verb: moved-HEAD re-anchor (--allow-reanchor) — 3way onto moved coordination HEAD + computable path-disjointness"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-13"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
