schema = "doctrine.backlog"
version = 1

id = 72
slug = "worktreecreate-hook-to-enable-parallel-claude-quality-isolated-dispatch-workers-on-subscription"
title = "WorktreeCreate hook for pre-worker fail-closability on the claude dispatch arm (deferred; NOT needed for base control)"
kind = "improvement"
status = "resolved"
resolution = "fixed"
created = "2026-06-14"
updated = "2026-06-25"
tags = ["area:dispatch"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = ["IMP-065"]                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
