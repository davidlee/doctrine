schema = "doctrine.backlog"
version = 1

id = 13
slug = "lift-shared-listshow-rowtablejson-shape-across-slice-and-spec-verbs"
title = "Lift shared list+show row/table/JSON shape across slice and spec verbs"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-10"
tags = ["dry", "render", "slice", "spec", "trigger", "deferred"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "specs"
target = "PRD-011"
