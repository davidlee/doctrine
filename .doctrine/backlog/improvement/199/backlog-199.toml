schema = "doctrine.backlog"
version = 1

id = 199
slug = "automate-r-5-delta-content-check-against-declared-file-set"
title = "Automate R-5 delta-content check against declared file set"
kind = "improvement"
status = "triaged"
resolution = ""
created = "2026-06-28"
updated = "2026-06-30"
tags = ["area:dispatch"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-204", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "RFC-005"

[[relation]]
label = "related"
target = "RFC-004"

[value]
value = 2.5

[estimate]
lower = 1.5
upper = 3.0
