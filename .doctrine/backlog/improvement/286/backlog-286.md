# IMP-286: Estimate comparison domain: constraint semantics and elicit frames

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->

## Intent

Estimate comparison domain — sibling extension riding the SL-210 ledger and
SL-213/217 machinery (RFC-019 "estimate domain" batch, sequenced after C):

- est-domain constraint semantics: `est_cost` ordering rows, range
  projection.
- Elicit frames for team-facing comparative sizing; records included.
- Precedes Phase E (IMP-287): cross-domain yield ranking needs this domain
  plus the estimate feasible-region model (RV-260 F-5).
