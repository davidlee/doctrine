schema = "doctrine.backlog"
version = 1

id = 34
slug = "interrogate-refactoring-all-relations-modelling-to-a-uniform-schema-across-kinds"
title = "Interrogate refactoring all relations modelling to a uniform schema across kinds"
kind = "improvement"
status = "resolved"
resolution = "promoted"
created = "2026-06-12"
updated = "2026-06-15"
tags = ["area:relations"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
