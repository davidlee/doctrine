schema = "doctrine.backlog"
version = 1

id = 33
slug = "cross-kind-depseq-capture-extend-needsafter-sequencing-beyond-backlog-to-specsslices"
title = "Cross-kind dep/seq capture — extend needs/after sequencing beyond backlog to specs/slices"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-12"
updated = "2026-06-24"
tags = ["area:relations"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-060"
