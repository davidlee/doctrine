schema = "doctrine.backlog"
version = 1

id = 407
slug = "doctor-leg-name-the-layer-blocking-claude-hook-activation"
title = "Doctor leg: name the layer blocking Claude hook activation"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-08-06"
updated = "2026-08-06"
tags = ["area:cli", "area:doctor", "harness:claude", "hooks"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "SL-250", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "governed_by"
target = "POL-002"

[[relation]]
label = "references"
role = "originates_from"
target = "IMP-400"

[[relation]]
label = "references"
role = "concerns"
descriptor = "Claude harness field notes: the empirical trust-layer findings this diagnostic walk is built on"
target = "RFC-018"
