schema = "doctrine.backlog"
version = 1

id = 321
slug = "verify-the-advice-surface-refusals-and-prescriptions-against-the-machine"
title = "Verify the advice surface: refusals and prescriptions against the machine"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-07-26"
updated = "2026-07-26"
tags = ["area:cli", "area:dispatch", "cluster:dispatch-funnel"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "SL-228", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "originates_from"
target = "SL-228"

[[relation]]
label = "related"
target = "ISS-250"

[[relation]]
label = "related"
target = "ISS-251"

[[relation]]
label = "related"
target = "ISS-249"

[[relation]]
label = "related"
target = "ISS-253"
