schema = "doctrine.backlog"
version = 1

id = 37
slug = "specrs-has-no-read-spec-reader-relation-edges-show-list-scan-each-re-parse-spec-toml-inline-4-copies-extract-one-reader"
title = "spec.rs has no read_spec reader: relation_edges + show + list + scan each re-parse Spec toml inline (4 copies) — extract one reader"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-15"
tags = ["area:spec", "quality"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-046"
