schema = "doctrine.backlog"
version = 1

id = 45
slug = "macos-sandboxing-via-seatbeltsandbox-exec-with-bwrap-fallback-cross-platform-jail-seam"
title = "macOS sandboxing via Seatbelt/sandbox-exec with bwrap fallback (cross-platform jail seam)"
kind = "improvement"
status = "closed"
resolution = "fixed"
created = "2026-06-13"
updated = "2026-07-03"
tags = ["area:dispatch"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-004", rank = 0 }, { to = "SL-182", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "originates_from"
target = "SL-056"

