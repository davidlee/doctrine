schema = "doctrine.backlog"
version = 1

id = 129
slug = "separate-main-from-edge-decouple-dispatch-landing-zone-from-daily-authoring-churn"
title = "Separate main from edge: decouple dispatch landing zone from daily authoring churn"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-20"
updated = "2026-06-21"
tags = ["area:dispatch", "area:governance"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-124", rank = 1 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
