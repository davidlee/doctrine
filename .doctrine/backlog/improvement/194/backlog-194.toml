schema = "doctrine.backlog"
version = 1

id = 194
slug = "diff-aware-funnel-verify-zero-new-across-gatesdoctor-not-just-compile"
title = "Diff-aware funnel verify: zero-new across gates+doctor, not just compile"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-28"
updated = "2026-06-28"
tags = ["area:dispatch", "area:testing"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "SL-170", rank = 0 }, { to = "IMP-198", rank = 1 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "RFC-005"

[[relation]]
label = "related"
target = "SL-170"

[[relation]]
label = "related"
target = "IDE-008"

[[relation]]
label = "related"
target = "IMP-198"

[value]
value = 3.5

[estimate]
lower = 3.0
upper = 6.0
