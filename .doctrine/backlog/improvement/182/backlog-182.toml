schema = "doctrine.backlog"
version = 1

id = 182
slug = "knowledge-authoring-skill-route-agents-to-create-transition-asmdecquecon-records"
title = "/knowledge authoring skill: route agents to create + transition ASM/DEC/QUE/CON records"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-26"
updated = "2026-06-26"
tags = ["area:memory", "area:skills"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
target = "RFC-007"

[[relation]]
label = "references"
role = "concerns"
target = "SPEC-019"

[[relation]]
label = "references"
role = "concerns"
target = "RFC-008"

[value]
value = 3.0

[estimate]
lower = 2.0
upper = 5.0
