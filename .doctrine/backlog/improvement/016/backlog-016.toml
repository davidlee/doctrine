schema = "doctrine.backlog"
version = 1

id = 16
slug = "structural-relation-surface-for-product-to-product-and-spec-to-adr-links-cross-corpus-relations-are-prose-only"
title = "Structural relation surface for product-to-product and spec-to-ADR links — cross-corpus relations are prose-only"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-18"
tags = ["area:relations", "area:spec"]

[relationships]                 # outbound-only (ADR-004)
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-015", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "specs"
target = "PRD-013"

[[relation]]
label = "specs"
target = "PRD-012"

[value]
value = 1.1

[estimate]
lower = 1.0
upper = 3.0
