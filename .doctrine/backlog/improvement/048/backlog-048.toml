schema = "doctrine.backlog"
version = 1

id = 48
slug = "wire-the-relation-linkunlink-cli-verb-so-structural-relations-need-not-be-hand-authored"
title = "Wire the relation link/unlink CLI verb so structural relations need not be hand-authored"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-13"
updated = "2026-06-24"
tags = ["area:cli", "area:relations"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
