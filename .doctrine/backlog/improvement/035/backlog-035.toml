schema = "doctrine.backlog"
version = 1

id = 35
slug = "slice-relationships-schema-has-no-adr-slot-slice-to-adr-refs-are-prose-only"
title = "Slice relationships schema has no ADR slot: slice to ADR refs are prose-only"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-15"
tags = ["area:relations", "area:slice", "area:spec"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-064", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-048"
