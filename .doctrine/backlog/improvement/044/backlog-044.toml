schema = "doctrine.backlog"
version = 1

id = 44
slug = "migrate-priority-render-surfaces-onto-renderopts"
title = "Migrate priority render surfaces onto RenderOpts"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-13"
updated = "2026-06-24"
tags = ["area:backlog", "area:cli"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-040", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
