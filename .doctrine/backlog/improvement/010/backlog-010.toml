schema = "doctrine.backlog"
version = 1

id = 10
slug = "sl-032-review-cleanups"
title = "SL-032 review cleanups: write_class test rewrite + reseat atomicity"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-24"
tags = ["area:review"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
