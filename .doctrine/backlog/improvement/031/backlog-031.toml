schema = "doctrine.backlog"
version = 1

id = 31
slug = "lightweight-patch-or-update-artifact"
title = "Lightweight patch/update entity kind for incremental edits"
kind = "improvement"
status = "closed"
resolution = "wont-do"
created = "2026-06-11"
updated = "2026-06-16"
tags = ["area:backlog"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-021", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
