schema = "doctrine.backlog"
version = 1

id = 42
slug = "code-review-skill-is-structurally-orphaned-integrate-it-into-the-corpus-backlog-lifecycle-awareness"
title = "code-review skill is structurally orphaned — integrate it into the corpus (backlog + lifecycle awareness) beyond the IMP-023 RV rewire"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-15"
tags = ["area:review", "area:skills"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-023", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
