schema = "doctrine.backlog"
version = 1

id = 17
slug = "memory-list-adopts-shared-column-model"
title = "memory list adopts the shared column model"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-15"
tags = ["area:cli", "area:memory"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
