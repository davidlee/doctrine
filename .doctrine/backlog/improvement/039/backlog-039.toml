schema = "doctrine.backlog"
version = 1

id = 39
slug = "colour-the-deferred-listing-surfaces-ad-hoc-writeln-priority-surveynext"
title = "Colour the deferred listing surfaces: ad-hoc writeln! + priority survey/next"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-12"
updated = "2026-06-12"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = ["SL-053"]
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
