schema = "doctrine.backlog"
version = 1

id = 64
slug = "sl-048-od-3-migrate-gov-supersedes-typed-array-to-relation-lifecycleonly-slot-repoint-supersede-verb"
title = "SL-048 OD-3: migrate gov supersedes typed array to [[relation]] LifecycleOnly slot; repoint supersede verb"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-14"
updated = "2026-06-24"
tags = ["area:governance"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-028", rank = 0 }, { to = "IMP-012", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-095"
