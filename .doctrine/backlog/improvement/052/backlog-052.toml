schema = "doctrine.backlog"
version = 1

id = 52
slug = "orchestrator-post-spawn-marker-check-abort-an-unstamped-worker-fork"
title = "Orchestrator post-spawn marker check: abort an unstamped worker fork"
kind = "improvement"
status = "resolved"
resolution = "promoted"
created = "2026-06-13"
updated = "2026-06-20"
tags = ["area:dispatch"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-056"
