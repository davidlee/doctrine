schema = "doctrine.backlog"
version = 1

id = 29
slug = "rv-review-verb-family-black-box-e2e-cli-golden-audit-pilot-coverage"
title = "RV review verb family: black-box e2e CLI golden + /audit pilot coverage"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-11"
updated = "2026-06-16"
tags = ["area:coverage", "area:review"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
