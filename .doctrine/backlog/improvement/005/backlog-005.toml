schema = "doctrine.backlog"
version = 1

id = 5
slug = "normalise-explicit-slug-like-a-derived-slug-at-entity-new"
title = "Normalise explicit --slug like a derived slug at entity new"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-08"
updated = "2026-06-24"
tags = ["area:backlog", "area:cli"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-024"
