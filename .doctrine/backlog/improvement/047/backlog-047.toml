schema = "doctrine.backlog"
version = 1

id = 47
slug = "trinary-actionability-decouple-blocking-from-eligibility-so-unsettled-records-and-kinds-gate-work"
title = "Trinary actionability: decouple blocking from eligibility so unsettled records (and kinds) gate work without being actionable"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-13"
updated = "2026-06-13"
tags = []

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-033", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
