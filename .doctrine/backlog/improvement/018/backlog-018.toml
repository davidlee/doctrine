schema = "doctrine.backlog"
version = 1

id = 18
slug = "columns-in-spine-flags-matrix"
title = "columns flag joins the uniform shared-grammar conformance matrix when memory migrates"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-24"
tags = ["area:cli", "area:memory"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-050", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
