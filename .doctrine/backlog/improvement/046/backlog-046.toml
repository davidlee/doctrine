schema = "doctrine.backlog"
version = 1

id = 46
slug = "fresh-session-probe-of-the-literal-dispatch-worker-subagentstart-matcher-path"
title = "Fresh-session probe of the literal dispatch-worker SubagentStart matcher path"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-06-13"
updated = "2026-06-24"
tags = ["area:dispatch", "testing"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "ISS-011", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
