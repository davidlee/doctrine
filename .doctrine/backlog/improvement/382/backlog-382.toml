schema = "doctrine.backlog"
version = 1

id = 382
slug = "govern-the-phase-plan-content-model"
title = "Govern the phase-plan content model"
kind = "improvement"
status = "started"
resolution = ""
created = "2026-08-02"
updated = "2026-08-02"
tags = ["area:governance", "area:slice", "area:spec", "planning"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "IMP-381"

[[relation]]
label = "related"
target = "RFC-027"

[[relation]]
label = "related"
target = "EVD-005"

[[relation]]
label = "related"
target = "PRD-001"

[[relation]]
label = "related"
target = "SPEC-014"

[[relation]]
label = "related"
target = "SPEC-002"

[[relation]]
label = "related"
target = "REV-045"

[[relation]]
label = "related"
target = "DEC-119"
