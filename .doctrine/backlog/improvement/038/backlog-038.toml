schema = "doctrine.backlog"
version = 1

id = 38
slug = "column-model-validate-default-set-against-available-at-construction-not-first-request"
title = "Column model: validate default set against available at construction not first request"
kind = "improvement"
status = "resolved"
resolution = "fixed"
created = "2026-06-12"
updated = "2026-06-17"
tags = ["area:cli"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-028", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-079"
