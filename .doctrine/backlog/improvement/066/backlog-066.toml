schema = "doctrine.backlog"
version = 1

id = 66
slug = "f1-refuse-reword-slice-backlog-knowledge"
title = "Finish the F-1 non-destructive refuse reword across slice/backlog/knowledge status setters (RV-024 F-2)"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-14"
updated = "2026-06-18"
tags = ["area:backlog", "area:memory", "area:slice"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-061", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
