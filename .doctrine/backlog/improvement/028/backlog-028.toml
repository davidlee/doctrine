schema = "doctrine.backlog"
version = 1

id = 28
slug = "retire-backlog-order-fold-ordering-into-list-as-a-flag"
title = "retire backlog order; fold ordering into list as a flag (--order)"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-11"
updated = "2026-06-16"
tags = ["area:backlog", "area:cli"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = [{ to = "IMP-005", rank = 0 }, { to = "IMP-010", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
