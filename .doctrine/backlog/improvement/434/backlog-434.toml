schema = "doctrine.backlog"
version = 1

id = 434
slug = "capsule-execution-agent-defs-and-a-capsule-driver-skill"
title = "Capsule-execution agent defs and a /capsule-driver skill"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-08-15"
updated = "2026-09-18"
tags = ["area:agents", "area:dispatch", "area:skills", "cluster:capsule", "harness:claude"]

# outbound-only (ADR-004). Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
descriptor = "capsule agent definitions and driver skill for the claude subagent arm"
target = "ADR-020"

[[relation]]
label = "references"
role = "originates_from"
target = "SL-251"

[[relation]]
label = "references"
role = "concerns"
descriptor = "per-agent boot floor is the cost driver behind the capsule tiering"
target = "IMP-435"

[[relation]]
label = "references"
role = "concerns"
descriptor = "subagent memory reach — capsule workers have no ambient surfacing"
target = "IMP-436"
