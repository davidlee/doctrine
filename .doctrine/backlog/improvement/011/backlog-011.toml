schema = "doctrine.backlog"
version = 1

id = 11
slug = "warn-on-record-of-an-unverified-thread-invisible-to-findretrieve-until-verified"
title = "Warn on record of an unverified thread (invisible to find/retrieve until verified)"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-15"
tags = ["area:memory"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
