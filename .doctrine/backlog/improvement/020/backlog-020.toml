schema = "doctrine.backlog"
version = 1

id = 20
slug = "cordage-queryrs-traversal-triplication-reachablespine-pathextend-chains-diverged-walks"
title = "cordage query.rs traversal triplication: reachable/spine_path/extend_chains diverged walks"
kind = "improvement"
status = "open"
resolution = ""                 # seeded empty so `backlog edit` sets it in place
created = "2026-06-10"
updated = "2026-06-11"
tags = ["cordage", "reuse"]

[relationships]                 # outbound-only (ADR-004); seeded empty
slices = ["SL-036"]
specs  = []
drift  = []
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }
