# IMP-023: Rewire remaining review skills onto RV kind (/inquisition, /code-review, drift, reconciliation) beyond the one piloted in IMP-001

<!-- Backlog item body — context, detail, links. The structured, queried fields
     live in the sister `backlog-NNN.toml`; this prose is free-form and is never
     structurally parsed (the storage rule). -->

## Resolution (SL-061, 2026-06-14)

The two genuinely unstructured review skills — `/code-review` and
`/inquisition` — were rewired onto the RV ledger by SL-061 via a shared
`review-ledger.md` protocol (`/audit` was the IMP-001 pilot). The remaining two
aspects named in this item's title are owned elsewhere and are **not** part of
this closure:

- **drift** → IMP-022 (the Drift Ledger kind; a separate `drift` facet/kind, an
  ADR-007 carve-out).
- **reconciliation** seam → IMP-008 (the reconciliation-seam work).

With the review-skill rewiring complete, this item is resolved (done). Drift and
the reconciliation seam continue under IMP-022 / IMP-008.
