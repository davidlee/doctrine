schema = "doctrine.backlog"
version = 1

id = 95
slug = "record-supersede-migration"
title = "Migrate record Supersedes from typed [relationships] to [[relation]] + typed superseded_by carve-out (SL-097 follow-up)"
kind = "improvement"
status = "open"
resolution = ""
created = "2026-06-18"
updated = "2026-06-24"
tags = ["area:entity", "area:relations"]

# outbound-only (ADR-004). The cross-kind axes (slices/specs/drift) are uniform
# `[[relation]]` rows now (SL-048 "the cut") — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = [{ to = "SL-095", rank = 0 }]                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "slices"
target = "SL-097"
