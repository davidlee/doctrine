schema = "doctrine.backlog"
version = 1

id = 14
slug = "cross-verb-golden-harness-over-listingrs-shared-render-surface"
title = "Cross-verb golden harness over listing.rs shared render surface"
kind = "improvement"
status = "resolved"
resolution = "done"
created = "2026-06-10"
updated = "2026-06-15"
tags = ["area:cli", "deferred", "golden", "listing", "render", "risk", "testing", "trigger"]

[relationships]                 # outbound-only (ADR-004); seeded empty
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "references"
role = "concerns"
target = "PRD-011"

[value]
value = 1.0

[estimate]
lower = 2.0
upper = 4.0
