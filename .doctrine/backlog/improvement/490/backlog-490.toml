schema = "doctrine.backlog"
version = 1

id = 490
slug = "review-finding-index-on-show-list-target-rfc-032-0c-quick-win"
title = "review: finding index on show + list --target (RFC-032 0c quick win)"
kind = "improvement"
status = "closed"
resolution = "done"
created = "2026-09-26"
updated = "2026-09-26"
tags = ["area:cli", "area:render", "area:review", "cluster:review-system", "rfc-032"]

# outbound-only. Cross-kind structural edges (e.g. references with a role
# — `originates_from` for backlog→slice provenance — governed_by, related, drift) are
# uniform `[[relation]]` rows — author them with
# `doctrine link ITEM-NNN <label> <target>`; `doctrine inspect ITEM-NNN` renders
# them. The dep/seq axes below stay typed (they carry per-edge payloads).
[relationships]
needs    = []                   # item→item hard prerequisite
after    = []                   # item→item soft sequence: { to, rank? }
triggers = []                   # watched source globs: { globs, note? }

[[relation]]
label = "related"
target = "IMP-475"

[[relation]]
label = "related"
target = "RFC-032"
