# RFC-002: Consumption surfaces program: estimate, value, and scoring outward pipeline

## Context

RFC-001 establishes the thesis: doctrine's graph value is gated on **consumption
surfaces**, not on deeper internal modelling. The estimate/value authoring CLI
(SL-118) shipped the capture side; this RFC tracks the program to build the outward
surfaces that make that data matter.

**Why now.** SL-118 shipped `doctrine estimate set/clear` and `doctrine value
set/clear` CLI verbs. The facets (`[estimate]`, `[value]`, risk `[facet]`) exist in
the TOML model but nothing consumes them:
- No read path surfaces them (you can't *see* an item's estimate without opening its
  TOML)
- No scoring engine uses them to influence priority ordering (`survey`/`next`
  currently sort by raw inbound-reference count)
- No history tracking makes estimate drift observable (every `set` silently
  overwrites)

This is exactly the pattern RFC-001 names: the model exists, the capture exists,
but the **consumption surfaces** are missing. The cheapest, highest-leverage moves
are all outward.

Six items were surfaced during the 2026-06-21 session, scoped into slices, and
sequenced. This RFC is the program-level coordination artifact — it holds the
overall context, tracks progress, and maintains the big picture so neither context
drift nor parallel work loses coherence.

### Session origin

The items emerged from:
1. Re-reading RFC-001 and identifying concrete consumption-surface gaps
2. Noticing that IDE-013 (estimate/value history) and IMP-112 (show-path display)
   were created same day (2026-06-19) but unlinked and unconnected to the thesis
3. Identifying IMP-118 as the "real value" item — it makes estimate/value facets
   actually matter by feeding them into the priority scoring engine
4. Discovering gaps while dependency-mapping IMP-118: no risk facet CLI verb
   (IMP-132), tagging only on backlog items (IMP-134), CLI UX inconsistencies
   (IMP-133 / ISS-040)

A brief detour through ISS-008 (shared-target pollution flake) confirmed the
pattern: ISS-008 is a concrete motivating example of why estimate/value surfaces
matter for triage, but IMP-118 is the systemic fix.

## Relationship graph

### Thesis → program

```
RFC-001 (thesis) ──related──→ IDE-013, IMP-112, IMP-118, IMP-132, IMP-133, IMP-134, ISS-008
RFC-002 (program) ──related──→ RFC-001, SL-118, SL-132, SL-133, SL-134,
                               IMP-112, IMP-118, IMP-132, IMP-133, IMP-134, ISS-040
```

### Backlog → slice mapping

```
IMP-112 ──slices──→ SL-132 (show-path display) ⚡ done
IMP-118 ──slices──→ SL-133 (priority scoring)  ⚡ done
IMP-132 ──slices──→ SL-134 (risk facet CLI)    ⚡ done
IMP-134 ──slices──→ SL-136 (extend tagging)     ⚡ done
IMP-133 — no slice yet (research, deferred)
```

### Slice sequencing (soft `after` edges)

```
SL-133 (scoring) ──after──→ SL-132 (display)
SL-133 (scoring) ──after──→ SL-134 (risk CLI)
```

SL-132 and SL-134 are independent — no edge between them. Both completed
before SL-133 started. SL-133, SL-134, SL-132, and SL-136 are all done —
the scoring engine is live, risk CLI exists, show path displays
estimate/value, and tagging works across all entity types.

### Deferred

```
IDE-013 (history) ──after──→ IMP-112 (display)
```

IDE-013 is an **idea** (provisional/speculative). Its `after IMP-112` is a
soft intent: history tracking makes most sense once display exists.

## Program structure

### Stream A — Foundation (done)

| Item | Slice | What | Status |
|------|-------|------|--------|
| **IMP-134** | SL-136 | Extend tagging to all entity types | ⚡ done |
| **IMP-133** | — | CLI usability UX review (agent run) | deferred |

### Stream B — Facet tooling (done)

| Item | Slice | What | Status |
|------|-------|------|--------|
| **IMP-132** | SL-134 | Risk facet CLI verb — set/clear likelihood/impact | ⚡ done |
| **IMP-112** | SL-132 | Wire estimate/value display in show path | ⚡ done |

### Stream C — The tentpole (done)

| Item | Slice | What | Status |
|------|-------|------|--------|
| **IMP-118** | SL-133 | Multi-dimensional priority scoring for survey/next | ⚡ done |

Scoring engine is live: two-pass base+consequence, kind weights, risk dimension,
recursive needs-leverage, one-hop ref-optionality. `survey`/`next`/`explain` all
wired with score.

### Stream D — Scoring config & polish (done)

| Item | Slice | What | Status |
|------|-------|------|--------|
| **IMP-142** | SL-142 | Wire tag coefficients into scoring — `tags` on `EntityFacets`/`ScannedEntity`, `tag_coeff()` summed into `value_dim` | ⚡ done |
| **IMP-147** | — | Priority config per-field isolation for wrong-type values | ✅ resolved |
| (config CLI) | SL-146 | Inspect/modify `[priority]` coefficients, kind_weights, tag_coefficients from the CLI — shipped as `doctrine config get/set`, not a `priority` verb | ⚡ done |
| **B — seed** | — | Seed `[priority.tag_coefficients]` in `doctrine.toml` | ◇ optional, not done (no `[priority]` section; defaults neutral 1.0) |
| **IMP-021** | — | Filter/sort backlog by risk facet axes (likelihood/impact/exposure) | open, orthogonal — out of scope |

### Stream E — Remaining RFC-001 surfaces

RFC-002 originally tracked only **surface (d)** of RFC-001's five (the authored
priority slot / scoring pipeline). With A–D done, the program extends to umbrella
the four surfaces still unbuilt. None need new modelling — all are verbs/exports
over the model that exists.

| RFC-001 surface | Item(s) | What | State |
|-----------------|---------|------|-------|
| **(a) transitive impact** | SL-138 · IMP-120 · IMP-138 | `inspect --transitive` over the relation graph; cordage walk shipped (SL-140), verb wired | ⚡ **done** (SL-138) |
| **(e) unified doctor** | IMP-121 | single `doctrine doctor` health gate folding the integrity checks scattered across `spec validate` / `validate` / etc. | **next — start here** |
| **(c) graph interchange surface** | — | navigable relation-graph viz + JSON already shipped via `map serve` (`/api/graph`, server-side DOT→SVG, d3-dag actionability maps). Residual = static file-export (GraphML/Cypher/DOT-file) for external tools | ✅ **satisfied** (interactive); file-export demoted to on-demand |
| **(b) concept-map bridge** | IDE-015 | validate/drift-detect the human concept map against the real relation graph | unbuilt — speculative, last |

Sequencing: (a) **done**. (e) is now the next real surface — independent, scoped,
no minting needed. (c)'s high-value mode (navigable viz) is already live in
map/web, so it leaves the program; only static file-interchange remains, built
on-demand if an external-tool need appears. (b) depends on the concept-map
subsystem and is the most speculative — sequence last.

### Deferred (speculative)

| Item | What |
|------|------|
| **IDE-013** | Estimate/value change history (time-series of facet edits) |
| **IMP-133** | CLI usability UX review (agent-run research, not a slice) |

## Current state (2026-06-26)

**Headline: nearly done.** Three of RFC-001's five surfaces delivered —
(d) scoring pipeline, (a) transitive impact (SL-138 landed), (c) navigable
graph viz (already shipped via map/web). Remaining: (e) `doctrine doctor`
(IMP-121, **next**) and (b) concept-map bridge (IDE-015, speculative, last).
Surface (c)'s static file-export tail is demoted to on-demand.

### Surface (d) — scoring pipeline — complete

Streams A–D are all **done**. The estimate/value/scoring outward pipeline that
RFC-002 originally scoped is fully built and consumed:

| SL | Title | Status |
|----|-------|--------|
| SL-132 | Wire estimate/value display in show path | ⚡ done |
| SL-133 | Multi-dimensional priority scoring for survey/next | ⚡ done |
| SL-134 | Risk facet CLI verb — set/clear likelihood/impact | ⚡ done |
| SL-136 | Extend tagging to all entity types — generic cross-kind tag verb | ⚡ done |
| SL-142 | Wire tag coefficients into priority scoring | ⚡ done |
| SL-146 | Config coefficient CLI (`config get/set` for priority coefficients) | ⚡ done |

Scoring engine is live: `survey` orders by `actionability → score desc → id asc`,
`next` uses score-aware frontier ordering, `explain` decomposes into
value_dim/risk_dim/leverage/optionality/total, **tag coefficients now multiply
into `value_dim`** (SL-142), and `doctrine config get/set` edits the `[priority]`
coefficients without hand-editing TOML (SL-146). IMP-147 (wrong-type per-field
isolation) is **resolved**.

Only optional/deferred tails remain on surface (d): **B-seeding** (`doctrine.toml`
still has no `[priority]` section — defaults are neutral, so this is data-authoring,
not a gap), **IDE-013** (estimate history, speculative), **IMP-133** (CLI UX
research), **IMP-021** (backlog risk-facet sort, orthogonal).

### Program extension — surfaces (a)/(b)/(c)/(e)

The thesis is nearly done. RFC-002 has now delivered **three** of RFC-001's five
surfaces:
- **(d)** scoring pipeline — Streams A–D (complete).
- **(a)** transitive impact — **SL-138 landed** (`inspect --transitive`, relation-only,
  cordage `reachable_bounded` walk). IMP-120 and IMP-138 resolved on close.
- **(c)** graph interchange — the high-value *navigable* surface was already shipped
  under the map/web program (`map serve` + `/api/graph` JSON + server-side DOT→SVG +
  d3-dag actionability maps). RFC-001's "relation-graph interchange absent" note is
  **stale**. Only static external-tool file-export (GraphML/Cypher/DOT-file) remains,
  demoted to on-demand — not program work.

Two surfaces remain:
- **(e)** unified `doctrine doctor` (IMP-121) — **next**. Scoped, no slice yet,
  no minting needed.
- **(b)** concept-map ↔ relation-graph bridge (IDE-015) — speculative, last.

> **Scope note.** The RFC title still reads "estimate, value, and scoring outward
> pipeline" (surface d). With Stream E the RFC umbrellas the surfaces; the
> title is now narrower than scope. Left as-is pending a rename decision.

## Code terrain

Key files a fresh agent will need to navigate:

### Existing surfaces being extended

| File | Relevance |
|------|-----------|
| `src/priority/graph.rs` | Current graph build: `NodeAttr` (line 51), `consequence` pre-pass (line 162), `counts_toward_consequence` (line 106). SL-133 replaces the consequence tally with the two-pass scoring. |
| `src/priority/surface.rs` | `survey`/`next` sort integration (line 93: `consequence: u32`, line 140: `consequence desc` sort). SL-133 replaces `consequence desc` with `score desc`. |
| `src/priority/render.rs` | Display rendering for survey/next rows. |
| `src/relation.rs` | `RELATION_RULES` table (line 267) — source kinds, target kinds, writable labels. IMP-118's consequence post-pass walks `CONSEQUENCE_LABELS` (line 93). IMP-134 needs to add `sources` entries for tags across kinds. |
| `src/backlog.rs` | Risk `[facet]` model: `RiskFacet` (line 382), `exposure()` (line 507), `apply_tags` (line 1819). IMP-132 reuses the risk model; IMP-134 reuses the tag logic. |

### CLI change sites

| File | Relevance |
|------|-----------|
| `src/commands/relation.rs` | `link`/`unlink`/`needs`/`after` handlers — IMP-133 UX review targets these |
| `src/main.rs` | CLI subcommand dispatch — where new verbs (risk facet, tag) register |

### Config

| File | Relevance |
|------|-----------|
| `doctrine.toml` | IMP-118 adds `[priority]` section (coefficients, kind_weights, tag_coefficients, consequence). Precedent: `[conduct]` (src/slice.rs:443) and `[dispatch]` (src/dispatch_config.rs:29). |
| `.doctrine/templates/*.toml` | IMP-134 edits slice template to add `tags = []` |

### Data model

| File | Relevance |
|------|-----------|
| `.doctrine/backlog/improvement/118/backlog-118.md` | IMP-118 design body — read this first for the scoring formula |
| `.doctrine/backlog/idea/013/backlog-013.md` | IDE-013 open questions (governance, granularity, reader, wire shape) |
| `.doctrine/backlog/issue/040/backlog-040.md` | ISS-040 UX issue — read for IMP-133 context |
| `.doctrine/rfc/001/rfc-001.md` | RFC-001 thesis — strategic context |

## Dependencies (what already exists)

These exist and need no changes before the program starts:

| Piece | Source |
|-------|--------|
| `[value]` facet | `doctrine value set/clear` (SL-118) |
| `[estimate]` facet | `doctrine estimate set/clear` (SL-118) |
| Risk `[facet]` with `likelihood`/`impact` | `src/backlog.rs` — parses from risk TOML, `exposure()` = 1..=16 |
| `survey`, `next`, `explain` commands | Exist, sort by `score` (two-pass base+consequence) |
| `doctrine.toml` config sections | Precedent: `[conduct]`, `[dispatch]` |
| Relation engine | `RELATION_RULES`, `link`/`unlink`/`needs`/`after` CLI verbs |
| Tagging (all entity types) | `doctrine tag set/clear` — SL-136 |

## What is NOT in scope

- **IMP-021** — backlog risk-facet sort/filter is independent of this program's
  scoring-config track; it's a list-surface affordance, not a scoring concern.
- **IDE-013** — estimate/value change history remains deferred (speculative).
- **IMP-133** — CLI UX review is an agent-run research task, not a slice;
  its findings feed into individual CLI improvements.
- **Per-item priority overrides** — coefficients only, no escape hatch.
  A future dimension, not this program.
- **History tracking of coefficient changes** — `doctrine priority set` writes
  the current value; if drift tracking is wanted later it belongs in a
  separate slice.

## Next actions

Surfaces (d) and (a) are complete and (c)'s navigable surface already shipped via
map/web. Two surfaces remain; the program advances cheapest-first.

### E1 — surface (a): transitive impact query → SL-138 ⚡ DONE

Shipped. `inspect --transitive` walks the relation graph via cordage's
`reachable_bounded` (relation-only, ADR-001 — no `priority` up-call), grouped by
direction, depth-bounded, overlay-backed predicate table-derived. IMP-120 and
IMP-138 resolved on close.

### E2 (start here) — surface (e): unified `doctor` health gate (IMP-121)

One `doctrine doctor` that folds the integrity checks currently scattered across
`spec validate`, `validate`, `memory validate`, orphan/FK/relation-dangler scans,
etc. into a single CI-runnable "is the graph sound?" gate. No new checks — the
work is the aggregating surface + a clean exit-code contract.

**Entry point:** route IMP-121 to `/slice` (no governing slice yet), then `/design`.
Decide: which existing validators fold in, aggregate-vs-fail-fast, exit-code/output
contract (human + JSON), and whether `--fix` is in scope (recommend: report-only v1).

**Slice shape:** small–medium. Reuse the existing validator entry points; don't
re-implement their checks.

### E3 — surface (c): graph interchange — SATISFIED (interactive)

The high-value navigable surface is **already shipped** under the map/web program:
`map serve` exposes `/api/graph` (relation graph as JSON: nodes/edges/units),
server-side DOT→SVG render (`/api/dot/svg`), and a d3-dag actionability map
frontend (`web/map/`). RFC-001's "relation-graph interchange absent" is stale.

Residual = static external-tool **file-export** (GraphML / Cypher / DOT-file) for
piping into Gephi/Neo4j offline. **Demoted to on-demand** — not program work; mint
a backlog item only if a concrete external-tool need appears.

### E4 — surface (b): concept-map ↔ relation-graph bridge (IDE-015)

Validate/drift-detect the human concept map against the real relation graph. Most
speculative; depends on the concept-map subsystem. Sequence last.

### Optional / deferred (not blocking)

- **B-seeding** — author `[priority.tag_coefficients]` in `doctrine.toml` if/when
  project-meaningful coefficients are wanted; defaults are neutral so there is no
  forcing function. Use `doctrine config set` (SL-146), not hand-edit.
- **IMP-021** (backlog risk-facet sort), **IDE-013** (estimate history),
  **IMP-133** (CLI UX research) — orthogonal/speculative, run independently.

## Environment

- **Bwrap jail**: `/workspace` is the repo root, `~/.cargo/doctrine-target-jail/`
  is the shared CARGO_TARGET_DIR — per-worktree target dirs for dispatch workers.
- **Gate**: `just check` (fast, root package only), `just gate` (full, includes
  cordage workspace). Both run clean before committing.
- **Build**: `cargo build` writes to `~/.cargo/doctrine-target-jail/debug/`;
  `./target/debug/doctrine` is STALE.
- **Lint**: `cargo clippy` — no `--all-targets` (would trigger `unwrap_used`/
  `expect_used` denials in tests). `just gate` runs plain clippy.
- **Test**: `cargo test` (root package), `cargo test --workspace` (incl. cordage).
- **Format**: `cargo fmt`
- **Conventions**: 2-space indent, conventional commits (`feat(SL-NNN): …`,
  `fix(SL-NNN): …`). Commit to `main`. No stashing. No branch discards.

## Risk

- **Tag coefficients change all scores.** Once wired, any entity with tags
  will see a multiplier shift. If `tag_coefficients` defaults are absent
  (all 1.0), the term is neutral; but if the project seeds aggressive
  coefficients, ordering may shift non-obviously. Mitigation: land the
  code (A) before configuring meaningful values (B), so the delta is
  visible at each step.
- **`doctrine priority` CLI is TOML-editing.** It mutates `doctrine.toml`,
  which is a project-root config file. Corrupting it (e.g. concurrent write,
  encoding error) would break the project's boot and build. Mitigation: reuse
  the `toml_edit` pattern from `facet_write.rs` which is edit-preserving;
  validate before write; keep a single-writer lock (no concurrent priority
  CLI from dispatch workers).
- **IMP-021 is scope-creep adjacent.** It's a legitimate feature but
  orthogonal to the scoring-config track. Don't let it pull focus from
  closing the tag-coefficient seam.
- **IMP-147 is a genuine correctness gap.** A wrong-type value in `[priority]`
  today silently nukes the whole section. Small fix, easy to defer, but it's
  the kind of sharp edge that erodes trust in the config surface.

## References

- RFC-001 — Strategic thesis: graph value is gated on consumption surfaces
- SL-118 — Estimate/value authoring CLI verb (the foundation this program builds on)
- SPEC-001 D10 — Authored priority slot design
- REQ-054 (FR-006) — Priority scoring requirement
- PRD-011 OQ-001 — Priority scoring outcome question
- ISS-040 — Sequencing verb arg order too easy to reverse (concrete UX example)
- ISS-008 — Shared-target pollution flake (motivating example of why estimate/value surfaces matter)
- SL-142 — Wire tag coefficients into priority scoring (Stream D)
- SL-146 — Config coefficient CLI (Stream D)
- SL-138 — Relation-transitive walk for inspect (Stream E1, surface a — ⚡ done)
- IMP-120 — Transitive impact query on relation graph (surface a — resolved via SL-138)
- IMP-121 — Unified corpus health doctor verb (surface e — Stream E2, next)
- IDE-015 — Bridge concept map to relation graph (surface b — last, speculative)
- SL-072 / SL-103 / SL-091 — Map explorer web server (`map serve`, `/api/graph`,
  DOT→SVG, d3-dag actionability maps) — already delivers surface (c)'s navigable mode
