# RFC-005: Dispatch funnel integrity: hazard survey and correctness posture

## Context

The dispatch funnel — spawn workers on isolated worktrees, import their deltas
onto a coordination branch, verify, cut a review bundle, and integrate onto the
trunk — is the highest-throughput change path in Doctrine. It runs under
concurrent multi-agent load on a shared single clone. Correctness hazards in the
funnel are high-leverage: a silent wrong-base worker run, a phantom-index
integration revert, or a missing ledger commit each undermine the contract the
funnel offers.

This RFC surveys the open backlog items and memories tagged `area:dispatch` /
`area:worktree`, classifies the hazard landscape, and draws relationships between
them. It is a **governance-neutral deliberation** — it asserts no canon, makes no
decisions. Its purpose is to give a structured reading of the field so that
subsequent slices, ADRs, or revisions have a shared map.

## Survey: hazard classes and their relationships

### H1 — Base integrity: worker lands on the wrong (or moving) base

**The hazard.** Under multi-agent git-lock contention, the claude dispatch arm's
`isolation: worktree` silently falls back to the main worktree, where
`baseRef: "head"` then tracks a moving `main`. The worker executes on a wrong,
dirty, moving base instead of the coordination tip B.

**Evidence.** ISS-034 (closed, mitigated) is the canonical incident — 3
consecutive wrong-base spawns on SL-121 PHASE-02 while other agents advanced
`main`. The third attempt showed the worker's working directory **clobbered to
`main` mid-run** (first reads: correct isolated base `bbfc90bb`; moments later:
`63d96f90` on `main`, dirty with unrelated files, `merge-base --is-ancestor
bbfc90bb HEAD` → false).

**Current posture.** SL-123 hardened the post-run `verify-worker` belt:
`not-isolated` / `branch-mismatch` detection + pre-funnel footer gate now turn a
contention-induced fallback-to-main into a **loud, pre-import halt** — fail-closed,
not fail-safe. The isolation race itself is unchanged; throughput under churn is
zero (worker spawns, fails, must be retried or routed to the subprocess arm).

**Structural question.** Is fail-closed-at-post-run sufficient, or does the
wasted-worker-run residual (SL-123 §5.2 / OQ-2) justify the pre-run
fail-closability of IMP-072 (WorktreeCreate hook)?

**Sibling memory.** `mem.signpost.doctrine.dispatch-claude-arm-wrong-base`
codifies the base-guard pattern (grep prerequisite seams + assert clean tree +
STOP on mismatch) and the recommendation to prefer the subprocess arm under
active `main` churn.

**Related.**

| Id | Status | Role |
|----|--------|------|
| ISS-034 | closed (mitigated by SL-123) | Canonical incident: isolation race → wrong/moving base |
| ISS-031 | closed | Same outcome (worker on `main` not B) via jail `cd`-revert path |
| SL-123 | closed | Hardened post-run `verify-worker` belt — fail-closed, not fail-safe |
| IMP-072 | open | WorktreeCreate hook for pre-worker fail-closability (deferred) |
| `mem_019ee28ee9ee` | active | Claude dispatch arm wrong-base risk under shared-clone contention |
| `mem_019ec65ecbc7` | active | `isolation: worktree` forks Bash-cwd HEAD |
| `mem_019ec6142d3b` | active | `isolation: worktree` forks orchestrator session's local HEAD |

### H2 — Integration integrity: the trunk integration step has correctness hazards

**The hazard.** `dispatch sync --integrate --trunk refs/heads/main` advances the
trunk ref but operates against the **live shared checkout**, not a dedicated
clean worktree. Two distinct failure modes:

1. **Phantom index (ISS-038).** Integrate moves the `main` ref but does not sync
   the checkout — the index holds the slice's code files as **staged
   reverse-deletions** relative to the new HEAD. A subsequent unrelated commit
   (e.g. protecting audit artefacts) rides that stale index, committing the
   reverse-deletions, **silently reverting the integration**. Witnessed on SL-122
   close: slice code briefly vanished from `main`.

2. **Shared-trunk race (mem close-integrate).** The FF-only + expected-tip CAS
   races when another agent commits to `main` between `candidate create` and
   `sync --integrate`. FF refusal is correct but the agent is blocked until
   trunk stabilizes or a superseding candidate is created. The close driver
   cannot self-unblock when the dirty file is another agent's uncommitted work.

**Current posture.** ISS-038 is **open** — no fix, only the ISS-030 phantom
detector (which fires but gives no recovery procedure). The shared-trunk race is
documented in memory with the chaining workaround
(`create→admit→integrate` in one shell invocation to shrink the window).

**Structural question.** Should integrate operate via a dedicated clean worktree
or pure-ref CAS, never depending on shared checkout state? The ISS-038 phantom
is a mechanism defect, not a one-off — the integrate step depends on checkout
state that it does not control.

**Related.**

| Id | Status | Role |
|----|--------|------|
| ISS-038 | open | Phantom index silently reverts integration |
| ISS-030 | closed | Phantom-reverse-diff detector (fires, no recovery) |
| `mem_019ee41ac4c7` | active | Close-integrate on shared trunk races repeatedly |
| `mem_019ee3d7792b` | active | Clean exclusive trunk checkout or phantom index commits |
| RSK-010 | — | Stale dispatch base risk (same drive context) |

### H3 — Arm asymmetry: claude and subprocess arms have diverged

**The hazard.** The two dispatch arms (claude `isolation: worktree` vs subprocess
`doctrine worktree fork --worker`) differ in reliability, capability, and
failure modes. Several defects are arm-specific, creating an uneven correctness
surface:

- **Claude-only:** isolation race / wrong base (H1), `boundaries.toml` never
  committed to the dispatch branch (ISS-039), `WorktreeCreate` hook not yet
  wired (IMP-072).
- **Subprocess-only:** deterministic fork, no fallback-to-main, but codex/pi
  workers instead of claude.
- **Both arms:** gitignored build artifact provisioning gap (H4).

**ISS-039** is a concrete arm-specific defect: the claude arm writes
`boundaries.toml` to the live coordination worktree but never commits it to the
`dispatch/<slice>` branch. `read_ledger` sources from the branch object-db, so
`prepare-review` projects 0 phase cuts — per-phase review granularity is lost.
The subprocess arm presumably commits it (unconfirmed). Open, minor impact
(cumulative review bundle is unaffected), but a real defect for dispatched-slice
review ergonomics.

**Structural question.** Should the two arms converge on a single, deterministic
provisioning path (the subprocess model), or should the claude arm's unique
capabilities (in-harness workers) justify maintaining two paths with
arm-specific hardening?

**Related.**

| Id | Status | Role |
|----|--------|------|
| ISS-034 | closed | Claude arm isolation race |
| ISS-039 | open | Claude arm: boundaries.toml uncommitted |
| IMP-072 | open | WorktreeCreate hook (claude-only) |
| IMP-004 | open | Jail dispatch isolation spike: per-worktree target and bwrap confinement |
| `mem_019edf7cbf87` | active | Claude-arm dispatch: coordination worktree placement pattern |

### H4 — Worker provisioning: gitignored build artifacts absent from forks

**The hazard.** `doctrine worktree fork --worker` and `dispatch setup`
coordination worktree both provision only tracked files + the `.worktreeinclude`
allowlist. Gitignored build artifacts (`web/map/dist/` for RustEmbed,
`web/map/node_modules/` for JS lint) are absent. Consequences:

- **In worker forks:** `map_server` runtime tests fail (empty embed); `just gate`
  fails on `lint-js` (no eslint). Funnel verify inside the worker fork is
  unreliable.
- **In coordination tree:** `cargo build` fails on missing `web/map/dist/`
  (`RustEmbed` compile error), blocking the entire funnel verify step even for
  Rust-only deltas.

**Current posture.** CHR-017 (closed) addressed `node_modules` via the
skip-loud guard pattern (option A). The `web/map/dist/` gap is worked around by
copying the prebuilt artifact from the main worktree into the coordination tree
before verify. IDE-017 (open, idea) captures the latent gap: if
`.worktreeinclude` ever grows to include per-worktree-divergent untracked state,
the current stamp-provision mechanism (sourced from primary worktree) cannot
supply it.

**Structural question.** Is per-dep skip-loud guards sustainable, or do two
distinct gitignored targets (plus the general class) argue for a general
provisioning answer — symlink or copy gitignored deps into forks, as CHR-017
option B/B′ proposed?

**Related.**

| Id | Status | Role |
|----|--------|------|
| CHR-017 | closed | node_modules absent → lint-js fails; skip-loud guard |
| IDE-017 | open | Orchestrator-addressable provisioning when `.worktreeinclude` grows divergent state |
| `mem_019eec3285e4` | active | Worker fork omits gitignored embed → spurious test failures |
| `mem_019eeac33cf3` | active | Coordination worktree missing build artifacts → compile failure |
| `mem_019eacb95c39` | active | Gitignored-tier partition for worktree dispatch (ADR-006 D9) |

### H5 — Configuration and ergonomics

Lower severity, but collectively they represent friction in the dispatch UX:

| Id | Status | Title |
|----|--------|-------|
| IMP-126 | open | `[dispatch] trunk_preference` config overrides trunk-ladder freshness default |
| IMP-101 | open | `[dispatch] deliver_to` config field for single-source trunk delivery ref |
| IMP-127 | open | `dispatch candidate` ingest a hand-resolved 3-way merge conflict |
| IMP-103 | open | `dispatch sync --integrate --help`: clarify `--trunk` dry-run semantics |
| ISS-024 | open | Stray `.doctrine/slice/` dirs in worktree break corpus-scanner tests |

IMP-126 and IMP-101 likely share a config surface (single `[dispatch]` trunk-ref
source feeding both ladder selection and integrate delivery). IMP-127 is the
most correctness-adjacent of these: without it, any split-lineage close
conflict dead-ends the agent, forcing a direct-land workaround that forfeits the
candidate CAS integrity.

## Discussion

### The correctness perimeter

The dispatch funnel's correctness contract is: **worker runs on a stable base B,
delta is imported and verified, integration is atomic and lossless.** The four
hazard classes H1–H4 each attack one link in that chain:

```
spawn → [H1: wrong base] → execute → import → verify → [H4: missing artifacts]
→ [H3: arm asymmetry / missing ledger] → review → integrate → [H2: phantom revert]
```

Today, H1 is fail-closed (SL-123), H2 is open and unmitigated, H3 has an
open claude-specific defect (ISS-039), and H4 has tactical workarounds but no
general answer. The funnel is **operationally usable** (many slices have shipped
through it) but **not formally robust** — correctness depends on environmental
conditions (no concurrent `main` commits during integrate, build artifacts
pre-copied, the right arm chosen for the load).

### What's structural vs incidental

- **Structural.** The shared-single-clone model. Git's repo-global locks mean
  concurrent worktree operations contend. The claude arm's `isolation: worktree`
  + `baseRef: "head"` architecture makes it inherently vulnerable to `main`
  movement. The integrate step's dependence on live checkout state (not a
  dedicated clean worktree) is a mechanism choice, not an inherent limitation.
- **Incidental.** Specific missing commits (ISS-039 boundaries.toml), specific
  missing gitignored paths (`web/map/dist/`), specific config gaps. These are
  fixable one-by-one but the accumulation suggests the funnel's provisioning and
  ledger models need a systematic review.

### Tensions

1. **Claude arm capability vs reliability.** The claude arm gives in-harness
   workers (higher quality) but has unique failure modes (isolation race,
   missing ledger commits). The subprocess arm is deterministic but uses
   codex/pi workers. Is the right posture: harden the claude arm to subprocess
   reliability, or accept the asymmetry and route by load?

2. **Per-dep guards vs general provisioning.** CHR-017 chose skip-loud for
   `node_modules`. Two distinct gitignored targets now exist. Each new
   gitignored dep adds a guard. At what N does a general provisioning answer
   (`.worktreeinclude` symlink/copy) become cheaper than N guards?

3. **Post-run vs pre-run fail-closed.** SL-123's post-run belt catches wrong-base
   before import but after a wasted worker run. IMP-072's pre-run hook would
   abort before the first command. Is the wasted-run residual acceptable at
   current churn rates, or does it block throughput?

4. **Integrate checkout independence.** ISS-038 is the only open correctness
   hazard with no mitigation. The fix — integrate via dedicated clean worktree
   or pure-ref CAS — is a mechanism change, not a guard. Is it a priority, or
   is the low frequency of the trigger (concurrent dirty checkout at integrate
   time) acceptable?

## Outcome

Open. This RFC surveys and connects; it does not decide. The following questions
are offered for deliberation before any slice is cut:

- **OQ-1.** Is the dispatch funnel's current correctness posture (fail-closed on
  H1, unmitigated on H2, tactical on H4) acceptable for the project's current
  scale and concurrency, or does it need systematic hardening?

- **OQ-2.** Should the two dispatch arms converge on a single deterministic path
  (subprocess model), or is maintaining both with arm-specific hardening the
  right investment?

- **OQ-3.** At what point does per-dep skip-loud guarding (H4) give way to
  general gitignored-dep provisioning — and does IDE-017's orchestrator-
  addressable provisioning question change the answer?

- **OQ-4.** Is pre-worker fail-closability (IMP-072) worth the
  `WorktreeCreate` hook's σ blast-radius (fires for every `isolation: worktree`
  subagent in the repo), or is SL-123's post-run belt sufficient?

- **OQ-5.** Should integrate (H2) be refactored to operate checkout-independent
  (dedicated clean worktree or pure-ref CAS), or is a hardening of the
  existing path (pre-integrate dirty-check gate + defined recovery procedure)
  adequate?

## References

### Backlog items

| Id | Status | Hazard class |
|----|--------|--------------|
| ISS-034 | closed (mitigated) | H1 — isolation race / wrong base |
| ISS-038 | open | H2 — phantom index reverts integration |
| ISS-039 | open | H3 — boundaries.toml uncommitted (claude arm) |
| CHR-017 | closed | H4 — node_modules absent in forks |
| IDE-017 | open | H4 — orchestrator-addressable provisioning |
| IMP-072 | open | H1/H3 — WorktreeCreate pre-worker fail-closability |
| IMP-004 | open | H3 — jail dispatch isolation spike |
| IMP-126 | open | H5 — trunk_preference config |
| IMP-101 | open | H5 — deliver_to config |
| IMP-127 | open | H5 — hand-resolved merge conflict ingestion |
| IMP-103 | open | H5 — --trunk dry-run help text |
| ISS-024 | open | H5 — stray .doctrine/slice/ dirs break tests |

### Slices

| Id | Status | Role |
|----|--------|------|
| SL-123 | closed | Hardened post-run verify-worker belt (H1 mitigation) |
| SL-121 | closed | Slice under dispatch when ISS-034 surfaced |
| SL-122 | closed | Slice whose integration was silently reverted (ISS-038) |
| SL-125 | closed | Stamp provision source fix; IDE-017 origin |
| SL-127 | closed | Trunk ladder freshness; IMP-126 origin |
| SL-128 | closed | Un-rustfmt-clean due to missing lint-js; CHR-017 origin |

### Memories

| Key | Hazard class |
|-----|--------------|
| `mem.signpost.doctrine.dispatch-claude-arm-wrong-base` | H1 |
| `mem.pattern.dispatch.claude-isolation-worktree-forks-orchestrator-session-head` | H1 |
| `mem.pattern.dispatch.agent-worktree-forks-bash-cwd-head` | H1 |
| `mem.pattern.dispatch.close-integrate-shared-trunk-race` | H2 |
| `mem.pattern.dispatch.worker-fork-missing-gitignored-embed` | H4 |
| `mem.pattern.dispatch.coord-worktree-missing-build-artifacts` | H4 |
| `mem.concept.dispatch.gitignored-tier-partition` | H4 |
| `mem.pattern.dispatch.claude-arm-coord-placement` | H3 |
| `mem.system.dispatch.orchestrator-on-shared-main-contention-cost` | H2/H3 |
| `mem.pattern.dispatch.sync-tree-reads-ledger-not-worktree` | H3 |
| `mem.pattern.dispatch.review-branch-extraneous-deletions` | H3 |

### ADRs

| Id | Relevance |
|----|-----------|
| ADR-006 | Worktree posture: policy-agnostic framework, orchestrator-sole-writer dispatch |
| ADR-011 | Harness-agnostic orchestrator spawn interface and per-harness capability altitude |
| ADR-012 | Dispatch integration topology: isolated coordination worktree |

### Risk records

| Id | Relevance |
|----|-----------|
| RSK-010 | Stale dispatch base risk |
