# RFC-025: Discrete dispatch execution capsules

## Context

### Status

Living investigation document. Originally a working design synthesis from an
external (web) agent conversation, dropped in largely as-is on 2026-07-31;
now the coordination surface for the capsule programme — it inventories the
state of play and the next obvious actions (see § State of play), and links
the ledgered artifacts that carry the settled/open detail. Epistemic content
graduates out of this prose into knowledge records (QUE/DEC/EVD) as it
firms; governance (SPEC/ADR/REV) is written *from* those records, not from
this document.

This document consolidates the current investigation into simplifying Doctrine’s dispatch and confinement model. It records the direction agreed so far, distinguishes settled choices from longer-term targets, and identifies the next design questions.

### The microVM turn (2026-08-11) — read before § State of play

`RSK-231` called for an architectural revision rather than another remediation
slice, and this is it beginning. Everything below this subsection is still
accurate as a record of what was built and learned; what has changed is which
mechanism the programme expects to carry the confinement boundary, and what the
conformance suite is *for*.

**What happened.** `SL-252` was abandoned at design (`DEC-188`): every narrowing
of the fixture's readable set ran into the same production defect, `ISS-344`, and
the owner closed it out cheaply so the programme could be revisited
architecturally. In parallel, a Firecracker/microvm.nix spike
(`/workspace/microvm-spike`) reached a booting confined guest with a verified
fail-closed host perimeter in roughly ten hours — `EVD-015`, with its limits
attached and they are not optional.

**What is salvaged, and it is the epistemology rather than the mechanism.**

* `DEC-189` — **representativeness binds the row list, not only the readable
  set.** `DEC-185`'s bar generalises one level up. The suite already carries the
  rule, in `Unrowed`'s doc comment: *a row needs a delta that falsifies its
  reading; a condition no delta can change is not a test.* Under a hypervisor
  boundary four of the fourteen properties lose their host-facing delta, so the
  row **membership** does not port even though the taxonomy and the
  one-property-removed discipline (`DEC-156`) do. Transplanting them would
  rebuild `ISS-341`'s defect family on a new mechanism.
* `DEC-191` — **confinement assurance is a per-front posture, not a rank**, and
  the owner refused both of `QUE-211`'s candidates to get there. Equivalence is a
  fiction; so is ranking, because Seatbelt beats bubblewrap on some fronts and
  loses on others and the available complements differ by platform. The cut that
  makes this safe: **authority is an invariant floor** (`ADR-020`'s territory,
  unamended) while **confinement strength is a published profile**. `REQ-459`
  conflates the two in one property list and needs a `REV`; `IMP-405`'s rename
  folds into it.
* `DEC-190` — **the conformance suite splits** into a small backend-neutral
  verdict kernel and a per-mechanism payload layer. `conformance.rs` is 14,252
  lines; the bubblewrap payload layer is not migrated. This is the concrete
  answer to `RSK-231`'s third question, and the kernel is front-structured from
  the first line rather than retrofitted.

**Cheaper than it looks, in two places.** `BackendId` is deliberately open —
not a closed enum, no `Display`, `&'static str` so it cannot be built from
runtime text, *"because the contract must bind mechanisms nobody has written
yet"* (`DEC-156`). And `AdmissionVerdict` already carries every row including the
proven ones; the per-front vector is **already in the type**. What is wrong is
only the final AND-reduction to `Admitted`. The fix is to stop collapsing, not to
build a ranking apparatus.

**What `ADR-020` survives.** Checked against its text rather than assumed: its
step 2 launches the worker *"through a platform backend that enforces the same
authority properties"* — no mechanism named, and authority is the invariant axis.
`RSK-231` guessed ADR-020 was the decision most in question; it is not. `SPEC-030`
`REQ-459` is.

**Next move, and it is measurement rather than design.** `IMP-426` — an
evidence-not-product spike on the `SL-241` precedent, asking whether a
doctrine-driven microVM capsule can be **fresh per transaction** (`REQ-450`'s
five freshness axes) at tolerable cost. The spike is presently the opposite
shape: one persistent VM, a persistent `/work` volume, foreground build-then-exec,
and multi-capsule *"scoped, not started."* Conformance measures a capsule and
there is no per-transaction capsule yet, so this gates the rest. `NOTES.md` item
17 has already scoped it — the deciding cost is the guest image, and
netns-per-capsule is spiked and holds.

**The threat ranking, stated because it has never been stated** (`CPT-002`). A
capsule faces two threats of different shapes. **Escape** — the worker breaks the
boundary — is low-likelihood, adversarial, dramatic, and is what the property
suite measures. **Heresy** — the worker stays perfectly inside the boundary and
produces plausible, confident, wrong work — is high-likelihood, continuous,
present on ordinary days with no adversary, and is measured by almost nothing.
Confinement bounds the blast radius; governance bounds the intent.

The architecture already carries the defence: `ADR-020`'s *"local claims, process
exit, branch names, and prose are evidence to inspect, never admission
authority"*, built out by `FR-006`'s object-derived conformance and `FR-007`'s
separate fresh verification. What it has never carried is the **priority**, and
the attention asymmetry is the reason: confinement work yields a nineteen-row
green transcript, anti-heresy work yields a finding. `RSK-231` is the receipt —
six days on the wall, and the wall was never the headline threat. The ratio was
not chosen; it was set by which work was easier to see.

Two consequences for everything above. `DEC-191`'s fronts are **escape fronts**
and the profile must say so, or a reader takes a strong wall for a strong safety
claim. And a better wall raises inspection's share of residual risk rather than
lowering it — the microVM turn makes anti-heresy work *more* load-bearing, not
less. `SPEC-030` should carry this in § Overview or § Concerns, riding the same
`REV` that `REQ-459` already needs.

**Still open.** The front list is a sketch, not a closed enumeration. The
admission floor is unset, and "not ranked" must not become "nothing can fail" —
that would be `ISS-341`'s family a fourth time. Whether `DEC-191` wants an ADR of
its own is the owner's call. And whether bubblewrap-on-NixOS is a profile or is
simply unadmitted is undecided; `ISS-344` and `REQ-459` criterion 2's `partial`
stand either way until it is.

#### Where this stands, and what happens next

**Resume point: the spike, not this repo.** The round is directed by a
**disposable context packet**, `/workspace/microvm-spike/IMPORT.md` — probe list,
priority order, hazards, reporting bar. Untracked and deliberately so; it is
deleted when its findings graduate into knowledge records and back into
`IMP-426`. **P0 has now been probed there** (`EVD-016`, `EVD-017`); everything
below P0 is still unrun.

**The round** (`IMP-426`, widened from a freshness measurement on owner's
direction): import the most important unproven requirements into the spike and
**price** them there — *"costs about this much, here's the shape, here's what it
breaks"* — before the governance gears turn. Imports: `REQ-450` (freshness, five
axes), `REQ-454` (separate verification capsule — highest value per `CPT-002`),
`REQ-448` (authority floor), and `REQ-451`/`REQ-452` once the blocker settles.
Out of scope: the conformance suite, `REQ-455`–`REQ-458`, `REQ-461`, and
`DEC-191`'s front list.

**`QUE-212` — how does a result leave a shareless capsule? — is answered, and
`IMP-426`'s gate is clear.** `DEC-192` is `accepted`: the owner committed to the
inversion and to removing git-daemon. The framing that got it there was the
correction that `DEC-135`'s rule is about the repository used as **execution
context**, so the spike's defect was **direction, not format**.

**What the probes found** (`EVD-016`, n = 1, hand-run against the booted guest on
the current tap shape — *not* the netns design): host-initiated `git fetch` into
a fresh quarantine repo works (66,436 objects / 32.03 MiB at 96 MiB/s), **and so
does host-initiated push** into a guest repo seeded with
`receive.denyCurrentBranch=updateInstead`, unborn `HEAD` included. So provisioning
inverts too, and `DEC-192` deletes git-daemon from the perimeter — roughly 130
lines of `perimeter/` and `host/services.nix`, plus every capsule-specific
program in the guest. `EVD-017` supplies the argument that was not available when
the round opened: the shared mirror guest-push *requires* is group-writable in
`hooks/` and `config` while `capsule-sync` runs git as the human in it, so
confining the daemon uid closes the forward escalation and opens the reverse one.
The inversion deletes that precondition rather than defending it.

**Two corrections that must not travel.** *"The guest never initiates a
connection to the host at all"* is **false** — the proxy remains, is
guest-initiated, and tinyproxy is the larger C parser of the two. The claim that
holds is *the host runs no service that parses guest-authored Git input*, which
makes `REQ-448`'s third demonstration **absence of a git channel**, not absence
of a channel. And the host's refspec does **not** fully decide the destination
namespace: automatic tag following also wrote `refs/tags/*`, so `--no-tags` is
required before the unqualified form is true.

**Three residuals, none closed.** `index-pack` still parses hostile bytes
host-side and the receive-side ceiling goes away with the receive side —
`QUE-213`, now the blocker for `REQ-451`/`REQ-452`. A fetch leaves no artifact,
so a retained quarantine repo is the only `DEC-133` exhibit candidate; a
retention question, not a security one. And the agent loses `capsule-push`, which
is a workflow regression and the one thing the inversion makes worse.

**`ASM-010` bounds the option space** and is held, not measured: the host never
hands guest-authored filesystem metadata to the host kernel. `ro`/`nosuid`/
`nodev`/`noexec` do not mitigate a mount — the parse happens before they
constrain anything. The line falls at the kernel parser, not at inspection;
`fuse2fs`/`debugfs` are the sanctioned route and the spike already held that line
in `vm/capsule.nix`. A raw-offset read is named as the loophole, not the
workaround. Consequence for `DEC-133`: no host-side disk forensics, so a retained
quarantine repo is the only candidate exhibit.

**Governance is deliberately not moved yet.** The `REV` against `REQ-459` that
`DEC-191` requires — with `IMP-405`'s rename folded in, and `CPT-002`'s priority
into § Overview or § Concerns — waits until the round has priced what it is
revising. That ordering is the owner's, and it is the point: shape and price
cheaply in the spike before turning the heavy gears.

**Round records, all committed:** `DEC-189`, `DEC-190`, `DEC-191`, `DEC-192`
(accepted), `QUE-211`, `QUE-212` (answered), `QUE-213` (open, blocks P1d),
`ASM-010` (held), `CPT-002` (active), `EVD-015`, `EVD-016`, `EVD-017`
(captured), `IMP-426` (open, ungated).

**The namespace boot worked** (2026-08-11) — firecracker comes up with its tap
inside a capsule namespace on the host-module path, which was the one thing
`NOTES.md` item 11's design had reasoned to but not run. P1a and P1b are
unblocked; their *figures* are still unmeasured, which is what those imports are
for. The spike's `NOTES.md` is updated and `PLAN_C.md` is being reworked around
the result and around `DEC-192`'s deletion.

**Next actions, in order.** (1) P1a/P1b — the freshness axes and two concurrent
capsules, the two most expensive imports and the ones `CPT-002` ranks highest.
(2) `QUE-213` before P1d. (3) The git-daemon deletion `DEC-192` authorises is
spike-side work, sequenced by the spike's own plan, not by this RFC. Nothing here
moves governance; the `REV` against `REQ-459` still waits on the round, and that
ordering is unchanged.

### State of play (2026-08-10)

**Banked (committed alongside this RFC in `.doctrine/rfc/025/`):**

* `mechanism-census.md` — full-inventory verdicts over SPEC-012/021/022 per
  § Investigation method. Deletions cluster in identity/role choreography,
  mediation apparatus, and patch transport; SPEC-022's object/ref substrate
  survives nearly whole. The verdicts describe the capsule **target state**,
  not mechanisms already retired from the incumbent dispatch arms.
* `red-team.md` — adversarial pass. Its RT-1 finding, control-plane execution
  of hostile verification code, requires verification to run in a separate
  capsule. Its RT-2 finding, candidate-layer reuse, requires admission to ride
  the existing `candidate` verbs rather than a parallel implementation. Its
  RT-3 topology question is settled for v0: the interactive session persists
  in the control plane while capsule workers are headless and fresh-context
  per phase.
* `probe-specs.md` — mechanical experiment designs: P-C1 (capsule
  sufficiency + cost baseline), P-C2 (uniform confinement, scripted hostile
  probes), P-C3 (16-row hostile ingestion matrix, quarantine-fetch vs
  bundle side by side, expected kill boundary per row).
* `QUE-200` — the answered ingestion-mechanism question
  (next-design-question 1 below). `DEC-128` records why the spike could not
  settle its residual empirically; `DEC-135` chooses bundle ingestion under
  the structural rule that trusted control-plane code never runs Git in a
  capsule-authored repository.
* `SL-241` — the completed evidence-not-product spike. Its evidence directory
  and `go-no-go.md` carry the scoped verdict: **go** for the measured capsule
  ingestion and confinement model on Linux/bwrap and the tested client shape;
  not production readiness and not a portable macOS result.
* `DEC-133` — accepted separation between a durable trusted-side admission
  journal and short-horizon forensic exhibits that may expire. Retention,
  quotas, and project/slice/machine configuration remain deliberately
  unspecified and unprecluded.
* `DEC-134` — accepted v0 topology: the interactive orchestrator persists in
  the trusted control plane while every phase receives a headless,
  fresh-context worker in fresh mutable capsule state.
* `DEC-135` — accepted v0 result transport and implementation handoff: the
  worker publishes a Git bundle; the parent snapshots bounded hostile bytes
  into owned storage, imports and checks them in fresh quarantine, conforms
  and verifies the pinned result, then transfers and advances once under CAS.
* `QUE-201` — the answered interpretation-policy-home question. `DEC-136`
  places the required project-owned `[interpretation]` block in
  `.doctrine/doctrine.toml`, resolved from the contracted base; phase contracts
  may restrict it but never author, widen, replace, or weaken it.
* `QUE-207` — the answered control-plane topology question, and it no longer
  gates the decomposition below. `DEC-153` takes framing **B**: two binaries
  over shared code, split at canonical mutation. `doctrine-control` is that
  second binary, landed by `SL-248`.
* `SL-248` — the first implementation slice, **executed and audited**; ten
  phases `completed`, lifecycle at `reconcile`. It ships the capsule contract,
  provisioning, the Linux/bwrap backend and its nineteen-row conformance suite
  beside the incumbent worktree arms — additive and unused, per the
  no-half-cutover rule below. `RV-352` is the reconciliation audit: eight
  findings, all terminal, and its `## Reconciliation Brief` is what `/reconcile`
  executes. Its `## Synthesis` carries the reproduction evidence — two
  independent gate runs at `297 passed; 0 failed`, `backend verify` exit 0 with
  all fourteen properties and five axes `Proven`.
* `ADR-021` — `unsafe_code` `forbid` → `deny` with a **two-site budget**, both
  sites in the bubblewrap backend (the child's `RLIMIT_FSIZE` via `pre_exec`,
  and the `/proc/self/fd` sweep via `BorrowedFd::borrow_raw`). Neither has a
  safe route. `proposed`, and it wants acceptance: the ceiling is held by one
  test, which is deletable with no governance trace, and the ADR is that trace.

**Rulings and evidence now carried forward:**

* The capsule target uses uniform sandboxed subprocess workers (`claude -p` /
  codex / pi) rather than in-session subagents. This is a migration target:
  the incumbent Claude arm remains an in-session `Agent` worker identified by
  its disk marker and commits through the gated `worker_commit` tool until the
  capsule cutover. ADR-011 remains the incumbent authority meanwhile.
* FR-007/REQ-335 (confined-orchestrator mediated-write tier) is held
  pending; the capsule contract is its successor at the right boundary.
* Same-base parallel admission (next-design-question 8 below) must ride the
  existing candidate/CAS machinery. The spike found fresh-per-phase
  provisioning tolerable for this project; POL-002 requires client projects
  to measure their own costs rather than inheriting that local result.
* The evidence compares sixteen hazard rows across two ingestion mechanisms,
  but must never be summarized as “16/16”: fifteen rows reached model-level,
  the env-file row is unproven beyond the Rust fixture, structural `n/a` cells
  are not omissions, and four recorded `fail` rows are successful mutant
  detections. The real-agent phase is n = 1. The seven limits in
  `evidence/README.md` remain part of every claim.

**Next obvious actions:**

1. ADR-020 and SPEC-030 now carry the accepted target authority decision and the
   active forward-intent technical contract. SPEC-030 includes the concrete
   `[interpretation]` schema, bundle-snapshot ingestion, fresh verification,
   journaled admission, capsule-provenance candidate recovery, and frozen-source
   lifecycle as pending requirements. Both are in REV-046's touched set.
2. Implement SPEC-030 across a **sequence** of slices, not one. Reuse the existing
   Git ancestry, object-only merge, compare-and-swap, strict conformance, candidate
   identity, and admission seams; turn SL-241's hostile rows and stage assertions
   into production acceptance tests rather than migrating the disposable rig.
   Fourteen requirements in one slice extrapolated past both ends of this project's
   measured comfortable band (~1000 design lines over 6–9 phases): SL-233 ran 16
   phases on 781 design lines and left holes; SL-244 ran 8 phases on 3459. The
   working decomposition — still **provisional** below slice 1, but no longer
   waiting on `QUE-207`, which `DEC-153` answered:

   1. `SL-248` — capsule contract resolution, provisioning, Linux backend and its
      property suite, capacity advisory (REQ-449, REQ-450 partial, REQ-459, REQ-461).
      **Executed and audited; at `reconcile`.** Every SPEC-030 requirement is still
      `pending`: the slice's requirement closures are `/reconcile`'s to record, and
      `RV-352`'s brief holds REQ-459 criterion 2 to `partial` — the conformance
      suite exists, production acceptance tests do not.
   2. ingestion and trusted conformance (REQ-451, REQ-452, REQ-453). **Next to
      mint**, once `SL-248` reconciles and closes.
   3. verification capsule, normalization, journaled admission and CAS (REQ-454,
      REQ-455).
   4. recovery — capsule-provenance candidate adapter, freeze/fresh-repair, retention
      and exhibit lifecycle (REQ-456, REQ-457, REQ-458).
   5. cutover — the named compatibility boundary, skill and CLI collapse.

   Slices 2–5 stay **unminted** until their predecessor is designed; scoping slice 4
   before slice 2 exists repeats SL-233's failure at a coarser grain. Shipping is not
   cutting over: every slice but the last lands beside the incumbent worktree arms,
   additive and unused, so the repo never sits in a half-cutover state. REQ-448,
   REQ-450, and REQ-460 are cross-cutting and close in no single slice — each slice
   names its share in its own closure intent, or an invariant owned by all is proven
   by none.
3. Use that approved implementation design and plan to finish REV-046's remaining
   cutover work: name the compatibility boundary, disposition every incumbent
   requirement, preserve solo worktrees, and keep Linux evidence distinct from
   unselected macOS mechanism.

   **Sequencing corrected by `SL-248`'s execution.** REV-046 (`proposed`,
   `approval=none`) gates the **cutover** — slice 5 — not every implementation
   phase. `SL-248` shipped its whole surface without it, correctly, because a
   slice landing beside the incumbent arms revises no present-tense governance.
   Approve and apply REV-046 when there is an exact migration plan rather than an
   aspirational delete list, which is at slice 5 and not before.
4. `IMP-397` separately owns capsule egress allowlisting and build inputs that
   Git cannot carry, including the work represented by `QUE-204`.
5. **Close the follow-up register below before slice 2 starts.** Owner's
   direction at `SL-248`'s reconcile: not "read it" and not "discharge the
   programme-level half" — *close them all* before the real slice 2 begins, so
   slice 2 opens against a settled base rather than carrying another slice's
   tail. Two of its items bear on slice 2's shape rather than on `SL-248`'s
   tidiness, but that is now a reading order, not a filter.

**Follow-up register — what `SL-248` threw off:**

The live view is `doctrine backlog list --tag cluster:capsule`; `doctrine inspect
SL-248` lists everything that originates from the slice — **24 items, every one
`open`** at `SL-248`'s close, plus `ADR-021` `proposed`. Each carries a
`references --role originates_from SL-248` edge except `ADR-021`, which cannot:
the engine refuses that edge from an ADR, so its provenance is in prose.

Two of the 24 (`ISS-340`, `ISS-341`) were minted on `sl-248` and the six
reconcile-minted ones on `edge`, because entity ids allocate per worktree. The
union is intact once `sl-248` lands; `doctrine inspect SL-248` on either branch
alone is short.

The cut worth making here is between items that shape the **programme** and items
that are the slice's own hygiene.

*Programme-level — read these before slice 2 is designed:*

* `IMP-417` — **a third admission outcome for an unavailable backend.** The
  owner's ruling on `RV-352` `F-8`: `PHASE-10`'s `EX-14` forbids an availability
  condition, an `#[ignore]`, a skip or an early return reaching `Admitted`, and a
  runner that cannot satisfy the rows currently reds for a reason that is not a
  conformance defect. The chosen shape is a distinct non-`Admitted` verdict, the
  only one that keeps the answer inside the type `admission()` already returns.
  This affects every later slice's gate, not just `SL-248`'s. The load-bearing
  caveat is recorded on the item: **do not** fold the new outcome from row-level
  `Indeterminate`, or `ISS-334`'s contention race is silently reclassified as a
  host problem and the vacuous pass is rebuilt one level up.

  **`ISS-339` now feeds this, as intended.** Its first off-jail run produced a
  concrete probe member rather than a hypothetical one: *a readable root must
  cover the literal path a payload execs*. Note the shape — the existing guard
  asks `path_exists("/bin/sh")` and passes on the very host that cannot exec it,
  so the probe has to be **structural** (is `SHELL` within a derived readable
  root) and not existential. Take `ISS-339`'s run 2 before designing the probe
  set; each red run off a new host is another member, and the set is cheaper to
  fix in a type once than to extend one convict at a time.
* `ISS-339` — **the shipped suite had never run off-jail. It has now, once, and
  it was red.** `SL-241`'s off-jail work ran *spikes*, so what was confirmed on a
  real host was the mechanism rows 13 and 14 rest on, not those rows executing
  there. This is the cheapest evidence bearing on next-design-question 9
  (Linux/macOS equivalence), and it is one command — `just capsule-verify`, added
  by `SL-248`.

  **Run 1 (2026-08-10, NixOS host).** All nineteen rows
  `Indeterminate { NoLiveness }`, both `sec-3`/`sec-4` claims failed, from
  `bwrap: execvp /bin/sh: No such file or directory`. The suite derived its bind
  set from the shell's *resolved* path while payloads exec the *literal*
  `/bin/sh`; roots are identity-mapped, so a store-based host got `/nix` and no
  `/bin`. Fixed on `sl-248` (`8f837c312`). It had survived every in-jail run
  because the jail supplies `/bin` twice by accident — `/bin/sh` is a real file
  there, and `/bin` is on `PATH`.

  **Runs 2 and 3, same day.** Run 2 came back 17/19 — row 4 was a regression from
  run 1's own fix (binding `/bin` broke the invariant row 4's permitted-`/` set
  rested on), corrected in `2761d0378`. Run 3 is **18/19**, all four claims
  `Passed`, and both `sec-9` observations `Read` — with the `no_new_privs`
  reading now **caveat-free**, because off-jail the trusted side does not already
  hold the bit. That is `sec-9` residual 3's neighbourhood closed by measurement,
  which is the outcome this item was opened for and the one an in-jail run can
  never produce.

  **The item stays open** on the last row, `BoundedInputSet`. Its fix is not its
  own: see `ISS-341` immediately below.

* `ISS-341` → `ISS-340` — **the conformance fixture binds the whole Nix store
  into the capsules that prove `BoundedInputSet`, and no shipped row can convict
  it.** Chasing the last red row found the more serious thing behind it.
  `system_readable_roots` declares the *top-level ancestor* of every canonicalized
  `PATH` entry a readable root, identity-mapped — so `/nix`, plus (on an ordinary
  NixOS desktop) the host's entire `/run` and `/var`, because `/run/wrappers/…`
  and `/var/lib/flatpak/exports/bin` sit on `PATH`.

  `--ro-bind` is read-only, **not `noexec`**. Measured: 691 store paths visible,
  and `git`, `curl` (TLS-capable) and `gcc` all execute inside such a capsule.
  The junk roots are noise; `/nix` is the finding.

  **This is not the platform's posture — production explicitly refuses it.**
  `readable_set` binds only declared `readable-roots` plus `closure-roots`
  expanded through an operator-declared `closure-resolver`, and says so: *"no
  host-shaped default and no fallback … no arrangement of configuration can make
  a host-wide artefact store readable whole"* (`EX-8`). The fixture is exactly
  the host-shaped default `EX-8` rules out, and it hosts the capsules in which
  the bounded-input-set property is demonstrated. Row 3 reads planted decoys and
  is silent; row 4 derives its permitted-`/` set from `$PATH`, so every such root
  is permitted by the very fact that put it there. Both read `Proven`. The
  verdicts are not false — they are worth much less than they read, and nothing
  in the transcript says so.

  Direction: have the fixture declare `closure-roots` for its shell rather than a
  top-level ancestor, which is the shape `EX-8` already contemplates. The tension
  to resolve is that a resolver is host-shaped by nature and production escapes
  that by making it operator-declared — a fixture has no operator, and must not
  grow a Nix dependency (`POL-002`). Design question, not a patch.

  **Owner's call, 2026-08-10: this gets its own slice, not slice 2.** Slice 2
  (ingestion and trusted conformance) has enough to settle without absorbing a
  cleanup of this size, and the readable-input posture is a design question in
  its own right rather than overhead on someone else's scope. It does not block
  slice 2, and slice 2 does not block it; the ordering between them is free.

  **Minted as `SL-252` — *Conformance fixture readable-input posture*,
  `proposed`.** Scope authored at `SL-248`'s reconcile; next step is `/design`.
  Its non-goals are worth knowing before anyone widens it: production's
  `readable_set` is the reference, not a target; `EX-8` is the standard being
  applied, not a question; and `IMP-417`'s host-capability probe must stay
  separate, because an unavailable backend and an over-wide readable set are
  different facts.

  One edge is **owed after `sl-248` lands**: `SL-252` was minted on `edge` and
  `ISS-341` lives on `sl-248`, so `SL-252 references ISS-341 --role implements`
  cannot be authored until the branches meet.

  **The reconcile ruling is discharged — `REV-051`.** `ISS-341` was *also* owed to
  `SL-248`'s reconciliation as a second shortfall in `REQ-459` criterion 2, a
  separate obligation the backlog item never satisfied. `REV-051` records it:
  `REQ-459` moved `pending → in-progress` — not `active`, which would assert a
  closure criterion 2 does not support — and `SPEC-030` now states that until
  `ISS-341` is settled, the suite's rows are evidence about the *properties*, not
  about the environment production constrains.

  This is the third instance of one pattern, and the pattern is now the finding:
  **a derivation that is accidentally correct in the single environment it has
  ever run in.** `/bin/sh` (run 1), the permitted-`/` set (run 2), the root set
  itself (run 3).

  Sequencing, and it is not the obvious order: **`ISS-341` before `ISS-340`.**
  Narrowing the bind set so a root must bear an executable to earn its place
  makes `/run` and `/var` stop being roots, drops their entries from the inner
  `PATH`, and collapses row 2's coverage set to `/nix` — which is covered. The
  red row dissolves without the row being touched, and without taking a
  criterion-level decision against `PHASE-10` `EX-3`. Fixing `ISS-340` on its own
  terms first would weaken `EX-3`'s "from **every** bound path" *and* leave
  `ISS-341` standing with its only witness removed.
* `ISS-338` — `gid_map` has no off-jail reading. It is the mapping leg that
  discriminates on a uid-1000 host, and `SL-248`'s reconciliation has a live
  decision (`CAPSULE_UID`) resting on evidence that is one leg short.
* `IMP-416` — a **second** execution-timeout key, sized against an agent's tail
  rather than a build's. `sec-5`'s `900` derives from a Rust build fixture, but
  `DEC-134` makes what a capsule executes an *agent*; at 900 an agent is
  `SIGTERM`ed mid-phase. The design correction (scope `900` to build/verification
  execution) is `/reconcile`'s; the key is separate work.

* `IMP-419` — **declare a capsule uid no ordinary operator holds.** `CAPSULE_UID`
  is 1000, and so is the commonest operator uid, so two of row 13's four
  credential surfaces do not discriminate *on the design host itself*. Arm `A3`
  (`--uid 4242`) moves `uid`, `gid` **and** `uid_map`, so the surfaces are not
  inert — the chosen value is. Programme-level because it is a security assertion
  that currently passes for the wrong reason on the commonest host shape, and
  because any later backend inherits the declared identity. Sequenced `after`
  `ISS-338`, which is the leg its evidence is short of.

*Reconcile-minted, from `RV-352`'s deferred decisions (all `open`):*

* `IMP-420` — a non-refusing capacity verdict (`Low`, `Unknown`) has no return
  channel and reaches only a locked stderr, invisible to a non-terminal caller.
* `IMP-421` — move `HostDescriptor`'s derivation onto `HostFacts`. An engine unit
  reading `/proc` is a second door onto facts `host.rs` exists to own. Coupling,
  not correctness; the whole derivation already sits behind one function.
* `IMP-422` — rename row 9's test to match its mechanism, carrying `PHASE-10`
  `VT-2`'s mandate with it. The name claims a literal write through every
  readable mount; the mechanism deliberately does not, because on a real host
  that writes into the operator's `/nix`. `SL-248` corrected the design's wording
  and left the name, since `VT-2` makes it a verification floor.
* `IMP-423` — extract a slice-agnostic dispatch loop template from `LOOP.md`.
  Both branches edited it for different slices, `edge`'s for one since closed, so
  neither side is the future. This is the cure for which `SL-248`'s
  `scope-relevant` adjudication of `LOOP.md` is the symptom: a file every slice
  edits and no slice owns.
* `CHR-061` — move `CAPSULE_UID`/`CAPSULE_GID` to `backend.rs`, beside the other
  placement constants. Declared identity is a property of a placement, not of one
  backend's argv assembly. Do it before `IMP-419` and that one becomes a one-line
  edit in the right file.

*Slice-local hygiene — tracked, not programme-shaping:* `ISS-334` (row-B5
sibling tests load-fragile under contention), `ISS-335` (spike scripts inherit an
unpinned `PATH`), `ISS-336` (spike artefacts do not record their environment),
`ISS-337` (setup decoys stay inheritable across the arm), `ISS-323` (`sec-6`'s
`EXPORTED` constant omits `clock::today`). `ISS-335` and `ISS-336` are the same
class as `ISS-339` one level down — an artefact that does not carry its own
preconditions — and are worth fixing while the spike rig is still understood.

*Governance owed the owner:* `ADR-021` (`unsafe_code` `forbid` → `deny` with a
two-site budget) is `proposed` and wants acceptance. It carries no `references`
edge to `SL-248` because the engine refuses one from an ADR; the provenance is in
its prose.

*Doctrine-tooling items surfaced while running the slice*, unrelated to capsule
subject matter and deliberately not linked to this RFC: `IMP-412`, `IMP-418`,
`ISS-280`, `ISS-318`, `ISS-320`, `ISS-322`, `ISS-326`. `ISS-319` (fresh-id
allocation fails open when the trunk ref is unreachable) belongs here too — it
carries a `cluster:capsule` tag from where it was found, not from its subject.

*Standing capsule-cluster items not from `SL-248`, both newly relevant:*
`IMP-397` (action 4 above); and `IMP-404`, `SL-112`'s deferred engine/leaf crate
extraction — `DEC-153`'s two-binaries-over-shared-code answer is what makes the
shared half's crate boundary a decision rather than a tidy-up.

### Problem statement

Doctrine’s current dispatch machinery has accumulated complexity across several interacting concerns:

* parallel workers require independent mutable workspaces;
* implementation must be tightly confined and auditable;
* Claude Code subagents expose uneven and sometimes opaque lifecycle, identity, hook, worktree, and environment semantics;
* Git state must remain crash-safe, exact, reviewable, and resistant to accidental or hostile mutation;
* the workflow should preserve token efficiency and bounded autonomy without transferring excessive authority to agents;
* deterministic tooling has progressively absorbed workflow duties, but the remaining orchestration boundary is still broad and harness-specific.

Worktrees solve workspace independence efficiently, but they also couple workers through a shared Git repository and object database. That coupling makes workspace identity, worker privilege, base selection, ref ownership, marker semantics, hook behaviour, and integration recovery part of the same trust model.

The investigation therefore begins from a broader thesis:

> A worker should not be a privileged participant in Doctrine’s canonical Git topology. It should operate inside a disposable execution environment, while trusted control-plane machinery provisions work, observes results, verifies conformance, and performs canonical mutations through tightly constrained channels.

A raw clone per work thread is one possible implementation. The simplifying move is not the clone alone; it is the authority boundary around the execution capsule.

## Discussion

This is a discussion artifact, not a decision record or governance; it has no binding authority.

### Design priorities

Optimisation order:

1. correctness;
2. reduction of human exertion;
3. token efficiency;
4. wall-clock efficiency.

The initial design should prefer obvious correctness over cache reuse or low provisioning latency. Fast-but-ambiguous state is an unacceptable foundation. Once the model is correct, provisioning and execution can be accelerated without changing its trust semantics.

### Invariants to preserve

The migration should preserve the product and safety properties, not necessarily the current machinery.

#### Parallel execution

Multiple phases may execute concurrently from an explicit accepted base without observing or corrupting another phase’s mutable workspace.

The phase is the unit of parallelism.

#### Bounded authority

A capsule may have broad authority over its own disposable local environment, but it must not gain general authority over:

* the canonical control-plane repository;
* authored Doctrine state;
* integration refs;
* remote Git credentials;
* unrelated host files;
* network services not explicitly granted to the work contract.

Authority should be absent by construction where possible, rather than inferred from fragile process identity.

#### Declared-scope conformance

The existing selector model remains valuable:

* declared and changed;
* declared but unchanged;
* changed but undeclared.

That algebra should be applied before canonical acceptance and retained as audit evidence. Phase-level touch declarations should narrow the slice-level selector set.

#### Auditable evidence

Doctrine must retain enough evidence to reconstruct:

* the contracted base;
* the capsule and phase identity;
* the worker-produced final state;
* worker history and metadata where useful;
* the declared-versus-actual differential;
* verification and audit outcomes;
* the exact normalized candidate;
* the exact admitted object;
* the object ultimately integrated.

#### Crash-safe integration

A crash may leave a transaction incomplete, but not ambiguous. Recovery should mean replay, inspection, or abandonment, never reconstruction from undocumented mutable state.

#### Rigorous verification

Verification should run behavioural criteria against the exact candidate being considered for acceptance. Textual attestation is not sufficient evidence of behaviour.

#### Token efficiency

Agent conversational context may persist across phase transactions. Filesystem authority and mutable project state need not persist with it.

The design should allow a long-running interactive control-plane session to
orchestrate several phases without repeatedly re-ingesting the whole slice or
repository.

### Target authority model

#### Control plane

The control plane owns canonical truth and canonical mutation:

* provisioning;
* work contracts;
* accepted Git state;
* authored Doctrine state;
* result harvesting;
* conformance checks;
* candidate normalization;
* verification and audit records;
* admission;
* CAS-gated integration;
* capsule lifecycle and garbage collection.

#### Control-plane orchestrator

The interactive orchestrator remains on the trusted control-plane side and may
retain conversational context across phases. It chooses work, provisions a
fresh capsule worker, inspects trusted-side results, and handles user
interaction and escalation. The capsule model bounds worker authority; it does
not reduce the orchestrator's existing control-plane authority.

#### Capsule worker

Within its capsule, the headless worker may have relatively broad local authority. It may:

* inspect and edit the local checkout;
* run builds and tests;
* manage local commits and temporary branches;
* write nominated coordination, escalation, result, and audit artifacts.

It cannot directly mutate the canonical control-plane repository. Cross-boundary operations occur only through a narrow Doctrine interface or through the control plane inspecting the capsule.

The worker's process ends at the phase boundary. Conversational persistence
belongs to the control-plane orchestrator, not to mutable capsule state.

#### Worker identity and internal structure

The capsule process and its OS boundary identify the worker. The target model
does not use an on-disk worker marker, harness-carried subagent identity, or
in-capsule role tiers to confer canonical authority: the capsule has none.
Internal subprocesses share the capsule's authority boundary. If a future use
case needs finer in-capsule roles, that is a new design rather than retained
marker choreography.

## Worker launch and configuration direction

The control plane launches one headless worker process inside a prepared
capsule. The worker receives deterministic, harness-appropriate configuration
containing:

* confinement-appropriate hooks;
* worker-specific tool policy;
* Doctrine MCP or binary interfaces appropriate to the capsule;
* the phase's distilled contract and required plugins;
* workspace and base assertions.

The security-bearing configuration should not depend solely on a mutable plugin marketplace installation or on subagent-writable files. Prefer a deterministic launcher and externally protected hook implementation, for example:

```text
doctrine dispatch launch SL-123 PHASE-01
  -> provision capsule
  -> validate base and configuration
  -> enter OS sandbox
  -> exec <worker-binary> <headless-args>
```

Hooks and plugins remain harness integration mechanisms, not the sole hard
security boundary. Control-plane separation, credential refusal, OS sandboxing,
and trusted-side acceptance remain the stronger floor.

This target does not rewrite the incumbent in place. Until capsule dispatch
lands, the Claude arm continues to use ADR-011's in-session `Agent`, disk marker,
and gated `worker_commit`; codex/pi continue to use the subprocess arm. The
marker and gate become deletion candidates only at the capsule cutover.

### Cross-boundary communication

The capsule should not push authoritative state into the control plane.

#### Notification channel

The outbound capsule-to-control-plane channel is a doorbell, not a data channel.

A notification may indicate only:

* result ready;
* checkpoint reached;
* escalation requested;
* user attention required;
* nominated capsule state changed.

The payload should be nil or minimal. The control plane already knows the capsule identity and location, and reads authoritative artifacts or Git state directly.

This can begin as a webhook-like callback, signal, FIFO, socket message, or narrow Doctrine command. Its semantics should remain: “inspect the nominated capsule” rather than “trust this supplied result.”

#### Coordination artifacts

For the initial design, coordination state lives inside the capsule and is harvested by the control plane.

Use a small, disciplined set of machine-readable artifacts, shaped as future protocol messages even before a broker exists:

```text
contract.toml
status.toml
escalation.toml
result.toml
audit/
```

Use atomic replacement. Treat these as explicit state snapshots or append-only evidence, not an informal scratch directory.

This allows a later move to a control-plane ledger or broker without changing the conceptual contract.

### Capsule and phase lifecycle

#### Identity and reuse

Capsules are associated with a slice, but phases are execution and transaction
boundaries.

* v0 provisions fresh mutable capsule state for every phase.
* Serial phases retain the control-plane orchestrator's context, not the prior
  worker's process or mutable filesystem.
* Parallel execution fans out across independent capsules at the same accepted
  base.
* There are no different capsule species such as “primary capsule” and “phase
  capsule.”
* Every phase result is harvested at its boundary.
* A harvested capsule is frozen. It remains live work until its result, or an
  incorporating repair, is integrated and formally closed; v0 never deletes it
  automatically.
* Formal repair is a new transaction in a fresh capsule. The source capsule is
  evidence and recovery input, not a persistent worker environment.

Reusable immutable provisioning layers or caches may later reduce cost without
reusing phase-local mutable state. Mid-phase mutation of canonical state is not
required.

#### Correct target model

The stronger long-term model distinguishes:

1. a clean, verified provisioning state at an accepted base;
2. disposable phase execution state derived from that provisioning state;
3. a frozen harvested state while admission or repair remains live; and
4. complete teardown of phase-local mutable state only after the explicit
   integration-and-closure cleanup checkpoint.

On Linux this may eventually use overlayfs, btrfs snapshots, reflinks, or another filesystem snapshot mechanism. Similar semantics may be implemented differently on macOS.

#### v0 lifecycle

The initial implementation should use the simplest correct model:

1. create a fresh capsule from the accepted canonical commit;
2. provision files from an explicit manifest;
3. perform a cold baseline build and verification;
4. execute one phase transaction;
5. harvest the result and evidence;
6. verify the exact candidate inside a separate verification capsule;
7. accept and normalize the result in the control plane;
8. record the durable admission result;
9. if admission is stale or conflicted, freeze the source capsule and create
   any formal repair as a fresh capsule transaction;
10. after incorporating integration and formal closure, make the frozen capsule
    explicitly cleanup-eligible under the applicable forensic policy; and
11. provision each dependent phase from the newly accepted commit.

### Provisioning and filesystem hygiene

#### Provisioning source

Provisioning must not depend on copying arbitrary mutable state from the control-plane checkout. That is vulnerable to contamination and does not provide a reproducible base.

The provisioning input should be exactly:

```text
accepted Git tree
+ explicit provisioning manifest
+ immutable or independently managed toolchain/runtime inputs
```

The current `.worktreeinclude` mechanism provides a useful conceptual seed: selected non-Git files are named with gitignore-style paths and explicitly copied. It should evolve into a capsule provisioning manifest with clearer ownership and mutability semantics.

#### Hermetic phase boundary

Anything gitignored and not explicitly readonly is a possible mutable-state vector.

The desired reset semantics are:

> After a phase, nothing survives unless it belongs to the accepted Git tree, the provisioning manifest, or nominated harvest and coordination evidence.

Do not attempt to classify project caches as “safe across edits” versus “affected by this phase.” That distinction is likely brittle and language-specific. Preserve only caches that are demonstrably outside the mutable project state, or accept recomputation.

For v0, cold builds are acceptable.

### Accepted result and Git normalization

#### Worker freedom

Inside the capsule, the agent may commit as often as useful. It may use temporary branches, fixups, or checkpoint commits according to local working needs.

The control plane does not treat that commit topology as canonical project history.

#### Normative result

The authoritative phase result is the final accepted tree delta relative to the contracted base.

The control plane:

1. reads the capsule’s base and final Git state;
2. validates ancestry and contracted base;
3. computes the actual changed paths;
4. applies selector and phase touch-set conformance;
5. rejects governance or configuration escapes;
6. provisions a separate verification capsule and runs verification against
   the exact result;
7. normalizes the accepted result according to project and Doctrine policy;
8. creates the canonical phase commit or candidate object itself.

Doctrine’s current preference for one source-delta commit per phase remains a sensible default. Normalization may later become configurable according to project conventions.

#### Live work and forensic evidence

The durable admission journal, a pending capsule, and a forensic exhibit have
different lifecycles (`DEC-133`, refined by `DEC-137`).

The trusted-side journal retains the accepted base and candidate identities,
hashes, verdicts, archive references, and the lifecycle state of referenced
exhibits. It remains truthful after an exhibit expires.

A harvested capsule whose result has not been integrated and formally closed is
**live work**, not an expiring forensic exhibit. V0 freezes and retains the
original capsule as its rescue payload. A superseding attempt does not make it
disposable. Formal repair runs in a fresh capsule, and cleanup is an explicit
operator checkpoint; no automated path destroys unresolved work.

Once an incorporating result is integrated and closed, the frozen capsule and
its bundle cross into the forensic-exhibit lifecycle. An operator may also
explicitly abandon and clean up a result.

While retained, a forensic exhibit may include:

* capsule Git bundle or equivalent object archive;
* worker commit history;
* result manifest;
* tool and model metadata;
* audit findings;
* verification logs;
* declared and actual path sets;
* timestamps and capsule identity;
* relevant transcript or structured reasoning artifacts where permitted.

Post-closure forensic exhibits are ordinarily short-horizon operational material: usually
low-value after routine work, but highly valuable when an incident or surprising
result must be reconstructed. They therefore live behind a separately-owned
archive boundary and may expire; they are not ordinary authored project content
by default and do not carry the durable admission claim alone.

v0 does not prescribe a retention duration, quota, or project-, slice-, or
machine-level configuration hierarchy. Those controls are neither required nor
precluded and may be introduced when operational evidence identifies a real
owner. V0 uses a simple configurable free-space warning and manual intervention
rather than reservation, throughput backpressure, or automatic eviction. Expiry
must be explicit in the journal rather than silent disappearance.

### Git boundary

The current Git model contains valuable principles that should survive even if its topology is reduced:

* immutable object identity;
* explicit base pinning;
* exact candidate and admitted OIDs;
* no integration from a drifting live branch tip;
* journal-before-canonical-mutation;
* idempotent replay;
* expected-tip CAS;
* report-and-halt rather than auto-resolve;
* audit before admission.

The capsule should not write shared refs or canonical Git metadata.

SL-241 tested parent-side fetch into quarantine and Git bundle ingestion side by
side. They were equal on the measured trust-bearing axes; the bundle adds a file
refusal boundary, while fetch still touches hostile repository configuration and
that surface was sampled rather than universally cleared. Final-tree
materialization was not rigged and forfeits worker-history forensics by
construction. `DEC-128` therefore ruled the residual architectural rather than
evidential. `DEC-135` answers `QUE-200` for v0 with bundle ingestion: the parent
accepts a bounded hostile file boundary so it never runs Git inside the
capsule-authored repository.

Canonical integration should operate on a normalized, control-plane-owned candidate. The capsule’s local refs are evidence, not authority.

### v0 architectural sketch

```text
                         TRUSTED CONTROL PLANE
+------------------------------------------------------------------+
| canonical repository and authored Doctrine state                 |
| persistent interactive orchestrator                              |
| accepted base and work-contract registry                         |
| capsule provisioning and lifecycle                               |
| result harvesting, conformance, verification orchestration       |
| candidate/admission evidence                                     |
| CAS-gated integration                                            |
+-------------------------------+----------------------------------+
                                |
                                | provision explicit base + manifest
                                v
                         EXECUTION CAPSULE
+------------------------------------------------------------------+
| fresh local clone                                                |
| deterministic harness-specific worker configuration              |
| one headless fresh-context worker process                        |
| broad local authority inside the OS boundary                     |
| no canonical repository authority                               |
| no push credentials                                              |
| local commits, builds, tests and coordination artifacts          |
+-------------------------------+----------------------------------+
                                |
                                | nil/minimal notification
                                v
                         CONTROL-PLANE HARVEST
+------------------------------------------------------------------+
| inspect nominated capsule                                        |
| journal admission identities, hashes and verdicts                |
| retain/expire forensic exhibit under separate policy             |
| compute final tree delta                                         |
| enforce selectors and forbidden paths                           |
| verify exact result in a separate capsule                        |
| normalize canonical phase commit                                |
| audit, admit, integrate under CAS                                |
+------------------------------------------------------------------+
```


#### Migration path

#### 1. Define the capsule contract

Introduce a harness-neutral abstraction:

```text
provision(contract)
launch(capsule)
notify(capsule, event)
inspect(capsule)
harvest(capsule)
verify(contract, result)
normalize(result)
gc(capsule)
```

Initially adapt the existing subprocess and worktree implementation where useful, but keep the contract independent of worktrees.

#### 2. Establish uniform headless subprocess workers

Prove that the control plane can launch each supported harness as a headless
worker process in a prepared capsule with:

* the intended settings and plugin set;
* an explicit process identity and OS confinement boundary;
* the narrow Doctrine interface;
* no unintended control-plane authority.

This is the cutover that allows the current Claude-specific disk-marker,
SubagentStart-hook, and gated-commit choreography to retire. Until it lands,
those incumbent mechanisms remain authoritative rather than being deleted by
prose.

#### 3. Add raw-clone capsule provisioning

Provision selected phases from explicit accepted commits into independent clones. Remove remotes or credentials as required. Keep the existing downstream candidate and integration machinery temporarily.

#### 4. Move acceptance to parent-side harvesting

Stop treating worker self-commits or worker-produced patches as canonical. The
control plane computes and validates the final result, records the durable
admission facts, retains forensic exhibits according to policy, and creates the
normalized candidate.

#### 5. Rebuild from canonical accepted state between dependent phases

Use fresh provisioning after each accepted phase. Accept cold builds until correctness and reproducibility are established.

#### 6. Collapse obsolete coordination topology

Once workers no longer participate in the canonical repository, reassess:

* persistent `dispatch/*` refs;
* coordination worktrees;
* worker markers as identity;
* base-by-placement;
* worktree import and landing verbs;
* mandatory per-phase evidence refs;
* candidate repair propagation choreography;
* harness-specific implementation altitudes.

Retain only mechanisms that still protect an invariant in the new model.

#### 7. Optimise provisioning without changing semantics

Investigate:

* overlayfs;
* btrfs or ZFS snapshots;
* reflink copies;
* Git alternates or shared readonly object stores;
* Nix-built immutable runtime layers;
* container or VM backends;
* remote execution.

These should accelerate the already-correct capsule model, not redefine its trust boundary.

## Outcome

### Investigation method

SL-241 completed the bounded spike described here. For every current dispatch
mechanism, the census recorded:

```text
mechanism
property protected
failure prevented
whether the failure still exists with capsules
replacement mechanism
```

This avoids migrating complexity whose only purpose was defending against shared-worktree hazards.

The local investigation exercised the following hazard classes through the
probe matrix, guards, measurements, and one real-agent phase:

* subordinate writes to `.doctrine/` or `.claude/`;
* hook failure or removal;
* wrong base;
* rewritten local history;
* multiple or merge commits;
* undeclared paths;
* attempted push or credential access;
* capsule termination mid-phase;
* parent crash during harvest;
* trunk movement before admission;
* parallel disjoint phases;
* parallel conflicting phases;
* tester and implementer ownership violations;
* repaired candidate normalization;
* notification loss or duplication;
* stale coordination artifacts;
* baseline build failure.

It measured:

* trust-bearing lifecycle states;
* mutable refs;
* harness-specific branches;
* hooks with security significance;
* role-detection rules;
* crash-recovery procedures;
* Git operations between completion and candidate creation;
* token consumption per accepted phase where the harness exposed it;
* human interventions;
* wall-clock and disk cost as secondary metrics.

The authoritative result is `.doctrine/rfc/025/evidence/results-c3.tsv`, read
through the interpretation rules in `evidence/guards.md`; the narrative verdict
and its seven mandatory limits are in `evidence/README.md`, and
`go-no-go.md` states the scope boundary. The evidence is Linux/bwrap-only, one
real agent run, and not a production-readiness assessment.

### Settled direction so far

1. Use execution capsules as the main abstraction; clone-per-thread is an implementation candidate.
2. Keep the persistent interactive orchestrator in the trusted control plane;
   give each headless capsule worker broad local authority but no control-plane
   authority.
3. Push canonical mutation behind a narrow Doctrine boundary and gradually absorb orchestration duties into Doctrine.
4. Use outbound capsule calls mainly as payload-free notifications; the control plane inspects authoritative capsule state.
5. Keep coordination and result artifacts capsule-local initially, but structure them for eventual ledger or broker ingestion.
6. Associate capsules with a slice while treating every phase as a fresh
   execution transaction.
7. Reuse immutable provisioning layers or caches where useful, but not mutable
   phase execution state. Freeze every harvested source capsule; make it
   cleanup-eligible only after incorporating integration and closure.
8. Prefer fresh provisioning from canonical accepted Git state after each accepted dependent phase.
9. Begin with cold builds and correctness; optimise provisioning later.
10. Do not attempt fine-grained cache correctness classification.
11. Let workers commit freely inside the capsule.
12. Normalize the accepted final tree into canonical project history on the trusted side.
13. Preserve admission identities, hashes and verdicts durably. Treat a frozen
    unresolved capsule as live work under DEC-137; only after closure do its
    worker history, logs and metadata become separately-lived forensic exhibits
    that may expire under policy (DEC-133).
14. Preserve immutable OID, explicit admission, journal and CAS principles from the existing Git model.
15. Run candidate verification in a separate capsule; never execute a hostile
    candidate's verification code directly in the trusted control plane.
16. Treat uniform subprocess workers as the capsule target, not as a claim that
    the incumbent marker-based Claude arm has already been replaced.

### Resolved implementation handoff and next design questions

Continue breadth-first. Question 1 is retained in place because later records
cite its document-local number; it now carries the settled implementation
handoff. The remaining entries are unresolved unless their text says otherwise.

1. **Answered by `DEC-135`: Git bundle under a no-trusted-Git-in-hostile-repository invariant.**

   * The worker publishes a bundle at a control-plane-selected location.
   * The parent snapshots bounded hostile bytes into owned storage, refusing
     unsafe paths and incomplete or invalid artifacts.
   * A fresh quarantine repository verifies and imports the bundle, checks
     object integrity and pins the result identity.
   * Trusted conformance runs over quarantine objects; behavioural verification
     runs against the pinned identity in a separate verification capsule.
   * Only after every gate passes does the parent precheck the accepted base,
     transfer the pinned objects and make one CAS ref advance.
   * A durable admission journal survives; the bundle is a separately-lived
     forensic exhibit under `DEC-133`.
   * Product work reuses existing Git/conformance/admission seams and promotes
     `SL-241`'s hostile rows into acceptance tests. No further feasibility probe
     is required for ingestion.

2. **What exactly belongs in the provisioning manifest?**

   * copied mutable files;
   * generated configuration;
   * readonly contextual material;
   * secrets or credentials, if any.

3. **How should work contracts express tool, network, credential, path and verification policy without becoming a generic workflow language?**

4. **Which immutable provisioning-generation or cache state can be reused
   without carrying mutable phase execution state across transactions?**

5. **How should baseline verification be inherited by capsules derived from the same provisioning generation?**

6. **Which current refs remain useful as human-facing evidence views after the ledger and candidate OIDs become authoritative?**

7. **Answered by `DEC-137`: freeze the source; repair in a fresh capsule.**

   * The harvested source capsule remains intact as live work.
   * Formal repair is a new transaction in a fresh capsule based on the current
     accepted commit; the original mutable environment is never resumed.
   * The source becomes cleanup-eligible only after incorporating integration
     and formal closure, or explicit operator abandonment.

8. **Answered for same-base admission by `DEC-137`: serialize at the accepted
   ref and use explicit candidates.**

   * The first result may advance the accepted ref.
   * A stale second result is classified by the existing object-only candidate
     engine against the current accepted commit.
   * Clean composition creates a candidate; conflict records `Conflicted` and
     halts; every repair is a new candidate transaction.

9. **How should Linux and macOS provide equivalent security properties without requiring identical sandbox technology?**

10. **Answered by `DEC-137`: reuse candidate semantics through a new provenance
    seam.** The capsule admission layer supplies contracted base, pinned source,
    current accepted commit, and verification attestation explicitly instead of
    manufacturing the incumbent coordination-journal precondition. Candidate
    classification, immutable admission pinning, journal-before-mutation and
    expected-tip integration remain one state machine.

11. **Answered by `DEC-136`:** `QUE-201`, the client interpretation-surface
    declaration question, places required project-owned policy in the
    `[interpretation]` block of `.doctrine/doctrine.toml`. Phase contracts may
    consume and restrict the base-resolved policy, never author or weaken it.

12. **What is the smallest production slice that realizes the measured
    authority model without claiming the rest of the dispatch migration?**

13. **Answered by `DEC-153` (via `QUE-207`): framing B — two binaries over
    shared code, split at canonical mutation.** Not one binary with
    environmental privilege, which would have reproduced the
    inferred-from-process-identity pattern this programme exists to retire.
    `doctrine-control` is the second binary and `SL-248` landed it.

    The coupling to question 2 survives the answer and is now a live
    obligation: "absence as authority" holds only if the provisioning manifest
    reliably omits the control binary. `IMP-404` (engine/leaf crate extraction)
    is where the shared half's boundary gets decided.

### Source context

This synthesis originated in an external web-agent conversation (2026-07) and
was initially dropped in with incomplete per-question adjudication. The
mechanism census, red-team pass, SL-241 evidence, and durable knowledge records
now supersede recollection where they disagree. The original conversation
remains source context, not authority.

## References

Companion artifacts (this directory): `mechanism-census.md`, `red-team.md`,
`probe-specs.md`.

Entities: `QUE-200` (ingestion mechanism — question 1, answered); `DEC-135`
(bundle choice and implementation handoff); `QUE-202` (same-base admission,
answered); `DEC-137` (candidate admission and frozen-source/fresh-repair
lifecycle); `SL-241` (spike rig slice); `QUE-207` / `DEC-153` (control-plane
binary and crate topology — question 13, answered).

Governance and implementation: `ADR-020` (execution capsules as the dispatch
authority boundary); `SPEC-030` (the forward-intent technical contract, all
fourteen requirements still `pending`); `REV-046` (`proposed` — the cutover
revision, slice 5); `SL-248` (slice 1, at `reconcile`) and its reconciliation
audit `RV-352`; `ADR-021` (`proposed` — `unsafe_code` `deny` with a two-site
budget, from `SL-248`'s backend).

Current-model connective tissue, mapped in the census: `SPEC-012` — verb
mechanism, worker-mode guard, provisioning/`.worktreeinclude` (the manifest
seed), import belt (→ forbidden-path admission); selectors and declared/
changed conformance ride `REQ-316`'s provenance gate and the selector
algebra cited there.

* `SPEC-021` — current orchestrator process, funnel cadence, routing and per-harness authority altitude;
* `SPEC-022` — current Git ref, evidence, candidate, journal and integration model;
* `RFC-018` — empirical Claude Code harness findings and artifact map;
* `RFC-011` — dispatch token-efficiency benchmark (the measurement lens);
* `RFC-022` / `RFC-023` — agent trust and adversarial TDD (own the
  verdict-honesty gap identified by `red-team.md`'s RT-1 verification finding);
* `RFC-006` — the retired auto-merge posture question 8 must not rebuild.
