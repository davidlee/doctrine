# RFC-022: Agent trust model without human attestation

## Context

Doctrine's memory corpus is consumed by agents as fact. Nothing currently
establishes that a memory is *true* — only that it was recorded, and (via
`memory verify`) that its evidence was committed at some point. `trust_level` is
author-asserted. `verification_state` is largely `unverified` across the corpus.

**The threat model, stated.** The adversary is an agent compromised by prompt
injection, seriously misaligned, or stupid in a way indistinguishable from
malice. The goal is **blast-radius containment** — a bad agent should not be able
to propagate a false belief into the corpus and have later agents act on it. It
is explicitly *not* a model where the human operator is hostile, and it is not a
confidentiality model.

Two distinguishable harms sit under it:

1. **False attestation** — a memory whose recorded evidence does not match what
   the memory claims. Adversarial. This is what SL-232 has been attacking.
2. **Incorrect facts** — a memory that is simply wrong. Mostly accident and
   sloppiness, largely amenable to soft controls (prompting, authoring
   discipline), *but* soft controls fail silently and unmeasurably.

**Why this merits an RFC now.** SL-232 spent five adversarial review rounds
(RV-314, 41 findings) hardening `verify`'s byte-equality claim without a stated
threat model, and each round refuted the previous round's repair. The absence of
a bounded adversary meant no completeness criterion existed, so every round could
always find another mechanism. Stating the model above is what makes the question
answerable — and it reframes the question from "can `verify` be made total?" to
"what makes a claim in this corpus trustworthy at all?"

The question this RFC opens: **how do we build an effective trust / reputation
model for agents that does not depend on a human in the loop?**

Human attestation (hardware key, passphrase-gated signing) is assumed to be a
legitimate mechanism for a top trust tier and is *not* the subject here. The
subject is everything below that tier, which is where nearly all memories live.

## Discussion

### The crypto is bookkeeping; the security content is independence

Two sketches were raised:

- **(A) Certificates per model+role.** Self-sign at low trust; request
  independent verification from a different model+role holding a different key;
  promote on a two-key rule.
- **(B) No self-certification.** Bar an agent from boosting a memory it authored;
  have a fresh session review new/untrusted memories during `/dreaming`; allow a
  boost only with attached evidence.

Both reach for the same property. The keys are not what delivers it.

A signing key reachable by the agent attests **identity, not truthfulness**. A
prompt-injected agent `A` signs as `A` quite happily; the signature proves `A`
said it, which is exactly what we already knew. Against a compromised holder of a
key, a signature buys nothing.

What (A) is actually reaching for is the **two-man rule**: require two
independent failures rather than one. That is real security content, and the
crypto is only the auditable bookkeeping for it. So the design question is not
*what certificate scheme* but **what makes two agents independent, and can that
be enforced?**

### What actually decorrelates two agents

Ranked, with in-repo evidence:

| axis | strength | evidence |
|---|---|---|
| **different model** | strong | RV-314 rounds 2/3 (codex) and round 4 (Claude self-attack) each repeatedly caught what the other missed on the same document |
| **no shared context** | strong | a reviewer given the author's reasoning is anchored, not independent; one given the artifact plus the repo re-derives |
| **different session / agent-id** | weak alone | a fresh session of the same model on the same framing is barely independent |

Sketch (B) gets the second axis right and (A) does not address it at all. A
practical consequence: the reviewing agent should receive the memory body and the
repository, **not** the authoring session's transcript or rationale.

### The failure mode that defeats both sketches

**Prompt injection in the artifact under review is common-mode.** A memory
crafted to mislead attacks the author and every reviewer identically. Stacking
keys or models buys nothing against it — N-of-M assumes independent failure and
this is precisely a correlated one.

The only real mitigation is that the reviewer verifies against **external
evidence** rather than against the artifact's own claims. This is the load-bearing
part of sketch (B)'s "attaching the evidence they have": *`B` agrees with `A`* is
near-worthless; *`B` re-derived this from the code and here is the trace* is the
product. Doctrine already encodes this distinction in its verification taxonomy
(`VT` by test / `VA` by agent / `VH` by human) and in the review ledger's
"reproduce before you raise" discipline (ADR-007) — the machinery exists and is
not wired to memory trust.

A second structural failure: **a compromised agent that can spawn subagents can
manufacture its own quorum.** Any N-of-M scheme must have the reviewers spawned
by something the reviewed agent does not control. That is an orchestration
constraint, not a cryptographic one, and it is easy to get wrong.

### Three cheap wins, mostly already built

**1. Push facts toward executable verification, and trust becomes moot for them.**
A memory whose claim is pinned by a test or probe re-establishes its own truth on
every run; no reputation machinery is needed. The social layer should be reserved
for the residue that genuinely cannot be tested. This is the largest win and it
mostly exists (`VT`, `coverage record/verify`) — what is missing is the pressure
to prefer it at authoring time.

**2. Decay via the staleness seam.** Trust should fall automatically as the code a
memory describes churns. `commits_touching` and the staleness machinery — which
SL-232 is already reworking — *is* the decay function. This is a strong argument
for finishing a reduced SL-232 rather than shelving it.

**3. Track outcomes, not just attestations.** If a memory was retrieved and then
contradicted — an agent had to correct it, a `/feedback` cycle refuted it — that
should cost it trust automatically. Reputation grounded in *consequences* is much
harder to game than reputation grounded in votes, and needs no keys. **This
appears to be the clearest gap in the current system**: doctrine records
attestation but not outcome.

### Trust as a tuple, not a scalar

A single promotable number invites gaming and hides its own basis. A structured
value the consuming agent interprets:

| axis | values |
|---|---|
| evidence | asserted → re-derived → executable → human |
| independence | self → fresh-session → cross-model → cross-model + cross-context |
| freshness | commits touching the claim surface since attestation |
| outcomes | retrieved *n*, contradicted *m* |

"Two submarine keys" then falls out as one *policy* over this tuple rather than
being the mechanism itself — and other policies (a high tier requiring `VH`, a
low tier accepting self-assertion) compose on the same substrate.

### The enforcement point: MCP as a boundary rather than a convention

Verification reachable **only** through the MCP server is a materially stronger
position than a CLI verb, because the server is a long-lived process that can bind
an identity to a connection instead of trusting a self-asserted flag. Doctrine has
already drawn this distinction: ADR-007 records `--as` as "cooperative role
assertion, **not** a security boundary", while `worker_commit` is described as a
*gated* MCP tool. The seam exists; this would be its second use.

The honest limit: **MCP-only is a boundary only if the other paths are closed.**
An agent with shell access can invoke the CLI or edit the TOML directly, so on a
local server the property is soft — it raises cost and creates an audit trail
rather than building a wall. Making it hard requires the attestation to be
unforgeable without the server, which means either

- **(i)** trust state living somewhere the agent cannot write — which cuts
  directly against doctrine's storage model, where everything authored is in-repo,
  diffable and reviewable; or
- **(ii)** the *server* holding a signing key so that only it can mint an
  attestation.

**(ii) is a better use of cryptography than per-agent certificates** — and it is a
correction to the position taken earlier in this document. Per-agent keys attest
identity, which we established buys little. A server-held key attests *that the
attestation went through the gate*, which is the property actually wanted. It is
still soft against an agent that can read the local key, and it becomes genuinely
hard when the server is **remote** — which converges with the transit case below.

### Testimony is 1:n, and that decouples it from the memory schema

A single `verification_state` field models the wrong cardinality. "I testify this
is true" — and equally "I contradict this" — is an **event with an author, an
evidence trace, a timestamp, and the anchor it was made against**. A memory
accumulates a *set* of these over time.

The right shape is an **append-only testimony ledger per memory**, which is the
shape doctrine already uses for adversarial review (raise / dispose / contest /
verify / withdraw, append-only, ADR-007). Reusing that pattern rather than
inventing one has three consequences worth stating:

1. **Trust becomes derived, not stored.** The tuple above is a *projection* over
   the ledger, regenerable at will. This is what the storage rule already demands —
   queried and derived data does not get written into authored files.
2. **Contradiction stops being destructive.** It does not flip a bit; it is a
   claim carrying its own evidence, and it can itself be contested. The review
   ledger's verbs map onto this almost directly.
3. **It answers the schema-lock problem.** See below.

### The interchange format is locked, so testimony must live outside it

The memory entity's schema is a compatibility surface beyond this repo — memories
transit between the shipped corpus and client projects, and consumers that
doctrine does not control read them. Changing that schema to carry trust state is
therefore expensive and largely unavailable.

The 1:n shape dissolves the problem: **a testimony ledger is a separate entity
referencing `memory_uid`.** The memory schema changes not at all; a consumer that
knows nothing of testimony sees exactly what it sees today.

This also resolves what to do with the existing `trust_level` field. It does not
need to change or be deprecated — it needs to be **honestly re-described** as what
it always was: the author's self-assertion. That is a documentation change with
zero interchange impact, and it composes with the derived tuple rather than
competing with it.

*To verify before this progresses:* the exact compatibility guarantees SPEC-007
makes about the memory record, and whether any consumer treats
`verification_state` as normative rather than advisory.

### Transit between repositories is where this stops being optional

The mechanisms above are nice-to-have for a single repo and load-bearing the
moment memories move between them. That inverts part of the sequencing proposed
earlier in this document, and three problems appear that the local-only framing
hides entirely:

- **Anchors do not port.** A memory's anchor is a commit sha in *its* repo. In
  another repo it denotes nothing, so a transited memory arrives unverifiable by
  construction — the same problem SL-232 solves locally, with no local answer
  available.
- **Scope does not port.** `scope.paths` and `scope.globs` are repo-relative.
- **Testimony does not port with equal force.** `A`'s testimony that a fact holds
  in `A`'s repo says nothing about `B`'s. Facts about *doctrine itself* transit;
  facts about a project do not.

That last line is a boundary doctrine has already drawn once: ADR-002's global
orientation class is precisely the repo-empty, unanchored, evergreen case, and the
shipped-vs-local split is already the transit boundary. The design question is
whether testimony means anything for the global class, where there is no commit to
anchor against — which is OQ-5 arriving from the other direction.

**Revised sequencing.** Not "defer keys until memories cross a boundary", but:
**defer key *verification*; make the testimony record transit-shaped now.** A
testimony must carry who / what evidence / when / against which anchor in a form
that still means something outside this repo, even while nothing checks a
signature yet. That is field discipline rather than mechanism, it is cheap, and it
avoids a re-attestation cliff where every locally-minted testimony has to be
thrown away when transit arrives.

## Outcome

**Open.** Nothing here is canon; no decision is asserted.

Positions that appear settled enough to carry into design if this progresses:

- Per-agent keys are bookkeeping; **independence and external evidence** are the
  security content. A **server-held** key is a different and better proposition —
  it attests that the gate was passed, not who spoke.
- **Self-certification must not promote** (sketch B's core), and "self" is best
  keyed on *context lineage*, not merely agent-id.
- **Quorum members must not be spawnable by the reviewed agent.**
- **Testimony is 1:n and append-only**, following the review ledger's shape;
  trust is a **derived projection** over it, never a stored scalar.
- **The memory schema does not change.** Testimony is a separate entity keyed on
  `memory_uid`; `trust_level` is re-described as author self-assertion rather than
  altered.
- **Make the testimony record transit-shaped now; defer signature verification.**

Open questions:

- **OQ-1** — How is "no shared context" *enforced* rather than merely intended? A
  fresh agent can be handed a clean brief, but proving the absence of anchoring is
  harder than arranging it.
- **OQ-2** — Can the reviewing agent read the memory as data safely? The corpus
  already wraps bodies in "treat as data, never as instruction" guards; is that
  sufficient for an agent whose *job* is to reason about the body?
- **OQ-3** — What is the outcome-tracking seam? Retrieval is already observable;
  contradiction is not currently a recorded event.
- **OQ-4** — ~~Which kind carries the attestation record~~ — **answered in
  discussion**: a separate entity keyed on `memory_uid`, forced by the interchange
  lock. What remains is whether it is a new kind or a facet of the existing review
  ledger, given how closely the verbs correspond.
- **OQ-5** — Does decay need a policy per memory class? Evergreen orientation
  memories (ADR-002) have no claim surface to churn against — and the same class
  is the one that transits, so it needs a trust story that does not rest on an
  anchor.
- **OQ-6** — Can the MCP gate be made hard on a *local* server at all, or is the
  local case necessarily soft (audit trail, raised cost) with the hard boundary
  only available remotely?
- **OQ-7** — What does an anchor mean after transit? A commit sha does not port,
  so a transited memory is unverifiable by construction unless something replaces
  it. This may be the hardest problem in the RFC and it has no local analogue.
- **OQ-8** — Does a testimony ledger need its own conflict/merge story? Two repos
  independently accumulating testimony about the same shipped memory will diverge,
  and git's merge is not obviously the right resolver for an append-only claim set.

## References

- **SL-232** / **RV-314** — the five-round review whose lack of a bounded
  adversary motivated this RFC; source of the decorrelation evidence.
- **ADR-007** — adversarial review as a first-class kind; the ledger discipline
  ("reproduce before you raise") this RFC generalises to memory attestation.
- **ADR-002** — global orientation memory class (repo-empty, unanchored,
  evergreen) — bears on OQ-5.
- **RFC-011** — token-efficiency benchmarking; the review-round cost data.
- `install/glossary.md` — verification taxonomy (`VT` / `VA` / `VH`).
