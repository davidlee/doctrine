# RFC-018: Claude harness field notes: empirical findings and design-artifact map

## Context

Doctrine's dispatch / worktree / workflow machinery runs *on top of* the Claude
Code harness, whose spawn, isolation, hook, commit, and MCP semantics are only
partially documented and shift across releases. Repeatedly, correctness turns on
an **empirical** property of the harness (the Workflow primitive strips the
`Agent` tool; a linked worktree's `.git` is RO in the jail; `SubagentStop` fires
tree-intact) that no spec asserts and that cost real probe-sessions to establish.

Those findings are already durable in the memory corpus — but scattered across
~35 `mem.*` items and several RFC case-notes, discoverable only by someone who
knows the key. This RFC is **not** a decision and asserts **no** canon. It is a
**field notebook**: a curated, grouped index of what we have empirically learned
about the harness, plus a map of the significant design artifacts for the four
subsystems that ride it (dispatch, workflow, worktree, subagents).

Memory items remain source-of-truth; the entries below are one-line pointers, not
copies. Cite the `mem.*` key or artifact id, then read it via `doctrine memory
show` / `doctrine <kind> show`.

## Discussion

### 1. Empirical findings — the Claude Code harness

Grouped by seam. Each row: **finding → `mem.*` key**. Read the item for proof,
probe id, and consequence.

#### 1a. Subagent spawning & the `Agent` tool

| Finding | Memory |
|---|---|
| Workflow runtime **unconditionally strips `Agent`** from every `agent()` leaf — even a `Tools:*` def; leaves cannot nest-spawn. Also strips Grep/Glob, injects `StructuredOutput`. | `mem.fact.claude.workflow-strips-agent-tool` |
| Frontmatter `isolation:` **is** honored for nested (Agent-tool) subagent spawns. | `mem.fact.claude.frontmatter-isolation-honored-nested` |
| A `dispatch-worker` Agent spawn needs `isolation:worktree` explicitly — omission runs in-place in the coord tree, no fork. | `mem.fact.dispatch.claude-arm-spawn-needs-isolation-worktree-flag` |

#### 1b. `isolation:worktree` mechanics — base, minting, teardown

| Finding | Memory |
|---|---|
| `WorktreeCreate` plugin hook fires (2.1.198); `-w` bypasses it. | `mem.fact.claude.worktreecreate-hook-fires` |
| Teardown (auto-remove) is **conditional on** the WorktreeCreate hook having run. | `mem.fact.claude.worktree-remove-auto-teardown` |
| WorktreeCreate payload is minimal — **no type, no target path, no base**; use SubagentStart to stamp identity. | `mem.pattern.dispatch.claude-worktreecreate-payload-minimal-no-type-no-path` |
| WorktreeCreate payload `cwd` = session working-dir → an orchestrator-controlled **per-spawn channel**. | `mem.fact.dispatch.worktreecreate-cwd-channel` |
| `CLAUDE_PROJECT_DIR` is present in the worktree-subagent hook env. | `mem.fact.claude.project-dir-in-worktree-subagent-hook-env` |
| Agent `isolation:worktree` base = the spawning session's local HEAD (`baseRef=head`) — controllable by **placement**, not ref-redirect. | `mem.pattern.dispatch.claude-isolation-worktree-forks-orchestrator-session-head` |
| …restated: base is session-root main and **opaque** — not the coord-tree tip. | `mem.pattern.dispatch.claude-isolation-worktree-base-session-root-opaque` |
| Agent worker worktree base is **uncontrollable** (tracks origin/main) → run dependent serial phases inline on the coord tree. | `mem.pattern.dispatch.claude-agent-worktree-forks-origin-main-tracking-ref` |
| Agent worker commit **integrates onto the parent branch**, not an isolated fork. | `mem.pattern.dispatch.claude-agent-worktree-integrates-commit-onto-parent` |
| Agent worktree is **harness-born, not fork-provisioned** — a creation hook must provision+stamp (WorktreeCreate, fail-closed). | `mem.pattern.dispatch.claude-agent-worktree-not-fork-provisioned` |
| Fork path: worktree **persists** + footer carries `worktreePath` — proven live. | `mem.fact.dispatch.claude-fork-path-persist-footer-proven` |
| Workflow `agent(isolation:worktree)` **fires** the WorktreeCreate hook; payload cwd = session working-dir. | `mem.fact.workflow.agent-worktree-fires-create-fork-hook` |
| Workflow `agent(isolation:worktree)` mints the confined fork **AT the armed base** (positional arming), not cwd HEAD. | `mem.fact.workflow.confined-fork-mints-at-armed-base` |
| Native worktree isolation is **tool-layer only** until `/sandbox`, and its layers **disagree** with each other (SL-250 `VH-1`). | `mem.fact.claude.native-worktree-isolation-is-tool-layer-only` |

#### 1c. Commit path & worker identity

| Finding | Memory |
|---|---|
| A Workflow `isolation:worktree` leaf **cannot commit in the jail** — RO shared `.git`, and it is not a registered `DispatchRecord` so `worker_commit` refuses. Both commit paths closed. | `mem.fact.claude.workflow-leaf-cannot-commit` |
| The jail's **RO `.git` is the bwrap WRAP on a confined Bash, not an fs permission** — `PassThrough` (main thread, or an `agent_id` nominated via a `SubagentStart` allowlist) runs unwrapped ⇒ **RW `.git` everywhere ⇒ commits**. A nominated subagent commits like the main thread — no `worker_commit`/`DispatchRecord` needed (SL-206 P1 proven). | `mem.fact.claude.jail-ro-git-is-bwrap-wrap-passthrough-commits` |
| Claude-arm worker stamps a worker marker on the coord tree and commits onto the coord branch (expected). | `mem.pattern.dispatch.claude-arm-isolation-fallback` |
| Worker identity via **SubagentStart** hook, not a SessionStart env-seam. | `mem.pattern.dispatch.claude-subagentstart-worker-identity` |
| Claude dispatch worker **cannot get per-worktree env** via hooks. | `mem.fact.dispatch.claude-worker-no-per-worktree-env` |

#### 1d. Hooks — PreToolUse, SubagentStop, confinement

| Finding | Memory |
|---|---|
| PreToolUse hooks **fail OPEN** — only exit 2 blocks. | `mem.fact.claude.pretooluse-hook-fail-open` |
| SubagentStop fires **awaited and tree-intact** before WorktreeRemove — the capture seam. | `mem.fact.claude.subagentstop-awaited-tree-intact-capture-seam` |
| **SubagentStart FIRES for an Agent-tool spawn even from a child-session main thread** (`CLAUDE_CODE_CHILD_SESSION=1`); matches on `agent_type`, sync-blocking write lands before the subagent's first PreToolUse. Narrows the "child-session silently no-fires" claim. Also: a `settings.local.json` hooks edit **hot-reloaded** (no restart). | `mem.fact.claude.subagentstart-fires-from-child-session` |
| `/reload-plugins` registers plugin PreToolUse hooks, no restart. | `mem.fact.claude.reload-plugins-registers-pretooluse` |
| SL-182 PreToolUse jail walls **only Bash/Edit/Write**; other tools pass through. | `mem.fact.dispatch.pretooluse-wall-mediates-write-tools-only` |
| Confine `isolation:worktree` subagents via PreToolUse(Bash) + nested bwrap. | `mem.pattern.dispatch.claude-worktree-subagent-bwrap-confinement` |

#### 1e. MCP reachability

| Finding | Memory |
|---|---|
| `isolation:worktree` forks reach doctrine MCP with a clean binary — MCP-funnel viable for a workflow-spawned orchestrator. | `mem.fact.workflow.isolated-fork-reaches-doctrine-mcp` |
| STDIO-shim footgun: wrapping `${DOCTRINE_BIN} serve --mcp` breaks fork-spawned MCP servers. | `mem.fact.dispatch.mcp-stdio-shim-breaks-fork-servers` |

#### 1f. Confinement, cwd & arming

| Finding | Memory |
|---|---|
| Confined subagent Bash **cwd resets between calls** — breaks positional-arming fork discrimination. | `mem.fact.dispatch.confined-subagent-cwd-resets-breaks-positional-arming` |
| Dispatch arming is **single-slot** — per-arming policy granularity. | `mem.fact.dispatch.single-slot-arming-rendezvous` |
| Confined orchestrator drive-loop is **realizable** on the claude arm. | `mem.fact.dispatch.confined-orchestrator-driveloop-realizable` |
| claude-arm dispatch: place coordination worktree **inside the cwd-jail**; funnel workarounds. | `mem.pattern.dispatch.claude-arm-coord-placement` |
| A worktree subagent's filesystem view **lies in both directions** — the gitdir pointer is unresolvable from inside (SL-250 `VH-1`). | `mem.fact.worktree.gitdir-pointer-unresolvable-in-sandboxed-subagent` |

#### 1g. Escalation / human-in-loop

| Finding | Memory |
|---|---|
| A background Workflow worker has **no live escalation channel** — human-in-loop is return-value only. | `mem.fact.workflow.no-live-escalation-channel-from-worker` |

#### 1h. Plugins, install, trust

| Finding | Memory |
|---|---|
| Claude Code project-local **plugin load model**. | `mem.system.claude.plugin-load-model` |
| Claude Code trust: **three independent layers**. | `mem.concept.claude.trust-layers` |
| `plugin marketplace add` **overwrites source in place**; refresh needs no remove+add. | `mem.fact.claude.marketplace-add-overwrites-source` |
| `claude install` agents `.gitignore` is too broad. | `mem.pattern.distribution.claude-install-agents-gitignore-too-broad` |
| Direct-write vs plugin activation is an **asymmetry, not a subset** — see below. | SL-250; `install/claude-activation.md` § *Escape hatch* |

**The `strictPluginOnlyCustomization` asymmetry (SL-250, 2026-08-08).** The
tempting framing — "direct-write is strictly safer than the plugin channel" — is
false, and the shipped doc says so. Direct-write **sheds** two plugin-only failure
modes: the orphaned-marketplace trap (an `enabledPlugins` entry whose marketplace
is unregistered is skipped with `Skipping orphaned enabledPlugins entry` — no
error, plugin simply absent) and the managed-only gate that admits plugin hooks
only when the plugin id appears in *managed policy* `enabledPlugins`. It
**retains** `disableAllHooks`, `allowManagedHooksOnly` and folder trust unchanged.
And it **acquires** one: `strictPluginOnlyCustomization`, a managed-settings policy
that permits hooks only from plugins, which direct-write cannot satisfy at all —
`doctrine install` writes the file and Claude Code declines to load from it.

So the honest claim is *fewer failure modes overall, not a strict improvement on
every axis*. Where that policy is enforced, the plugin path is not a fallback for
the cautious; it is the only path that works, which is one of the reasons SL-250
retired the plugin *delivery* path while keeping `.claude-plugin/marketplace.json`
published.

The asymmetry is also the argument for IMP-407's doctor leg: with three surviving
gates plus one acquired one, an inert install has four possible causes and no
surface currently names which.

#### 1i. Method (meta) — how these were established

| Finding | Memory |
|---|---|
| Claude Code **docs are the ground-truth source** (docs/claude cache), not web summaries. | `mem.fact.claude.docs-ground-truth-source` |
| **Docs-first, then probe** — don't thrash empirically on documented semantics. | `mem.pattern.claude.docs-first-then-probe` |
| How to **empirically introspect** harness behavior. | `mem.pattern.claude.harness-introspection` |

#### 1j. Auth — the OAuth credential lineage

| Finding | Memory |
|---|---|
| Refresh **rotates unconditionally**; one refresher per lineage. A split home forks it and the losing client **zeroes its own credential file**. | `mem.fact.claude.oauth-refresh-rotation-single-writer` |
| Access TTL is **8h, fixed** (`expires_in` sent by the client, ignored by the server); refresh-token expiry is **absolute ~4.8d**, not sliding → re-login is routine, not exceptional. | ″ (same item); `EVD-024` |
| No hardware fingerprint on the wire — `userID`/`machineID` are random-and-persisted telemetry ids. | `EVD-024` § F4 |
| **All of the above is `/login`-grant behaviour.** `claude setup-token` mints a **one-year** token saved nowhere, which never joins the lineage — so per-agent grants avoid the whole problem. | ″ (same item); `EVD-024` § F10 |
| The pipe is `CLAUDE_CODE_OAUTH_TOKEN` (`_FILE_DESCRIPTOR` variant keeps it off the guest filesystem). Not `apiKeyHelper`: OAuth vs API key is a branch on credential *shape*, and the helper's untyped string lands in both headers. | `EVD-024` §§ F7, F9, F11 |

**Why this sits here (2026-08-14).** A shared home keeping every agent logged in
looks like shared *identity*; it is shared **concurrency control** — a
compare-and-swap on one `.credentials.json`, with an explicit `adopted_sibling`
branch for the loser. The standing folk-explanation (Anthropic fingerprints the
machine and busts mismatched sessions) was tested and **is not supported**: the
logout is executed locally by `Nfr()` on `invalid_grant`, which is why re-copying
a known-good credential does not rescue a forked home.

`EVD-024` carries the method, the minified identifiers to re-grep, and the live
numbers. It is the load-bearing input to any design that fans agents out into
isolated homes (`microvm-spike` capsules, jails, dispatch worktrees): exactly one
process may hold a given lineage, and everything else takes an access-token-only
credential, which the client accepts and never persists.

The sharp turn came last: **the lineage problem is avoidable.** A `setup-token`
grant is year-long, saved nowhere, and separate from `.credentials.json`, so
per-agent grants sidestep rotation, the CAS, and the ~5-day clock altogether. The
remaining question is not mechanism but containment — a year-long bearer
credential inside an agent that is confined precisely because it is not trusted.
`EVD-024` § *Where that leaves a fan-out design* states the trade without
deciding it.

### 2. Design-artifact map — dispatch / workflow / worktree / subagents

Read each via `doctrine <kind> show <id>` (specs/ADRs/RFCs/slices); skill and
agent-def markdown by path. Some rows are **planned** (design-target, not yet on
tree) — flagged inline.

#### 2a. Dispatch — orchestration funnel + integration/git seam

| id | covers |
|---|---|
| ADR-012 | Integration topology: isolated coordination worktree, class-routed projection, preserved code branches |
| SPEC-021 (tech) | Orchestrator process — funnel cadence contract, arm routing, concurrency, harness altitude, confined-orchestrator tier |
| SPEC-012 (tech) | Dispatch & worktree — provisioning, worker-sole-writer, branch-point check, verb families (also §2b) |
| SPEC-022 (tech) | Git interaction model — ref taxonomy, CAS journal, two-stage audit-gated projection, candidate layer |
| PRD-015 (product) | Dispatch & worktree product intent — isolation, sole-writer, crash-safety, harness/policy-agnostic (also §2b) |
| RFC-005 | Funnel integrity: hazard survey + correctness posture |
| RFC-006 | Auto-merge non-FF trunk at integrate: reversing FF-only posture |
| RFC-016 | Zero-rescue dispatch: invariants into verbs, lineage rows |
| skill:dispatch | `.agents/skills/dispatch/SKILL.md` — router, outer loop + funnel cadence, arm routing, red flags |
| skill:dispatch-agent | `.agents/skills/dispatch-agent/SKILL.md` — claude arm, Agent-tool worker spawn, explicit base via `arm-spawn --base B` |
| skill:dispatch-subprocess | `.agents/skills/dispatch-subprocess/SKILL.md` — codex/pi arm, `worktree fork --worker` + subprocess cwd binding |
| ref:dispatch-mechanics | `install/dispatch-mechanics.md` (installed `.doctrine/dispatch-mechanics.md`) — fork→verify→import→land funnel, git invariants, failure modes |

#### 2b. Worktree — isolation + jail confinement

| id | covers |
|---|---|
| ADR-006 | Worktree posture: policy-agnostic framework, orchestrator-sole-writer dispatch |
| ADR-008 | Project-local jail build isolation + worker confinement for parallel dispatch (also §2d) |
| skill:worktree | `.agents/skills/worktree/SKILL.md` — `doctrine worktree fork` (sole create+provision+mark), commit-before-spawn / branch-point / baseline guards |
| SPEC-012, PRD-015, RFC-012 | shared with §2a / §2c — provisioning + isolation boundary |

#### 2c. Workflow — the Claude Code Workflow primitive / drive-slice

| id | covers |
|---|---|
| SL-206 | Workflow-templated slice-driver — drive phases from a Workflow (JS harness) via confined orchestrator + `dispatch_import`/`conclude_phase`/`reap` MCP funnel; `governed_by` ADR-006/011/012, from SL-199. `design.md` present |
| SL-209 | drive-slice acceptance rig — status `proposed`, **no design.md yet** |
| skill:drive-slice | session skill; in-repo home is the workflow script `install/workflows/drive-slice.js` (SL-206 PHASE-04 authored home) |
| RFC-011 | Dispatch token efficiency — budget-pool metering lever the workflow driver exploits (also §2a) |
| RFC-012 | Parallel fan-out safety: containment, disjointness, shared responsibility (also §2b) |

#### 2d. Subagents / Agent-tool spawning

| id | covers |
|---|---|
| ADR-011 | Harness-agnostic orchestrator spawn interface + per-harness capability altitude |
| ADR-008 | Jail isolation / worker confinement of spawned agents (also §2b) |
| SPEC-021 | Harness-altitude contract + confined-orchestrator mediated-write tier |
| agent-def:dispatch-orchestrator | `install/agents/claude/dispatch-orchestrator.md` — confined orchestrator: coord-tree jail, nested-spawns workers, MCP-only git-boundary writes, no `worker_commit` |
| agent-def:dispatch-worker | `install/agents/claude/dispatch-worker.md` — `isolation: worktree`, ONE phase → single delta via `worker_commit`; never touches `.doctrine/`/`.claude/` |
| agent-def:dispatch-probe | `install/agents/claude/dispatch-probe.md` — divergence-probe def; **SL-206 design-target, planned** |

**Deeper lineage (referenced, not indexed):** SL-199 (confined-orchestrator
origin), SL-064 (dispatch topology impl), SL-068 (candidate layer), SL-182
(fan-out). SL-208 (cozy-table `--help`) touches dispatch/worktree only as example
CLI surfaces — not a subsystem design.

> Method caveat (Explore sweep): Bash was walled by the `worktree-jail` hook and
> directory listing errored, so artifacts were reached by canonical path +
> relation edges. Unreferenced specs may exist that the sweep could not
> enumerate; treat §2 as high-signal, not exhaustive.

## Outcome

Open by construction — this is a living notebook, not a resolved question. Append
findings as the harness is probed; append artifact pointers as designs land.
Individual `mem.*` items remain the source of truth and the place to correct a
finding; this RFC is the index over them.

## References

- RFC-011 (Dispatch Token Efficiency) — case-notes corpus, sibling instrumentation.
- RFC-012, RFC-014, RFC-016 — parallel-safety / multi-harness / zero-rescue dispatch.
- The `mem.*` keys inline above (read via `doctrine memory show <key>`).
<!-- + design artifacts once §2 is filled -->
