# use flake:
/.direnv
/.envrc
/.claude
/.agents
/.pi
/.codex
/.dirge
/.mcp.json
/.cursor

*.tmp
*.temp
*.local.*

.dispatch

node_modules

docs/claude/**/*.md
docs/claude/**/*.txt

# for oops
.spec-driver


handover.md
/scratch
/tmp

# Added by cargo

/target

# nix build symlink
/result

.worktrees/
.doctrine/state/
.doctrine/skills/*
.doctrine/memory/index/*
.doctrine/memory/embeddings/*
.doctrine/memory/state/*
.doctrine/memory/shipped/
.doctrine/slice/*/phases
.doctrine/slice/*/handover.md
.doctrine/slice/*/inquisition.md
.doctrine/slice/*/research/
.doctrine/**/handover.md
.doctrine/agents/*
.doctrine/dispatch/
!.doctrine/agents/AGENTS.md

# Observation records are AUTHORED — committed and visible in review.
# Only the reserved publication temporaries an interrupted capture can
# leave behind are ignored (SL-231; fsutil::PUBLICATION_TEMP_PREFIX).
.doctrine/observations/**/.tmp.*

# SL-241 capsule spike, OQ-1 v0 ruling. The probe SUMMARIES and the generated
# measurement table under .doctrine/rfc/025/evidence/ are authored and
# committed; the raw run logs they summarise are the exhibit, not the
# evidence, and stay out of the corpus (design.md 5.3).
.doctrine/rfc/025/evidence/raw/

web/map/dist/
web/map/node_modules/

/.harness/
