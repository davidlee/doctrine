schema = "doctrine.spec.product"
version = 1

id = {{id}}
slug = {{slug}}
title = {{title}}
status = "draft"
kind = "product"
tags = []

# product_level — closed product ladder: domain | capability | feature | story. Add as:
#   product_level = "capability"
# parent — single decomposition parent (PRD-NNN, product→product); the tree must
#   stay acyclic. Add as:
#   parent = "PRD-001"
